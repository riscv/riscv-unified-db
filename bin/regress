#!/bin/bash
ROOT=$(dirname "$(dirname "$(realpath "${BASH_SOURCE[0]}")")")
source "${ROOT}/bin/.mise.sh"

cd "$ROOT" || exit 1

exec mise exec -- bundle exec ruby "${ROOT}/tools/test/regress-cli.rb" "$@"
