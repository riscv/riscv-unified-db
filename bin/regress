#!/bin/bash
ROOT=$(dirname "$(dirname "$(realpath "${BASH_SOURCE[0]}")")")

EXEC_DIR=$(pwd)
cd "$ROOT" || exit 1

exec "${ROOT}/bin/mise" exec --cd "${EXEC_DIR}" -- bundle exec ruby "${ROOT}/tools/test/regress-cli.rb" "$@"
