#!/bin/bash
ROOT=$(dirname "$(dirname "$(realpath "${BASH_SOURCE[0]}")")")

EXEC_DIR=$(pwd)
cd "$ROOT" || exit 1

exec "${ROOT}/bin/mise" exec -- bash -c 'cd "$1" && exec bundle exec ruby "$2" "${@:3}"' _ "$EXEC_DIR" "${ROOT}/tools/test/regress-cli.rb" "$@"
