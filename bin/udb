#!/bin/bash
ROOT=$(dirname "$(dirname "$(realpath "${BASH_SOURCE[0]}")")")
EXEC_DIR=${PWD}
cd "$ROOT"
BUNDLE_GEMFILE="${ROOT}/tools/ruby-gems/udb/Gemfile" exec mise exec --cd "$EXEC_DIR" -- bundle exec udb "$@"
