name: Build and release UDB gem dependencies to GitHub

# Builds and uploads prebuilt eqntott, espresso, must, and/or z3 binaries as GitHub release assets.
#
# Runs automatically whenever a Renovate PR bumps a tool's *_VERSION file and
# lands on main. Can also be run manually.
#
# The *_VERSION files are the source of truth for dependency release tags:
# z3 uses an upstream release tag, while eqntott/espresso/must use
# <tool>-<git-commit>. The workflow builds and publishes binaries for the
# bumped version.

on:
  push:
    branches: [main]
    paths:
      - "tools/ruby-gems/udb/lib/udb/EQNTOTT_VERSION"
      - "tools/ruby-gems/udb/lib/udb/ESPRESSO_VERSION"
      - "tools/ruby-gems/udb/lib/udb/MUST_VERSION"
      - "tools/ruby-gems/udb/lib/udb/Z3_VERSION"
      - "tools/node/tree-sitter-idl/TS_IDL_VERSION"
  workflow_dispatch:
    inputs:
      tool:
        description: "Tool to build"
        required: true
        type: choice
        options:
          - eqntott
          - espresso
          - must
          - z3
          - tree-sitter-idl
          - all
      force:
        description: "Force rebuild and release even if release already exists"
        required: false
        type: boolean
        default: false

env:
  GITHUB_TOKEN: ${{ secrets.TOKEN }}

jobs:
  detect-tools:
    runs-on: ubuntu-24.04
    outputs:
      tools: ${{ steps.set-tools.outputs.tools }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 2 # Need to check changed files

      - name: Detect which tool(s) to build
        id: set-tools
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            if [[ "${{ inputs.tool }}" == "all" ]]; then
              TOOLS='["eqntott","espresso","must","z3","tree-sitter-idl"]'
            else
              TOOLS='["${{ inputs.tool }}"]'
            fi
          else
            CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD)
            TOOLS="["
            SEPARATOR=""

            if echo "$CHANGED_FILES" | grep -q "^tools/ruby-gems/udb/lib/udb/EQNTOTT_VERSION$"; then
              TOOLS="${TOOLS}${SEPARATOR}\"eqntott\""
              SEPARATOR=","
            fi

            if echo "$CHANGED_FILES" | grep -q "^tools/ruby-gems/udb/lib/udb/ESPRESSO_VERSION$"; then
              TOOLS="${TOOLS}${SEPARATOR}\"espresso\""
              SEPARATOR=","
            fi

            if echo "$CHANGED_FILES" | grep -q "^tools/ruby-gems/udb/lib/udb/MUST_VERSION$"; then
              TOOLS="${TOOLS}${SEPARATOR}\"must\""
              SEPARATOR=","
            fi

            if echo "$CHANGED_FILES" | grep -q "^tools/ruby-gems/udb/lib/udb/Z3_VERSION$"; then
              TOOLS="${TOOLS}${SEPARATOR}\"z3\""
              SEPARATOR=","
            fi

            if echo "$CHANGED_FILES" | grep -q "^tools/node/tree-sitter-idl/TS_IDL_VERSION$"; then
              TOOLS="${TOOLS}${SEPARATOR}\"tree-sitter-idl\""
            fi

            TOOLS="${TOOLS}]"
          fi

          echo "tools=$TOOLS" >> "$GITHUB_OUTPUT"
          echo "Tools to build: $TOOLS"

  build-eqntott:
    needs: detect-tools
    if: contains(fromJson(needs.detect-tools.outputs.tools), 'eqntott')
    name: Build Eqntott (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            arch: x64
          - runner: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Build and upload Eqntott for ${{ matrix.arch }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ inputs.force }}" = "true" ]; then
            ./bin/chore update eqntott -n -f
          else
            ./bin/chore update eqntott -n
          fi

  build-z3:
    needs: detect-tools
    if: contains(fromJson(needs.detect-tools.outputs.tools), 'z3')
    name: Build Z3 (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            arch: x64
          - runner: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Build and upload Z3 for ${{ matrix.arch }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ inputs.force }}" = "true" ]; then
            ./bin/chore update z3 -n -f
          else
            ./bin/chore update z3 -n
          fi

  build-espresso:
    needs: detect-tools
    if: contains(fromJson(needs.detect-tools.outputs.tools), 'espresso')
    name: Build Espresso (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            arch: x64
          - runner: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Build and upload Espresso for ${{ matrix.arch }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ inputs.force }}" = "true" ]; then
            ./bin/chore update espresso -n -f
          else
            ./bin/chore update espresso -n
          fi

  build-must:
    needs: detect-tools
    if: contains(fromJson(needs.detect-tools.outputs.tools), 'must')
    name: Build Must (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            arch: x64
          - runner: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Build and upload Must for ${{ matrix.arch }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ inputs.force }}" = "true" ]; then
            ./bin/chore update must -n -f
          else
            ./bin/chore update must -n
          fi

  build-tree-sitter-idl:
    needs: detect-tools
    if: contains(fromJson(needs.detect-tools.outputs.tools), 'tree-sitter-idl')
    name: Build tree-sitter-idl grammar (${{ matrix.os }}-${{ matrix.arch }})
    strategy:
      fail-fast: false
      # The grammar is a native shared object, so it must be built on the
      # matching OS. Each runner builds and uploads only its own (OS, CPU)
      # asset to the shared release.
      matrix:
        include:
          - runner: ubuntu-24.04
            os: linux
            arch: x64
          - runner: ubuntu-24.04-arm
            os: linux
            arch: arm64
          - runner: macos-13
            os: macos
            arch: x64
          - runner: macos-14
            os: macos
            arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Build and upload tree-sitter-idl for ${{ matrix.os }}-${{ matrix.arch }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ inputs.force }}" = "true" ]; then
            ./bin/chore update tree-sitter-idl -n -f
          else
            ./bin/chore update tree-sitter-idl -n
          fi
