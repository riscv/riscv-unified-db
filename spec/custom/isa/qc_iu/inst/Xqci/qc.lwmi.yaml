# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../../schemas/inst_schema.json

$schema: inst_schema.json#
kind: instruction
name: qc.lwmi
long_name: Load word multiple (Immediate)
description: |
  Loads multiple words starting from address (`xs1` + `imm`) to registers, starting from `xd`.
  The number of words is in the `length` immediate.
definedBy:
  allOf:
    - xlen: 32
    - extension:
        name: Xqcilsm
format:
  size: 32
  operands:
    - $ref: inst_operand/qc.uimm8.yaml#
    - $ref: inst_operand/qc.length5-n0.yaml#
    - $ref: inst_operand/xs1.yaml#
    - $ref: inst_operand/xd-n0.yaml#
  opcodes:
    - displayName: funct2
      location: 31-30
      value: 0b01
    - displayName: funct3
      location: 14-12
      value: 0b111
    - $ref: inst_opcode/custom-0.yaml#
assembly: xd, length, uimm(xs1)
access:
  s: always
  u: always
  vs: always
  vu: always
operation(): |
  XReg vaddr = X[xs1] + uimm;
  raise (ExceptionCode::IllegalInstruction, effective_ldst_mode(), $encoding) if ((xd + length) > 32);
  for (U32 i = 0; i < length; i++) {
    X[xd + i] = read_memory<32>(vaddr, $encoding);
    vaddr = vaddr + 4;
  }
