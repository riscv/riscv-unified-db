# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../../schemas/inst_schema.json

$schema: inst_schema.json#
kind: instruction
name: qc.c.setint
long_name: Set interrupt (Register)
description: |
  Set interrupt, interrupt number is in `int`.
definedBy:
  allOf:
    - xlen: 32
    - extension:
        name: Xqciint
assembly: xs1
format:
  size: 16
  operands:
    - { $ref: inst_operand/qc.c.int-n0.yaml# }
  opcodes:
    - displayName: funct3
      location: 15-13
      value: 0b000
    - displayName: f
      location: 12
      value: 0b1
    - displayName: funct5
      value: 0b00010
      location: 6-2
    - { $ref: inst_opcode/c2.yaml# }
access:
  s: always
  u: always
  vs: always
  vu: always
operation(): |
  Bits<12> MCLICIP0_ADDR = CSR[qc.mclicip0].address();

  XReg idx = int / 32;
  XReg bit = int % 32;
  Csr pre_csr = direct_csr_lookup(MCLICIP0_ADDR + idx);
  csr_sw_write(pre_csr, csr_sw_read(pre_csr) | (32'b1 << bit));
