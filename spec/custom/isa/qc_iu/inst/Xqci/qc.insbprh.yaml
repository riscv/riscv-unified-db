# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../../schemas/inst_schema.json

$schema: inst_schema.json#
kind: instruction
name: qc.insbprh
long_name: Insert bits, packed descriptor high part (Register)
description: |
  Insertion of a subset of bits from `xs1` into `xd`.
  The width of the subset is determined by `xs2` bits [29:24] (0..32),
  and the offset of the subset is determined by `xs2` bits [20:16].
  In case when width == 0, the destination register is left unchanged.
  In case when `xs2` bit [29] == 1 width is enforced to 32.
definedBy:
  allOf:
    - xlen: 32
    - extension:
        name: Xqcibm
format:
  size: 32
  operands:
    - $ref: inst_operand/xs1.yaml#
    - $ref: inst_operand/qc.xs2-n0.yaml#
    - $ref: inst_operand/qc.xd-n0-rw.yaml#
  opcodes:
    - displayName: funct7
      location: 31-25
      value: 0b0000011
    - displayName: funct3
      location: 14-12
      value: 0b011
    - $ref: inst_opcode/custom-0.yaml#
assembly: xd, xs1, xs2
access:
  s: always
  u: always
  vs: always
  vu: always
operation(): |
  XReg width_bits = X[xs2][29:24];
  XReg width = (width_bits > 32) ? 32 : width_bits;
  XReg shamt = X[xs2][20:16];
  if (width > 0) {
    XReg mask = ((32'b1 << width) - 1) << shamt;
    XReg orig_val = X[xd];
    X[xd] = (orig_val & ~mask) | ((X[xs1] << shamt) & mask);
  }
