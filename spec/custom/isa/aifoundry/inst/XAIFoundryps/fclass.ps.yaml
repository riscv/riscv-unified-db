# yaml-language-server: $schema=../../../../../schemas/inst_schema.json

$schema: inst_schema.json#
kind: instruction
name: fclass.ps
long_name: Packed single classify
description: |
  Packed single classify - classify each FP32 element.
definedBy:
  anyOf:
    - XAIFoundry
    - XAIFoundryps
base: 32
encoding:
  match: 111000000000-----001-----1111011
  variables:
    - name: rs1
      location: 19-15
    - name: rd
      location: 11-7
assembly: xd, fs1
access:
  s: always
  u: always
  vs: always
  vu: always
