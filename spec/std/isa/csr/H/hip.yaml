# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/csr_schema.json

$schema: "csr_schema.json#"
kind: csr
name: hip
long_name: Hypervisor Interrupt Pending Register
writable: true
description: |
  The `hip` register is an HSXLEN-bit read/write register that indicates pending interrupts at the hypervisor level.
  It contains interrupt-pending bits for both VS-level and hypervisor-specific interrupts.
address: 0x608
priv_mode: S
definedBy:
  extension:
    name: H
sw_read(): |
  XReg res = 0;

  # SGEIP: 1 iff bitwise AND of hgeip and hgeie is nonzero
  if (($bits(CSR[hgeie]) & $bits(CSR[hgeip])) != 0) {
    res[12] = 1'b1;
  }

  # VSEIP: logical OR of hvip.VSEIP and platform-specific sources
  if (CSR[hvip].VSEIP == 1'b1) {
    res[10] = 1'b1;
  }

  # VSTIP: logical OR of hvip.VSTIP and vstimecmp comparison (if Sstc)
  if (implemented?(ExtensionName::Sstc)) {
    if ((CSR[hvip].VSTIP == 1'b1) || ((read_mtime() + CSR[htimedelta].DELTA) >= CSR[vstimecmp].TIME)) {
      res[6] = 1'b1;
    }
  } else {
    if (CSR[hvip].VSTIP == 1'b1) {
      res[6] = 1'b1;
    }
  }

  # VSSIP: alias of hvip.VSSIP
  if (CSR[hvip].VSSIP == 1'b1) {
    res[2] = 1'b1;
  }

  return res;
length: SXLEN
fields:
  # TODO: For each writable bit in sie, the corresponding bit shall be read-only zero in both hip and hie.

  SGEIP:
    location: 12
    type: RO-H
    reset_value: 0
    description: |
      Pending interrupt bit for supervisor guest external interrupts (SGEI).
      This bit is 1 if and only if the logical AND of `hgeip` and `hgeie` is nonzero.

  VSEIP:
    location: 10
    type: RO-H
    reset_value: 0
    description: |
      Pending interrupt bit for VS-level external interrupts (VSEI).
      This bit is the logical OR of `vseip` from `hvip`, the interrupt from `hgeip` selected by `hstatus.VGEIN`,
      and any other external interrupt signal directed to VS-level.

  VSTIP:
    location: 6
    type: RO-H
    reset_value: 0
    description: |
      Pending interrupt bit for VS-level timer interrupts (VSTI).
      This bit is the logical OR of `vstip` from `hvip` and any other timer interrupt directed to VS-level.

  VSSIP:
    location: 2
    type: RO-H
    reset_value: 0
    sw_write(csr_value): |
      # VSSIP in hip is an alias of VSSIP in hvip
      CSR[hvip].VSSIP = csr_value.VSSIP;
      return csr_value.VSSIP;
    description: |
      Pending interrupt bit for VS-level software interrupts (VSSI).
      This bit is an alias of the `vssip` bit in `hvip`.
