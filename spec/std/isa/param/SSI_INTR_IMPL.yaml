# Copyright (c) David_Harris@hmc.edu 13 July 2026
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: SSI_INTR_IMPL
description: |
  Whether or not supervisor-level software interrupts are implemented.

  Possible values:

  true::
    Supervisor-level software interrupts are implemented. `mie.SSIE` is writable.

  false::
    Supervisor-level software interrupts are not implemented.
    `mie.SSIE` and `mideleg[1]` are read-only zero.
long_name: TODO
schema:
  type: boolean
definedBy:
  extension:
    allOf:
      - name: Zicsr
      - name: S
