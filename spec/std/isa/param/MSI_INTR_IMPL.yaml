# Copyright (c) David_Harris@hmc.edu 13 July 2026
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../schemas/param_schema.json

$schema: param_schema.json#
kind: parameter
name: MSI_INTR_IMPL
description: |
  Whether or not machine-level software interrupts are implemented.

  Possible values:

  true::
    Machine-level software interrupts are implemented.
    `mie.MSIE` and `mideleg.MSI` are writable.

  false::
    Machine-level software interrupts are not implemented.
    `mie.MSIE` and `mideleg.MSI` are read-only zero.
long_name: Machine-level software interrupts are implemented
schema:
  type: boolean
definedBy:
  extension:
    allOf:
      - name: Zicsr
      - name: Sm
