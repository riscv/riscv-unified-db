# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/inst_schema.json

$schema: "inst_schema.json#"
kind: instruction
name: vmflt.vv
long_name: No synopsis available
description: |
  No description available.
definedBy:
  extension:
    anyOf:
      - name: Zvl32b
      - name: Zvfbfa
assembly: vd, vs2, vs1, vm
encoding:
  match: 011011-----------001-----1010111
  variables:
    - name: vm
      location: 25-25
    - name: vs2
      location: 24-20
    - name: vs1
      location: 19-15
    - name: vd
      location: 11-7
access:
  s: always
  u: always
  vs: always
  vu: always
pseudoinstructions:
  - when: "true"
    to: vmfgt.vv vd, vs1, vs2, vm
data_independent_timing: false
operation(): |
  check_f_ok($encoding);
  VectorState state = vector_state();
  U32 elems_per_reg = VLEN >> state.log2_sew;
  for (U32 i = CSR[vstart].VALUE; i < CSR[vl].VALUE; i++) {
    if (vm == 1'b1 || V[0][i] == 1'b1) {
      U32 reg_off = i / elems_per_reg;
      U32 start_bit = (i % elems_per_reg) * state.sew;
      U32 end_bit = start_bit + state.sew - 1;
      if (state.sew == 32) {
        Bits<32> a = V[vs2 + reg_off][end_bit:start_bit];
        Bits<32> b = V[vs1 + reg_off][end_bit:start_bit];
        Bits<1> sign_a = a[31:31];
        Bits<1> sign_b = b[31:31];
        if (is_sp_nan?(a) || is_sp_nan?(b)) {
          set_fp_flag(FpFlag::NV);
          V[vd][i] = 1'b0;
        } else {
          V[vd][i] = (((sign_a != sign_b) && (sign_a == 1'b1) && ((a[30:0] | b[30:0]) != 0)) || ((sign_a == sign_b) && (a != b) && ((sign_a ^ ((a < b) ? 1'b1 : 1'b0)) != 0))) ? 1'b1 : 1'b0;
        }
      } else {
        Bits<64> a = V[vs2 + reg_off][end_bit:start_bit];
        Bits<64> b = V[vs1 + reg_off][end_bit:start_bit];
        Bits<1> sign_a = a[63:63];
        Bits<1> sign_b = b[63:63];
        if (is_dp_nan?(a) || is_dp_nan?(b)) {
          set_fp_flag(FpFlag::NV);
          V[vd][i] = 1'b0;
        } else {
          V[vd][i] = (((sign_a != sign_b) && (sign_a == 1'b1) && ((a[62:0] | b[62:0]) != 0)) || ((sign_a == sign_b) && (a != b) && ((sign_a ^ ((a < b) ? 1'b1 : 1'b0)) != 0))) ? 1'b1 : 1'b0;
        }
      }
    }
  }
  mark_f_state_dirty();
  CSR[vstart].VALUE = 0;
