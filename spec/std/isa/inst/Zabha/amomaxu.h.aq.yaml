# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.

# WARNING: This file is auto-generated from spec/std/isa/inst/Zaamo/amomaxu.SIZE.AQRL.layout

# Copyright (c) Abhijit Das(Sukuna0007Abhi)
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/inst_schema.json

$schema: "inst_schema.json#"
kind: instruction
name: amomaxu.h.aq
long_name: Atomic MAX unsigned halfword (acquire)
description: |
  Atomically with acquire ordering:

    * Load the halfword at address _xs1_
    * Write the sign-extended value into _xd_
    * Unsigned compare the least-significant halfword of register _xs2_ to the loaded value, and select the maximum value
    * Write the maximum to the address in _xs1_
definedBy:
  extension:
    name: Zabha
assembly: xd, xs2, (xs1)
encoding:
  match: 1110010----------001-----0101111
  variables:
    - name: xs2
      location: 24-20
    - name: xs1
      location: 19-15
    - name: xd
      location: 11-7
access:
  s: always
  u: always
  vs: always
  vu: always
operation(): |
  if (implemented?(ExtensionName::A) && MISA_CSR_IMPLEMENTED && CSR[misa].A == 1'b0) {
    reserved_instruction($encoding);
  }

  memory_model_acquire();

  XReg virtual_address = X[xs1];
  X[xd] = amo(16, virtual_address, X[xs2][15:0], AmoOperation::Maxu, 1'b1, 1'b0, $encoding);
