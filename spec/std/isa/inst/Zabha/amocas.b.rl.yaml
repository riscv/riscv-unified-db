# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.

# WARNING: This file is auto-generated from spec/std/isa/inst/Zacas/amocas.SIZE.AQRL.layout

# Copyright (c) Abhijit Das(Sukuna0007Abhi)
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/inst_schema.json

$schema: "inst_schema.json#"
kind: instruction
name: amocas.b.rl
long_name: Atomic compare-and-swap byte (release)
description: |
  Atomically with release ordering:

    * Load the byte at address _xs1_
    * Write the sign-extended value into _xd_
    * Compare the loaded value with the least-significant byte of register _xs2_
    * If equal, write the least-significant byte of register _xs2+1_ to the address in _xs1_
definedBy:
  extension:
    name: Zabha
assembly: xd, xs2, (xs1)
encoding:
  match: 0010101----------000-----0101111
  variables:
    - name: xs2
      location: 24-20
    - name: xs1
      location: 19-15
    - name: xd
      location: 11-7
access:
  s: always
  u: always
  vs: always
  vu: always
operation(): |
  if (!implemented?(ExtensionName::Zabha)) {
    raise (ExceptionCode::IllegalInstruction, mode(), $encoding);
  }

  XReg virtual_address = X[xs1];
  # TODO
  # X[xd] = amocas8(virtual_address, X[xs2][7:0], X[xd][7:0], 1'b0, 1'b1, $encoding);

  memory_model_release();
