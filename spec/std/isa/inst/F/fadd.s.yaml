# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/inst_schema.json

$schema: "inst_schema.json#"
kind: instruction
name: fadd.s
long_name: Floating-Point Add Single-Precision
description: |
  The `fadd.s` instruction performs single-precision floating-point addition of `fs1` and `fs2`
  and writes the final result to `fd`.
definedBy:
  extension:
    name: F
assembly: fd, fs1, fs2, rm
encoding:
  match: 0000000------------------1010011
  variables:
    - name: fs2
      location: 24-20
    - name: fs1
      location: 19-15
    - name: fd
      location: 11-7
    - name: rm
      location: 14-12
      encode(operands): |
        if (operands[rm] == "rne") {
          return 3'b000;
        } else if (operands[rm] == "rtz") {
          return 3'b001;
        } else if (operands[rm] == "rdn") {
          return 3'b010;
        } else if (operands[rm] == "rup") {
          return 3'b011;
        } else if (operands[rm] == "rmm") {
          return 3'b100;
        } else if (operands[rm] == "dyn") {
          return 3'b111;
        } # else reserved
operands:
  - $inherits: operand/fd.yaml#/data
  - $inherits: operand/fs.yaml#/data
    name: fs1
  - $inherits: operand/fs.yaml#/data
    name: fs2
  - $inherits: operand/rm.yaml#/data
access:
  s: always
  u: always
  vs: always
  vu: always
data_independent_timing: true
operation(): |
  check_f_ok($encoding);
  RoundingMode mode = rm_to_mode(rm, $encoding);
  Bits<32> a = F[fs1];
  Bits<32> b = F[fs2];

  if (implemented?(ExtensionName::D)) {
    # check for incorrectly NaN-boxed inputs
    if (F[fs1][63:32] != 32'hffffffff) {
      a = SP_CANONICAL_NAN;
    }
    if (F[fs2][63:32] != 32'hffffffff) {
      b = SP_CANONICAL_NAN;
    }
  }

  F[fd] = f32_add(a, b, mode);

  mark_f_state_dirty();
