# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.

# WARNING: This file is auto-generated from spec/std/isa/inst/Zaamo/amoswap.SIZE.AQRL.layout

# Copyright (c) Abhijit Das(Sukuna0007Abhi)
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/inst_schema.json

$schema: "inst_schema.json#"
kind: instruction
name: amoswap.d.aqrl
long_name: Atomic SWAP doubleword (acquire-release)
description: |
  Atomically with acquire and release ordering:

    * Load the doubleword at address _xs1_
    * Write the loaded value into _xd_
    * Store the value of register _xs2_ to the address in _xs1_
definedBy:
  allOf:
    - extension:
        name: Zaamo
    - xlen: 64
assembly: xd, xs2, (xs1)
encoding:
  match: 0000111----------011-----0101111
  variables:
    - name: xs2
      location: 24-20
    - name: xs1
      location: 19-15
    - name: xd
      location: 11-7
access:
  s: always
  u: always
  vs: always
  vu: always
operation(): |
  if (implemented?(ExtensionName::A) && MISA_CSR_IMPLEMENTED && CSR[misa].A == 1'b0) {
    reserved_instruction($encoding);
  }

  memory_model_acquire();

  XReg virtual_address = X[xs1];
  X[xd] = amo(64, virtual_address, X[xs2], AmoOperation::Swap, 1'b1, 1'b1, $encoding);

  memory_model_release();
