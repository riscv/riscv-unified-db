---
phase: 03-human-reviewed-data-preregistration
plan: 02
type: implementation
wave: 2
depends_on:
  - 03-01
autonomous: false
requirements:
  - TS-08
  - TS-09
  - H2-01
files_modified:
  - experiments/specchoice-v1.3.2/src/specchoice_data/splits.py
  - experiments/specchoice-v1.3.2/src/specchoice_data/cli.py
  - experiments/specchoice-v1.3.2/tests/test_data_splits.py
  - experiments/specchoice-v1.3.2/data/preregistration/family-registry-v1.json
  - experiments/specchoice-v1.3.2/data/preregistration/split-manifest-v1.json
  - experiments/specchoice-v1.3.2/reports/h2/family-split-review-v1/
  - experiments/specchoice-v1.3.2/receipts/family-split-review-readiness-v1.json
  - experiments/specchoice-v1.3.2/reviews/family-split-review-decision-v1.json
must_haves:
  truths:
    - "D-05: The closed family registry freezes human-approved definition, inclusion criteria, and exclusion criteria before any assignment can be approved."
    - "D-06: Every eligible item has exactly one unambiguous registered primary_family; ambiguity is disputed and secondary_tags never affect isolation."
    - "D-07: After prototype approval, held-out split membership is derived only from example and primary-family disjointness; family overlap is auxiliary and cannot be manually promoted to strict core."
    - "D-08: Every assignment, split row, and later judgment binds the registry version and content hash; any registry or primary-assignment change invalidates the full dependent chain."
    - "D-03: Approved count-eligible prototype pairs have globally unique example IDs and exact source-span identities, with swapped directions and lightly varied reuse rejected from counts."
    - "TS-08: Prototype/held-out example overlap, prototype/strict primary-family overlap, unregistered families, version mismatch, and held-out passage use in demonstrations each fail closed with stable diagnostics."
    - "Empty prototype or strict sets are valid audited inputs that produce insufficient eligibility later; null assignments are invalid; all membership lists are sorted by stable ID."
  artifacts:
    - path: experiments/specchoice-v1.3.2/data/preregistration/family-registry-v1.json
      provides: "Closed definition-first family authority and item primary assignments."
    - path: experiments/specchoice-v1.3.2/data/preregistration/split-manifest-v1.json
      provides: "Deterministic prototype, strict-core, and auxiliary membership with invariant results."
    - path: experiments/specchoice-v1.3.2/reviews/family-split-review-decision-v1.json
      provides: "Human family-definition, assignment, and membership decisions bound to exact registry/split bytes."
  key_links:
    - from: experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json
      to: experiments/specchoice-v1.3.2/data/preregistration/family-registry-v1.json
      via: "only admitted inventory items and approved pair state are assignable"
    - from: experiments/specchoice-v1.3.2/data/preregistration/family-registry-v1.json
      to: experiments/specchoice-v1.3.2/data/preregistration/split-manifest-v1.json
      via: "exact registry version/hash and deterministic split derivation"
    - from: experiments/specchoice-v1.3.2/data/preregistration/split-manifest-v1.json
      to: experiments/specchoice-v1.3.2/reviews/family-split-review-decision-v1.json
      via: "complete review packet and readiness bindings"
  prohibitions:
    - statement: "Do not redefine or expand a family after assignments or split results are visible within the same registry version."
      status: unverified
      verification: flagged
    - statement: "Do not use secondary tags, expected usefulness, or later results to promote an auxiliary case into strict core."
      status: unverified
      verification: flagged
    - statement: "Do not collapse strict and auxiliary membership or counts into one evaluation population."
      status: unverified
      verification: flagged
---

<objective>
Freeze a human-approved definition-first family registry and derive a leakage-safe strict/auxiliary split that no reviewer or machine can tune for expected usefulness.

Purpose: As a reviewer, I want every family definition and primary assignment visible before split membership is derived, so that strict-core isolation is semantic, reproducible, and resistant to post-hoc movement.

Output: Family/split validator, approved family registry, deterministic split manifest, complete family/split review packet/readiness, and human decision.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-human-reviewed-data-preregistration/03-CONTEXT.md
@.planning/phases/03-human-reviewed-data-preregistration/03-RESEARCH.md
@.planning/phases/03-human-reviewed-data-preregistration/03-01-PLAN.md
@experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/candidate-inventory.json
@experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json
@experiments/specchoice-v1.3.2/src/specchoice_data/schema.py
@experiments/specchoice-v1.3.2/src/specchoice_data/admission.py
@experiments/specchoice-v1.3.2/src/specchoice_data/review.py
</context>

<artifacts_this_phase_produces>
- Module `specchoice_data.splits`, exception `SplitValidationError`, and dataclass `SplitAudit`.
- Functions `validate_family_registry_v1`, `derive_split_manifest_v1`, `validate_split_manifest_v1`, `audit_prototype_reuse_v1`, `audit_held_out_demonstration_leakage_v1`, and `invalidate_registry_dependents_v1`.
- CLI commands `build-family-split-review-v1` and `validate-family-split-review-decision-v1`.
- Registry, split manifest, review packet JSON/Markdown, readiness, decision, and `tests.test_data_splits`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement closed family registry validation and global prototype reuse audit</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_data/splits.py; experiments/specchoice-v1.3.2/src/specchoice_data/cli.py; experiments/specchoice-v1.3.2/tests/test_data_splits.py</files>
  <read_first>
    - .planning/phases/03-human-reviewed-data-preregistration/03-CONTEXT.md
    - experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json
    - experiments/specchoice-v1.3.2/src/specchoice_data/schema.py
    - experiments/specchoice-v1.3.2/src/specchoice_data/admission.py
    - experiments/specchoice-v1.3.2/src/specchoice_data/cli.py
    - experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
  </read_first>
  <action>
1. RED: add tests `test_registry_requires_closed_definitions_before_assignments`, `test_primary_family_is_exactly_one_registered_id`, `test_registry_change_invalidates_all_dependents`, and `test_count_eligible_pairs_cannot_reuse_example_or_span_identity`; run them and require missing-symbol failures.
2. Implement a registry schema with exact `schema_version=family-registry-v1`, `registry_version`, sorted families, and for each family a stable `family_id`, non-empty definition, non-empty inclusion criteria, non-empty exclusion criteria, and human review state. Put primary assignments in an exact sorted list of `item_id`, `item_version`, `primary_family`, and `secondary_tags`; secondary tags are descriptive strings only. Reject zero/multiple/unknown/ambiguous primary families for eligible items.
3. Compute `registry_sha256` from canonical registry content excluding no self-hash field. Require the exact candidate inventory and pair-decision hashes. `invalidate_registry_dependents_v1` compares both version and hash and emits `FAMILY_REGISTRY_DEPENDENT_STALE` for every packet/readiness/decision/split/relevance/metamorphic/H2 artifact that binds a different value; it never repairs or rewrites dependents.
4. Audit approved count-eligible pairs across both directed sides. Define source-span identity as `(source_sha256,start_byte,end_byte)` and example identity as exact `example_id`. Identical identities collide; merely touching half-open byte ranges do not. Reject pair reuse, duplicate direction swaps, or same semantic version under a new pair ID from count eligibility while retaining deterministic diagnostics.
  </action>
  <acceptance_criteria>
    - Registry definitions, inclusion/exclusion criteria, and review state are required before assignments validate.
    - Every eligible item has exactly one registered primary family; `ambiguous`, empty, null, multiple, or unknown values fail.
    - Any registry content or version change marks all dependent Phase 3 artifacts stale rather than partially reusing them.
    - Example/span reuse and swapped-pair inflation are reported globally and excluded from qualifying counts without deleting audit records.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -m unittest tests.test_data_splits.DataSplitTests.test_registry_requires_closed_definitions_before_assignments tests.test_data_splits.DataSplitTests.test_primary_family_is_exactly_one_registered_id tests.test_data_splits.DataSplitTests.test_registry_change_invalidates_all_dependents tests.test_data_splits.DataSplitTests.test_count_eligible_pairs_cannot_reuse_example_or_span_identity</automated>
  </verify>
  <done>The registry and reuse validator fail closed on every definition, assignment, version, and count-inflation defect while preserving all records for review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive strict-core and auxiliary membership and prove every leakage invariant</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_data/splits.py; experiments/specchoice-v1.3.2/tests/test_data_splits.py; experiments/specchoice-v1.3.2/data/preregistration/split-manifest-v1.json</files>
  <read_first>
    - experiments/specchoice-v1.3.2/src/specchoice_data/splits.py
    - experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/candidate-inventory.json
    - experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json
    - experiments/specchoice-v1.3.2/config/data/phase3-data-schema-v1.json
    - experiments/specchoice-v1.3.2/tests/test_data_splits.py
  </read_first>
  <action>
1. RED: add tests `test_split_is_pure_function_of_approved_examples_and_primary_families`, `test_example_overlap_and_strict_family_overlap_fail_closed`, `test_family_overlap_routes_only_to_auxiliary`, `test_no_held_out_passage_identity_in_demonstrations`, and `test_empty_strict_core_is_valid_red_evidence`.
2. Implement `derive_split_manifest_v1` as a pure deterministic projection. Prototype membership contains only structurally valid, human-approved, globally unique directed pairs. A human-approved held-out case becomes strict only when its example ID is absent from all prototype sides and its primary family is absent from all prototype primary families. Example-disjoint held-out cases with family overlap become auxiliary. Every other state remains quarantined with exact reason; no caller-provided split override exists.
3. Store sorted `prototype_pair_ids`, `prototype_example_ids`, `prototype_primary_family_ids`, `strict_case_ids`, `strict_primary_family_ids`, `auxiliary_case_ids`, and quarantined item IDs plus exact registry/inventory/pair-decision bindings. Record audit booleans and diagnostic lists for example overlap, strict family overlap, unregistered family, incompatible version, reused prototype identities, and demonstration leakage.
4. Represent demonstrations only as declared source-span identities and complete pair IDs. Compare every held-out authoritative passage identity against all fixed/retrieval demonstration identities; any overlap emits `HELD_OUT_PASSAGE_IN_DEMONSTRATION` and blocks readiness. Empty prototype/strict sets serialize canonically and remain valid inputs for later `red_required`; null or omitted sets are invalid.
  </action>
  <acceptance_criteria>
    - Reordering input files produces byte-identical split manifest output.
    - No API accepts manual strict/aux membership overrides; family overlap deterministically routes to auxiliary.
    - Example overlap, strict-family overlap, demonstration leakage, unknown family, and registry mismatch each block split readiness with stable diagnostics.
    - Strict and auxiliary IDs/counts remain separate in every projection; empty sets are preserved as Red evidence.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -m unittest tests.test_data_splits</automated>
  </verify>
  <done>The split manifest is a canonical pure function of approved item identities and the approved registry, with all TS-08 leakage invariants proven.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Approve family definitions and assignments, then review the deterministic split</name>
  <files>experiments/specchoice-v1.3.2/data/preregistration/family-registry-v1.json; experiments/specchoice-v1.3.2/reports/h2/family-split-review-v1/review-packet.json; experiments/specchoice-v1.3.2/reports/h2/family-split-review-v1/review-packet.md; experiments/specchoice-v1.3.2/receipts/family-split-review-readiness-v1.json; experiments/specchoice-v1.3.2/reviews/family-split-review-decision-v1.json</files>
  <read_first>
    - experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json
    - experiments/specchoice-v1.3.2/src/specchoice_data/splits.py
    - experiments/specchoice-v1.3.2/data/preregistration/family-registry-v1.json
    - experiments/specchoice-v1.3.2/data/preregistration/split-manifest-v1.json
    - experiments/specchoice-v1.3.2/config/data/phase3-data-schema-v1.json
  </read_first>
  <action>First render the proposed closed registry definitions with inclusion/exclusion criteria and collect explicit human approved/disputed/excluded decisions for every family definition. Only after all used definitions are approved, present every item assignment and collect an explicit decision; disputed/ambiguous items are quarantined and never receive an `ambiguous` family value. Freeze the resulting canonical registry once. Recompute the split manifest from approved assignments, then render a packet showing why each held-out case is strict, auxiliary, invalid, disputed, or excluded and all reuse/leakage diagnostics. Collect a final human acknowledgment of the deterministic memberships without offering a move/promote control. Publish decision-free readiness and the complete decision with no-replace exact-resume semantics.</action>
  <instructions>Approve family semantics before assignments. Use secondary tags only for description. If one unambiguous primary family cannot be selected, mark the item disputed. Review the derived split for correctness of inputs and diagnostics; do not move a case based on usefulness, desired count, or expected result.</instructions>
  <resume-signal>Return complete family-definition and item-assignment dispositions plus acknowledgment of every derived split/quarantine row, reviewer identity, rationale, attestation, signature, and UTC timestamp.</resume-signal>
  <verification>The renderer presents definitions before assignments, recomputes the split without an override control, and the final decision validates against exact registry/split/packet/readiness hashes.</verification>
  <acceptance_criteria>
    - Every used family definition and eligible item assignment has one explicit human decision in dependency order.
    - Disputed definitions/assignments quarantine all affected records and cannot count or silently fall back.
    - The decision binds exact registry, split, inventory, pair-decision, authority, and H1 hashes.
    - The packet exposes but does not permit manual override of strict/auxiliary derivation.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -m unittest tests.test_data_splits</automated>
  </verify>
  <done>A complete human family/split decision exists, and the immutable split manifest proves strict isolation while preserving auxiliary and quarantined populations separately.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|---|---|---|---|
| T-03-02-01 | Tampering | high | Registry version and content hash bind every dependent artifact. |
| T-03-02-02 | Elevation of Privilege | high | Split is a pure function; no human or CLI override can promote auxiliary to strict. |
| T-03-02-03 | Repudiation | high | Definition and assignment decisions are separately explicit and immutable. |
| T-03-02-04 | Tampering | high | Global identity audit blocks reused examples/spans and swapped-pair inflation. |
| T-03-02-05 | Information Disclosure | medium | Reports use stable IDs and repository-relative paths only. |
| T-03-02-06 | Elevation of Privilege | high | Secondary tags and auxiliary data never enter strict isolation or headline counts. |
</threat_model>

<verification>
Run `tests.test_data_admission` and `tests.test_data_splits`; validate registry, manifest, packet, readiness, and decision through CLI; mutate one registry definition, primary assignment, example ID, span identity, and demonstration identity in temporary copies and prove each fails without rewriting authority.
</verification>

<success_criteria>
- Family semantics are approved before assignment, and every eligible item has one unambiguous registered primary family.
- Strict/auxiliary membership is deterministic, immutable, and fully leakage-validated.
- Registry changes invalidate the whole dependent chain; no partial approval remains authoritative.
- Strict, auxiliary, invalid, disputed, and excluded records and counts remain distinguishable.
</success_criteria>

<output>
Create `03-02-SUMMARY.md` with exact registry/split/packet/readiness/decision hashes, split counts, invariant results, tests, and any quarantined family or assignment disputes.
</output>
