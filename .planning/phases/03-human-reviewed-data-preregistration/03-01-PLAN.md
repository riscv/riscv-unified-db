---
phase: 03-human-reviewed-data-preregistration
plan: 01
type: implementation
wave: 1
depends_on: []
autonomous: false
requirements:
  - TS-08
  - TS-09
  - H2-01
files_modified:
  - experiments/specchoice-v1.3.2/config/data/phase3-data-schema-v1.json
  - experiments/specchoice-v1.3.2/src/specchoice_data/__init__.py
  - experiments/specchoice-v1.3.2/src/specchoice_data/schema.py
  - experiments/specchoice-v1.3.2/src/specchoice_data/admission.py
  - experiments/specchoice-v1.3.2/src/specchoice_data/review.py
  - experiments/specchoice-v1.3.2/src/specchoice_data/cli.py
  - experiments/specchoice-v1.3.2/tests/test_data_admission.py
  - experiments/specchoice-v1.3.2/tests/fixtures/data_preregistration/tracer/
  - experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/
  - experiments/specchoice-v1.3.2/reports/h2/pair-review-v1/
  - experiments/specchoice-v1.3.2/receipts/pair-review-readiness-v1.json
  - experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json
must_haves:
  truths:
    - "D-01: Every structurally valid candidate has exactly one human approved, disputed, or excluded disposition; invalid remains machine-owned and only approved items can become count-eligible."
    - "D-02: Descriptor-bound structural admission completes before any semantic review packet is emitted."
    - "D-03: The validator records global example_id and exact source-span identities so later plans can prove that one approved count-eligible pair cannot reuse either identity."
    - "D-04: One complete candidate inventory is canonicalized and hashed before the first production review packet; no later candidate may replace an invalid, disputed, or excluded item in version v1."
    - "D-09: Pair relationship review permits one discriminating axis or an explicitly declared inseparable coupled set and disputes any independently explanatory factor."
    - "D-10: Every frame axis, final status, and discriminating-axis rationale binds at least one exact non-empty authoritative source span through an explicit claim_id mapping."
    - "D-11: Pair sides are reviewed independently before their relationship, and relationship rejection cannot rewrite an approved side."
    - "D-12: A pair is directed; positive/contrast order and expected delta are frozen, and a swapped version never increases qualifying count."
    - "Touching non-empty byte spans remain distinct; identical source path/hash/start/end identities collide for count-eligibility; all arrays are canonically sorted by stable ID."
    - "An empty or singleton frozen inventory remains auditable and deterministically leads toward red_required rather than being rejected or backfilled; null required values are invalid."
  artifacts:
    - path: experiments/specchoice-v1.3.2/src/specchoice_data/admission.py
      provides: "Phase 2 authority gate, candidate inventory freeze, descriptor-bound source-span admission, and deterministic diagnostics."
    - path: experiments/specchoice-v1.3.2/src/specchoice_data/review.py
      provides: "Pair packet, readiness, and exact human decision validation without semantic inference."
    - path: experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/candidate-inventory.json
      provides: "Complete pre-review candidate path/hash/length/kind authority."
    - path: experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json
      provides: "Human decisions for all admitted pair sides and relationships, with invalid candidates retained separately."
  key_links:
    - from: experiments/specchoice-v1.3.2/phase2/source-authority.json
      to: experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/candidate-inventory.json
      via: "exact active authority and approved H1 bindings checked before freeze"
    - from: experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/candidate-inventory.json
      to: experiments/specchoice-v1.3.2/reports/h2/pair-review-v1/review-packet.json
      via: "complete-batch admission and canonical inventory hash"
    - from: experiments/specchoice-v1.3.2/receipts/pair-review-readiness-v1.json
      to: experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json
      via: "human response binds exact packet/readiness identities"
  prohibitions:
    - statement: "Do not generate pair semantics, labels, axes, or human dispositions with a model or infer them from machine readiness."
      status: unverified
      verification: flagged
    - statement: "Do not add replacement candidates after inventory freeze to reach Green or Yellow counts."
      status: unverified
      verification: flagged
    - statement: "Do not modify core UDB schemas, accepted Phase 2 source bytes, source authority, or external/public state."
      status: unverified
      verification: flagged
---

<objective>
Deliver a production-quality tracer from exact accepted Phase 2 bytes through candidate admission and a human pair decision, then expand it to the complete frozen pair and held-out candidate inventory.

Purpose: As a human RISC-V reviewer, I want to inspect every candidate's exact provenance, claims, sides, and directed relationship before any item can count, so that weak or disputed semantics remain visible without contaminating the corpus.

Output: Closed Phase 3 schema, admission/review modules, complete frozen candidate inventory, pair review packet/readiness, and one complete human pair decision artifact.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-human-reviewed-data-preregistration/03-CONTEXT.md
@.planning/phases/03-human-reviewed-data-preregistration/03-RESEARCH.md
@.planning/phases/03-human-reviewed-data-preregistration/03-PATTERNS.md
@.planning/phases/03-human-reviewed-data-preregistration/03-VALIDATION.md
@experiments/specchoice-v1.3.2/phase2/source-authority.json
@experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v6.json
@experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
@experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py
</context>

<dependency_gate>
Before any production Phase 3 write, require Phase 2 lifecycle closure plus the exact approved H1 decision named in `03-CONTEXT.md`. If lifecycle closure is absent, stale, uncommitted, non-approved, or hash-inconsistent, stop with `PHASE2_AUTHORITY_NOT_CLOSED` and create no Phase 3 production artifact. Tests may use isolated temporary fixtures while this gate is closed.
</dependency_gate>

<specless_probe_resolution>
- TS-08 adjacency: byte ranges `[start,end)` may touch but never overlap for distinct claims when uniqueness is required; identical span identities collide.
- TS-08 empty/order: empty candidate sets are valid Red evidence; null is invalid; every set-like list is sorted by stable ID.
- TS-09 empty/encoding: a missing human response writes nothing; canonical derived text is UTF-8/LF/NFC while source spans use raw-byte offsets and raw hashes.
- H2-01 adjacency/empty/order: positive and contrast sides must be distinct non-empty identities; zero or one qualifying pair is auditable Red evidence; pair and claim ordering is stable by ID.
</specless_probe_resolution>

<source_coverage_audit>
| Source | Coverage |
|---|---|
| TS-08 | Authority, provenance, exact spans, inventory identity, and reusable leakage identities are implemented and tested. |
| TS-09 | Human-only semantic decisions, frozen-input versioning, no mutation, and no public/model action are explicit. |
| H2-01 | Complete directed pair fields, independent side review, relationship review, manual verification, and no quota filling are explicit. |
| CONTEXT D-01..D-04, D-09..D-12 | Cited in `must_haves`, tasks, and tests; remaining decisions are owned by later plans. |
| RESEARCH | Standard-library-only package and existing canonical/filesystem/H1 patterns are reused. |
</source_coverage_audit>

<artifacts_this_phase_produces>
- Package `specchoice_data` and modules `schema`, `admission`, `review`, and `cli`.
- Exceptions `DataSchemaError`, `DataAdmissionError`, and `DataReviewError`.
- Functions `validate_phase3_schema_v1`, `freeze_candidate_inventory_v1`, `validate_candidate_inventory_v1`, `admit_pair_candidate_v1`, `admit_held_out_candidate_v1`, `build_pair_review_packet_v1`, `write_pair_review_readiness_v1`, and `validate_pair_review_decision_v1`.
- CLI commands `freeze-candidate-inventory-v1`, `build-pair-review-v1`, `validate-pair-review-decision-v1`.
- Closed schema, candidate tree, packet JSON/Markdown, readiness receipt, decision JSON, and `tests.test_data_admission`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Implement one real authority-to-review tracer without production semantic inference</name>
  <files>experiments/specchoice-v1.3.2/config/data/phase3-data-schema-v1.json; experiments/specchoice-v1.3.2/src/specchoice_data/__init__.py; experiments/specchoice-v1.3.2/src/specchoice_data/schema.py; experiments/specchoice-v1.3.2/src/specchoice_data/admission.py; experiments/specchoice-v1.3.2/src/specchoice_data/review.py; experiments/specchoice-v1.3.2/src/specchoice_data/cli.py; experiments/specchoice-v1.3.2/tests/test_data_admission.py; experiments/specchoice-v1.3.2/tests/fixtures/data_preregistration/tracer/</files>
  <read_first>
    - .planning/phases/03-human-reviewed-data-preregistration/03-CONTEXT.md
    - .planning/phases/03-human-reviewed-data-preregistration/03-RESEARCH.md
    - experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
    - experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py
    - experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py
    - experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
    - experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py
    - experiments/specchoice-v1.3.2/tests/test_canonical.py
    - experiments/specchoice-v1.3.2/tests/test_filesystem_boundary.py
  </read_first>
  <action>
1. RED: create `tests.test_data_admission` with a temporary, explicitly test-only accepted source tree and one complete directed pair. Add tests named `test_tracer_freezes_admits_renders_and_validates_explicit_human_decision`, `test_tracer_rejects_unlisted_or_mutated_source_bytes`, and `test_tracer_never_infers_human_fields`; require failures from missing Phase 3 symbols.
2. Define `phase3-data-schema-v1` with exact keys and enums: candidate kinds `pair`, `held_out`, `metamorphic`; machine state `valid`/`invalid`; human dispositions `approved`/`disputed`/`excluded`; frame axes `authority`, `choice_object`, `choice_space_origin`; final statuses `accept`/`classify_out`; source kinds `authoritative`/`human_synthetic`; and eligibility statuses `green_eligible`/`yellow_eligible`/`red_required`. Reject unknown keys, duplicate JSON keys, booleans-as-integers, null required fields, unsupported versions, and noncanonical serialized authority files.
3. Implement the production tracer using imported canonical/filesystem/strict-JSON/Diagnostic primitives. `freeze_candidate_inventory_v1` records the complete sorted candidate relative paths, kinds, byte lengths, SHA-256 values, active Phase 2 authority SHA-256, approved H1 decision SHA-256, and schema SHA-256. `admit_pair_candidate_v1` verifies two independent sides, distinct example IDs, exact raw-byte source spans, explicit `claim_id -> span_id[]` mappings for each axis/status/rationale claim, non-empty shared structure/discriminating axes, expected deltas, and fixed presentation order. Return all stable diagnostics sorted; do not repair inputs.
4. Implement packet/readiness/decision functions. Packet JSON is canonical authority; Markdown is derived. Readiness contains no decision. Decision validation requires explicit reviewer ID, per-side dispositions, relationship disposition, aggregate disposition, rationale, attestation, signature, and canonical UTC timestamp, all bound to packet/readiness hashes. No production decision writer is invoked by the tracer test; the test validates an explicitly supplied fixture response only.
5. Add thin CLI routes. Every production route first calls the exact Phase 2 lifecycle/authority gate and performs no write when it fails. No route accesses the network, a model, GitHub, or core UDB paths.
  </action>
  <acceptance_criteria>
    - `phase3-data-schema-v1.json` contains all exact versions/enums and rejects unknown/duplicate/null/noncanonical input.
    - The tracer test exercises inventory freeze -> descriptor-bound pair admission -> packet -> readiness -> explicit decision validation with real byte spans.
    - Mutated, symlinked, unlisted, out-of-root, empty-span, unmapped-claim, same-example, missing-axis, and inferred-human-field fixtures fail with stable diagnostics and no count-bearing output.
    - Production writes are impossible while the Phase 2 lifecycle gate is closed; temporary tests remain runnable.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -m unittest tests.test_data_admission.DataAdmissionTests.test_tracer_freezes_admits_renders_and_validates_explicit_human_decision tests.test_data_admission.DataAdmissionTests.test_tracer_rejects_unlisted_or_mutated_source_bytes tests.test_data_admission.DataAdmissionTests.test_tracer_never_infers_human_fields</automated>
  </verify>
  <done>A reviewer-visible, production-code tracer proves the complete authority-to-human-decision path on isolated test data, while production writes remain gated on exact Phase 2 closure.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Author and freeze the complete v1 candidate inventory before review</name>
  <files>experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/</files>
  <read_first>
    - ../../Downloads/LFX_RISCV_SpecChoice_v1.3.2_frozen_execution_baseline.md
    - experiments/specchoice-v1.3.2/phase2/source-authority.json
    - experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v6.json
    - experiments/specchoice-v1.3.2/config/data/phase3-data-schema-v1.json
    - experiments/specchoice-v1.3.2/src/specchoice_data/admission.py
  </read_first>
  <action>After the dependency gate passes, render a non-writing authoring worksheet over every accepted Phase 2 source item. The human authors the complete intended v1 pair, held-out, and metamorphic candidate files using exact source spans and the Phase 3 schema. Do not invent or complete semantic fields on the human's behalf. Run full-batch structural admission and show every invalid candidate and diagnostic. Once the human confirms the inventory is complete, write `candidate-inventory.json` exactly once with sorted path/kind/hash/length entries and the exact authority/H1/schema bindings. After this write, reject any new, removed, renamed, reordered-by-content, or byte-modified candidate as `CANDIDATE_INVENTORY_CHANGED`; do not permit replacements for invalid or disputed candidates.</action>
  <instructions>The reviewer confirms that the candidate set is complete before any semantic disposition is collected. Candidate count may be zero or below thresholds. Every semantic field must be human-authored or directly copied from already approved H1 semantics with its origin declared; no model-generated pair, label, family, axis, rationale, or metamorphic text is allowed.</instructions>
  <resume-signal>Return `FREEZE CANDIDATE INVENTORY V1` plus the complete candidate-file list and confirmation that no additional candidate will be added to replace a later invalid, disputed, or excluded item.</resume-signal>
  <verification>The renderer shows the complete accepted-source inventory and all structural diagnostics; after the freeze confirmation, a fresh validation proves exact path/kind/hash/length equality and rejects an added candidate.</verification>
  <acceptance_criteria>
    - Every candidate file is listed exactly once with kind/path/hash/length and exact Phase 2 authority/H1/schema bindings.
    - Structural admission reports all invalid items in one deterministic sorted batch and leaves them present for audit.
    - A post-freeze add/remove/rename/byte edit fails validation and creates no review packet.
    - No eligibility or qualifying-pair count is published before semantic review.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -m unittest tests.test_data_admission.DataAdmissionTests.test_complete_inventory_is_frozen_before_review tests.test_data_admission.DataAdmissionTests.test_invalid_candidates_remain_auditable_and_cannot_be_backfilled</automated>
  </verify>
  <done>The exact complete v1 candidate inventory is immutable, content-bound, and ready for semantic review without any threshold-driven replacement path.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Review every admitted pair side and directed relationship, then publish the exact decision</name>
  <files>experiments/specchoice-v1.3.2/reports/h2/pair-review-v1/review-packet.json; experiments/specchoice-v1.3.2/reports/h2/pair-review-v1/review-packet.md; experiments/specchoice-v1.3.2/receipts/pair-review-readiness-v1.json; experiments/specchoice-v1.3.2/reviews/pair-review-decision-v1.json</files>
  <read_first>
    - experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/candidate-inventory.json
    - experiments/specchoice-v1.3.2/src/specchoice_data/review.py
    - experiments/specchoice-v1.3.2/config/data/phase3-data-schema-v1.json
    - experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v6.json
  </read_first>
  <action>Revalidate the complete frozen inventory and all source bytes, then write one canonical packet and derived Markdown containing every admitted pair side first and every directed relationship second. Show exact claim-to-span mappings, frame/status claims, shared structure, discriminating axes, expected deltas, source provenance, and invalid candidate diagnostics. Write decision-free readiness only after packet JSON/Markdown cross-validation. Collect one complete human response with an approved/disputed/excluded disposition and rationale for every side and relationship plus reviewer identity, attestation, signature, and UTC timestamp. Validate aggregate consistency and publish the canonical decision without replacement. A disputed relationship retains side decisions; an excluded/disputed side makes the relationship non-qualifying; invalid candidates cannot be human-approved. Byte changes require a new version and full rereview.</action>
  <instructions>Review each positive and contrast side independently before deciding the relationship. Approve a relationship only when all material structure is shared except one discriminating axis or explicitly inseparable coupled axes. Do not approve a swapped direction as a second qualifying pair and do not reinterpret machine invalidity as a semantic dispute.</instructions>
  <resume-signal>Return the complete pair review response covering every admitted side and relationship, with explicit dispositions/rationales plus reviewer identity, attestation, signature, and UTC timestamp.</resume-signal>
  <verification>The checkpoint recomputes inventory and source identities, displays sides before relationships, and the published decision validates against the current packet/readiness with no inferred field.</verification>
  <acceptance_criteria>
    - Packet/readiness bind the exact frozen inventory, schema, authority, H1 decision, and every admitted candidate byte.
    - Every admitted side and relationship has exactly one explicit human disposition; no field is defaulted or inferred.
    - Qualifying-pair projection includes only relationships whose two sides and relationship are approved and structurally valid.
    - Invalid, disputed, excluded, and approved-but-not-yet-split states remain separately auditable; no replacements are introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -m unittest tests.test_data_admission</automated>
  </verify>
  <done>The complete frozen inventory has a content-bound human pair decision, and only structurally valid, independently approved directed pairs are eligible for later split/count evaluation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Control |
|---|---|---|
| Phase 2 authority -> candidate spans | stale/path-substituted bytes | exact authority/H1 hashes plus descriptor-bound reads |
| Candidate authoring -> frozen inventory | post-review replacement or quota filling | complete pre-review no-replace inventory |
| Machine admission -> human review | invalid items presented as semantically approvable | invalid quarantine and packet exclusion |
| Human response -> decision | missing, inferred, or forged fields | complete exact schema, identity/signature/timestamp, no inference |

## STRIDE Register — ASVS L1

| Threat ID | Category | Severity | Mitigation |
|---|---|---|---|
| T-03-01-01 | Tampering | high | Bind every source/candidate/packet/readiness byte and reject exact-resume divergence. |
| T-03-01-02 | Spoofing | high | Require explicit human identity, attestation, signature, and UTC timestamp. |
| T-03-01-03 | Elevation of Privilege | high | Machine readiness and invalid-state output cannot create semantic approval. |
| T-03-01-04 | Repudiation | high | Preserve all invalid/disputed/excluded entries and exact decision rationales. |
| T-03-01-05 | Information Disclosure | medium | Store only repository-relative paths; never emit absolute local paths. |
| T-03-01-06 | Tampering | high | Freeze complete inventory before review and forbid threshold backfill. |
</threat_model>

<verification>
Run the full admission suite, inspect the production packet and decision with the local validator, and verify `git diff -- experiments/specchoice-v1.3.2/phase2 experiments/specchoice-v1.3.2/bundles/accepted spec backends` is empty. Confirm no network/model/publication command exists in the new CLI.
</verification>

<success_criteria>
- A complete candidate inventory is frozen before semantic review and remains immutable within v1.
- Every pair side and relationship is structurally validated, evidence-bound, independently human-reviewed, and explicitly disposed.
- No invalid, disputed, excluded, swapped, reused, or post-freeze replacement item can increase a qualifying count.
- Phase 2 authority, accepted bytes, core UDB, remotes, and public state remain unchanged.
</success_criteria>

<output>
Create `03-01-SUMMARY.md` recording exact schema/inventory/packet/readiness/decision hashes, counts by invalid/approved/disputed/excluded, dependency-gate evidence, tests, and unchanged upstream boundaries.
</output>
