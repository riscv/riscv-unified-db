---
phase: 02-deterministic-measurement-spine
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - experiments/specchoice-v1.3.2/config/measurement/h1-review-schema-v1.json
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_h1.py
  - experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.json
  - experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.md
  - experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v1.json
autonomous: false
requirements: [TS-03, TS-04, TS-05]
must_haves:
  truths:
    - "Per D-13, H1 recognizes exactly approved, disputed, and incomplete; only an explicit independently human-authored approved decision permits local Phase 3 progression, while disputed/incomplete remains blocking and machines cannot override either state."
    - "Per D-14, the decision contains one signed semantic review for each of the exact 11 fixtures covering category, expect_extract, normalized count/names, candidate surfaced-then-classify_out semantics, and adapter lineage; any disputed item forces aggregate disputed."
    - "Per D-15, the H1 packet binds Plan 04's warning-free formal golden attempt and separately validated adversarial oracle; expected adversarial diagnostics never become formal golden warnings or score authority."
    - "Per D-16, H1 JSON and the human decision bind the active accepted verifier-rooted v2 generation/root/snapshot manifest/registry, adapter version/rule/batch, current schema, golden set, formal attempt, golden diagnostics, adversarial report, and packet; any changed binding invalidates approval and requires a new version/review."
    - "Canonical H1 JSON is authoritative and Markdown is a pure deterministic projection that calculates no facts, stores no independent decision, and validates byte-for-byte against the JSON."
    - "No Phase 2 command creates, guesses, defaults, serializes, edits, normalizes, infers, or upgrades an approved disposition; automation only validates an already existing human-authored decision."
    - "Every H1 disposition preserves external_publication_authorized=false and local-only scope; approval cannot authorize model calls, network/API access, push, PR, upload, deployment, publication, or any remote action."
    - "The final five-module focused Phase 2 suite passes without modifying or weakening any Phase 1 custody module, accepted byte, source-authority record, or live-boundary assertion."
  artifacts:
    - path: "experiments/specchoice-v1.3.2/config/measurement/h1-review-schema-v1.json"
      provides: "Closed packet/decision contract, exact three-state disposition enum, 11-item signature requirements, binding set, and local-only authority"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py"
      provides: "Canonical H1 packet builder, pure Markdown renderer, and human-decision/binding validator without a decision writer"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_h1.py"
      provides: "Task-owned TDD packet, projection, disposition, per-fixture review, binding invalidation, no-machine-approval, and no-publication tests"
    - path: "experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.json"
      provides: "Hash-bound canonical H1 source/gold review packet over the exact Plan 04 machine evidence"
    - path: "experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.md"
      provides: "Deterministic human-readable projection derived only from validated canonical H1 JSON"
    - path: "experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v1.json"
      provides: "Independently human-authored three-state H1 disposition; never machine-created approval"
  key_links:
    - from: "experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/attempt.json"
      to: "experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.json"
      via: "recompute non-cyclic canonical hashes over source, adapter, schema, golden prediction, formal attempt, clean diagnostics, and separate adversarial identities"
      pattern: "attempt_sha256|diagnostics_sha256|adapter_batch_sha256|schema_sha256|adversarial_report_sha256"
    - from: "experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.json"
      to: "experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.md"
      via: "render_markdown consumes validated JSON only and round-trips byte-identically"
      pattern: "render_markdown|validate_h1_packet"
    - from: "experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v1.json"
      to: "experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.json"
      via: "validate-h1-decision recomputes every binding and all 11 item reviews before accepting the stated human disposition"
      pattern: "approved|disputed|incomplete|external_publication_authorized"
  prohibitions:
    - "flagged-unverified: A machine or Phase 2 must not create, infer, or upgrade H1 approved."
    - "flagged-unverified: H1 or any measurement completion status must not authorize external publication, push, model calls, APIs, deployment, or other remote action."
    - "flagged-unverified: A targeted, partial, warning-bearing golden, failed, or diagnostic-only attempt must not be presented as H1-ready formal measurement authority."
    - "flagged-unverified: Phase 1 custody and live-boundary checks must not be weakened, rebased, or suppressed to make Phase 2 pass."
---

<objective>
**As a** human RISC-V reviewer, **I want to** inspect a hash-bound H1 packet and author its disposition myself, **so that** local Phase 3 progression depends on explicit reviewed semantics rather than machine-created approval.

Build and validate the canonical H1 JSON/Markdown packet from Plan 04 evidence, enforce D-13 through D-16 decision semantics and binding invalidation, and stop at the blocking human review boundary.

Purpose: Complete TS-03/04/05 source/gold review authority while preserving no-machine-approval and local-only/non-publication scope.
Output: H1 schema/module/tests, canonical JSON and derived Markdown packet, a decision validator, and an independently authored decision when the human checkpoint completes.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-VERIFICATION.md
@.planning/phases/02-deterministic-measurement-spine/02-CONTEXT.md
@.planning/phases/02-deterministic-measurement-spine/02-RESEARCH.md
@.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md
@.planning/phases/02-deterministic-measurement-spine/02-VALIDATION.md
@.planning/phases/02-deterministic-measurement-spine/02-04-SUMMARY.md
@experiments/specchoice-v1.3.2/src/specchoice_evidence/receipt.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/attempts.py
@experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/attempt.json
@experiments/specchoice-v1.3.2/reports/h1/adversarial-oracle-results-v1.json

<interfaces>
Plan 04 contracts consumed here:

```python
def validate_measurement_attempt(*, attempt_root: Path) -> object: ...

@dataclass(frozen=True)
class AttemptView:
    role: str
    status: str
    attempt_sha256: str
    bindings: object
    diagnostics: tuple[Diagnostic, ...]
```

Contracts created here:

```python
def build_h1_packet(*, formal_attempt: Path, adversarial_report: Path, output_json: Path, output_markdown: Path) -> object: ...
def render_h1_markdown(packet: object) -> str: ...
def validate_h1_packet(*, packet: Path, markdown: Path) -> object: ...
def validate_h1_decision(*, packet: Path, decision: Path) -> object: ...
```
</interfaces>
</context>

## Probe Disposition

- TS-03 adjacency/empty/ordering remains unresolved by the spec-less probe; Plans 01/03 convert it only into explicit all-11 uniqueness/completeness/stable-order tests, without claiming a classified probe result.
- TS-04 remains flagged-unclassified; Plan 02's D-05 through D-08 JSON-only/legacy-ingress/closed-schema matrix is the defensive implementation, not a claim that the probe classified it.
- TS-05 adjacency/empty/ordering remains unresolved by the spec-less probe; Plans 02/03 require distinct spans/diagnostics, explicit empty collections, and declared stable tie-breaks, with no silent drop or implicit merge.
- The four project prohibitions remain descriptor-less `flagged-unverified` entries across the plan set. Traversal, injection, special files, and other canonical security concerns appear only in threat models.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Trust adjudication semantics and diagnostics before frame, retrieval, or model results | 02-01 through 02-05 | COVERED |
| REQ | TS-03 | Versioned PR #2164 adapter and deterministic golden all-11 scoring including candidate classify-out | 02-01, 02-03, 02-04, 02-05 | COVERED |
| REQ | TS-04 | Strict canonical adjudication, unique no-finding representation, unknown/enum rejection, no repair | 02-02, 02-04, 02-05 | COVERED |
| REQ | TS-05 | Stable structured diagnostics and identity-warning separation | 02-02 through 02-05 | COVERED |
| RESEARCH | — | Stdlib-only sibling package reusing Phase 1 canonical/filesystem/verification primitives | 02-01 through 02-05 | COVERED |
| RESEARCH | — | Accepted verifier-rooted v2 authority only; no historical/candidate/live/network fallback | 02-01 and revalidated in 02-02 through 02-05 | COVERED |
| RESEARCH | — | Versioned adapter batch and bounded PR #2164 field reader | 02-01 | COVERED |
| RESEARCH | — | JSON-only formal input, separately declared legacy ingress, duplicate-safe closed validation, collect-then-decide preflight | 02-02 | COVERED |
| RESEARCH | — | Exact `{source_sha256,start_byte,end_byte,text}` spans with non-empty half-open ranges, per-span validation, no merge; independent scoring and warnings | 02-02, 02-03 | COVERED |
| RESEARCH | — | Immutable formal/diagnostic attempts with exclusive-create/fsync/no-replace publication | 02-04 | COVERED |
| RESEARCH | — | Canonical H1 JSON/derived Markdown, complete binding invalidation, and human-only decision gate | 02-05 | COVERED |
| RESEARCH | — | Focused Phase 2 suite without weakening Phase 1 custody/live-boundary checks | 02-01 through 02-05 | COVERED |
| CONTEXT | D-01–D-04 | Adapter lineage, conflict blocking, score allowlist, immutable versions | 02-01 | COVERED |
| CONTEXT | D-05–D-08 | Legacy alias boundary, closed schema, unique no-finding, exact raw semantics/evidence | 02-02; exact evidence scoring in 02-03 | COVERED |
| CONTEXT | D-09–D-12 | Complete preflight, warning separation, immutable attempts, formal/diagnostic-only boundary | 02-02 through 02-04 | COVERED |
| CONTEXT | D-13–D-16 | Three-state human H1 review, per-fixture signatures, clean golden/adversarial separation, immutable binding/local-only authority | 02-05 | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-05-01: Prove H1 packet and human-decision validation end to end</name>
  <reversibility rating="costly">The versioned H1 packet/decision contract is consumed by local Phase 3 progression and later evidence; changing it requires a new packet and human review while historical decisions remain immutable.</reversibility>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_h1.py, experiments/specchoice-v1.3.2/config/measurement/h1-review-schema-v1.json, experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py</files>
  <behavior>
    - "RED first: a validated warning-free all-11 formal attempt plus the separate exact adversarial result produces one canonical packet with 11 fixture review items and every D-16 binding."
    - "Canonical JSON is authoritative; rendering validated JSON twice yields byte-identical Markdown, and Markdown cannot introduce or calculate facts absent from JSON."
    - "Decision validation accepts exactly human-authored approved/disputed/incomplete with exactly 11 signed item reviews; one disputed item forces aggregate disputed, and missing material/signature yields incomplete or rejection."
    - "Changing any accepted source, registry, adapter, rule, batch, schema, golden set, formal attempt, golden diagnostic, adversarial result, packet, Markdown, item signature, or aggregate binding invalidates validation."
    - "No API or CLI path writes a decision, supplies approved, upgrades disputed/incomplete, or changes external_publication_authorized=false."
  </behavior>
  <action>Create `tests/test_measurement_h1.py` first and run the final five-module suite red against the missing H1 implementation. Define the closed H1 review schema with the exact D-13 dispositions, D-14 per-fixture semantic/signature fields, D-15 formal/adversarial separation, and D-16 complete immutable binding set. Implement a packet builder that accepts only the validated Plan 04 formal attempt and separate adversarial report; hash a non-cyclic projection and render Markdown only from validated JSON. Implement packet and decision validators that recompute every binding, require exactly the finite 11-fixture review set, force disputed aggregation, treat missing material/signature as blocking, and preserve local-only/non-publication semantics. Expose only build/validate packet and validate-existing-decision commands; do not implement a decision writer, approval default, content sniffing, or normalization of the human disposition.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter tests.test_measurement_parsing tests.test_measurement_scoring tests.test_measurement_attempts tests.test_measurement_h1 -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>H1 packet, Markdown, and human-decision validation are closed, deterministic, fully bound, three-state, and incapable of creating approval or external authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-05-02: Generate and validate the exact H1 JSON/Markdown review packet</name>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_h1.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py, experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.json, experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v1.md</files>
  <behavior>
    - "The generated packet binds the active accepted-v2 source identities, adapter/rule/batch, canonical schema, golden set, warning-free formal attempt, diagnostics=[], separate adversarial result, all 11 semantic review items, and external_publication_authorized=false."
    - "The Markdown bytes equal a fresh pure projection of validated JSON and contain no independent disposition or authority."
    - "Targeted, partial, warning-bearing golden, failed, diagnostic-only, mixed-attempt, stale-source, or changed-binding inputs cannot produce an H1-ready packet."
    - "The packet contains review material and signature slots but no aggregate disposition; missing human decision remains blocking/incomplete rather than machine-filled."
  </behavior>
  <action>Extend the owning H1 tests first with the exact committed-output generation path, JSON/Markdown round-trip, stale/mutated binding matrix, clean-golden requirement, 11-item completeness, and absent-decision blocking behavior, then run the final five-module suite red. Validate Plan 04's formal attempt and adversarial result again, generate the declared canonical packet and pure Markdown projection, then rerun packet validation from disk. Preserve exact source/adapter/schema/golden/attempt/diagnostic/adversarial hashes, D-14 item semantics, blank signature material, and external_publication_authorized=false. Stop without H1-ready status if any high threat, focused test, artifact validator, projection, or binding check fails; do not create the human decision file.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter tests.test_measurement_parsing tests.test_measurement_scoring tests.test_measurement_attempts tests.test_measurement_h1 -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-attempt --attempt runs/measurement-attempts/formal-golden-pr2164-v1 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-adversarial-report --report reports/h1/adversarial-oracle-results-v1.json &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-h1-packet --packet reports/h1/h1-source-gold-review-v1.json --markdown reports/h1/h1-source-gold-review-v1.md &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>The exact H1 canonical JSON and derived Markdown packet validate from disk, bind every D-16 identity and all 11 D-14 review items, and remain unable to decide or authorize anything.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 02-05-03: Human reviewer authors the H1 source/gold disposition</name>
  <files>experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v1.json</files>
  <what-built>A warning-free all-11 formal attempt, separate exact adversarial artifact, canonical H1 source/gold packet, deterministic Markdown projection, full five-module suite, and a validator that cannot create approval.</what-built>
  <how-to-verify>
    1. Inspect `reports/h1/h1-source-gold-review-v1.json` and its Markdown projection; verify the accepted source, registry, adapter rule/batch, schema, golden prediction, formal-attempt, diagnostics, and adversarial hashes.
    2. Review every fixture's category, expect_extract, normalized count/names, candidate surfaced-then-classify_out semantics, adapter lineage, and exact raw evidence.
    3. Independently author canonical `reviews/h1-source-gold-decision-v1.json` using the published review schema, one signed item per fixture, and exactly one aggregate disposition: approved, disputed, or incomplete. Do not ask the executor or CLI to choose or create approved.
    4. Run `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-h1-decision --packet reports/h1/h1-source-gold-review-v1.json --decision reviews/h1-source-gold-decision-v1.json`; confirm any changed binding fails and external_publication_authorized remains false.
    5. Treat disputed/incomplete as blocking. Treat approved only as local Phase 3 progression; it does not authorize model calls, APIs, push, PR, upload, deployment, publication, or any remote action.
  </how-to-verify>
  <action>The reviewer, not the executor and not Phase 2 code, creates the decision file. After the reviewer supplies it, the executor may only run the read-only validator and report its exact disposition/bindings; it must not create, edit, normalize, infer, or upgrade an approved value. Do not modify ROADMAP, STATE, REQUIREMENTS, Phase 1 custody, or any remote state in this plan.</action>
  <resume-signal>Provide the path to the independently authored decision file and state `approved`, `disputed`, or `incomplete`; include any disputed fixture IDs/reasons.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| immutable Plan 04 evidence → H1 packet | Only validated warning-free formal evidence plus the separate exact adversarial result may enter the bound review packet. |
| canonical H1 JSON → Markdown | JSON is authority; Markdown is a deterministic projection and cannot introduce facts or decisions. |
| H1 packet → human decision | Automation may validate packet/decision bytes and bindings but cannot supply semantic approval. |
| local H1 state → later/external actions | Even approved authorizes only local Phase 3 progression and never model, remote, or publication capability. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | H1 packet/decision closed inputs | high | mitigate | Task 02-05-01 reruns strict parser tests and adds exact-key, duplicate fixture-review, missing/extra signature, invalid disposition, and finite 11-item tests. |
| T-02-02 | Tampering | score-bearing H1 bindings and Markdown | high | mitigate | H1 binds only hashed source/adapter/schema/golden/formal/diagnostic/adversarial identities; Markdown is regenerated from validated JSON and cannot alter facts or disposition. |
| T-02-03 | Tampering / Repudiation | stale/mixed evidence and decision bindings | high | mitigate | Tasks 02-05-01/02 recompute every D-16 hash, reject warning-bearing/partial/diagnostic-only formal inputs, and invalidate any changed packet, projection, item, or source binding. |
| T-02-04 | Elevation of Privilege / Repudiation | H1 approval and external authority | high | mitigate | No decision writer exists; the human authors the closed 11-signature decision, validator forces disputed aggregation and rejects changed bindings, and every state keeps publication false plus local-only progression. |
| T-02-PATH | Tampering / Elevation of Privilege | packet/decision paths | medium | mitigate | Consume experiment-relative regular files through Phase 1 filesystem primitives; reject traversal, links, special files, mount drift, and replacement of existing immutable artifacts. |

All high threats are mitigated by `test_measurement_h1` plus the accumulated four earlier modules. Any failed high mitigation, unexpected golden diagnostic, invalid packet, or invalid human binding blocks H1 and Phase 3 progression.
</threat_model>

<verification>
- Run the final five-module focused Phase 2 suite after each automated task and the exact source-authority validator alongside it.
- Validate the immutable formal attempt, separate adversarial report, canonical H1 JSON, pure Markdown projection, all 11 review items, and every D-16 hash binding.
- Prove source contains no command/API that writes H1 decisions or grants model/remote/publication authority; the independently authored decision remains a blocking checkpoint.
- Confirm disputed/incomplete remains blocking, approved is local-only, and all dispositions preserve external_publication_authorized=false.
- Keep Phase 1 custody and live-boundary tests unchanged; repository-wide discovery is not substituted for the focused Phase 2 gate.
</verification>

<success_criteria>
- TS-03/04/05 final five-module suite passes, and the H1 packet binds the warning-free all-11 formal attempt plus the separate exact adversarial result.
- D-13 through D-16 disposition, per-fixture signature, clean-golden, immutable binding, invalidation, and local-only contracts pass.
- Canonical H1 JSON and derived Markdown are deterministic, hash-bound, and decision-free until the reviewer acts.
- Only a valid independently human-authored H1 decision can authorize local Phase 3 progression; disputed/incomplete remains blocking.
- All T-02-01 through T-02-04 high-risk mitigations pass.
- No machine-created approval, external/model authority, push, publication, Phase 1 custody change, ROADMAP/STATE mutation, or remote action occurs.
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-measurement-spine/02-05-SUMMARY.md` when done, recording the automated packet status and the independently supplied human disposition without altering it.
</output>
