---
phase: 02-deterministic-measurement-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/specchoice-v1.3.2/config/measurement/pr2164-adapter-rules-v1.json
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/__init__.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py
autonomous: true
requirements: [TS-03]
must_haves:
  truths:
    - "Per D-01 and D-03, the adapter consumes only the active accepted source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2 generation through phase2/source-authority.json; candidates, historical accepted generations, revoked generations, live Git, and network inputs are rejected."
    - "Per D-01 through D-04, one canonical adapter batch contains exactly 11 unique sorted fixture records and all 28 independently verified raw identities with one adapter version and rule hash; an empty, singleton, proper-subset, duplicate, or mixed-version batch is never score eligible."
    - "The complete batch deterministically records the frozen 6 positive / 4 negative / 1 candidate partition and derives candidate semantics from expect_extract=true plus expected_parameter_count=0 without scoring directly from raw YAML."
    - "The unresolved TS-03 spec-less adjacency/empty/ordering probe is retained as a defensive contract: duplicate or adjacent fixture declarations cannot collapse, empty/subset input blocks the batch, and all fixture/set output follows the declared fixture_id tie-break."
    - "Per D-02 and D-03, any fixture-directory, registry, expected.yaml, gold.yaml, count, name, category, source path, length, or raw-hash disagreement emits a complete stable diagnostic set and zero canonical records."
    - "No Phase 1 custody module, accepted bundle byte, registry byte, source-authority byte, core UDB schema, generated data, model, external API, publication state, or remote repository is modified."
  artifacts:
    - path: "experiments/specchoice-v1.3.2/config/measurement/pr2164-adapter-rules-v1.json"
      provides: "Canonical adapter version, explicit score-bearing allowlist, bounded source-field rules, category derivation, stable sort key, and rule hash input"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py"
      provides: "Frozen Phase 2 adapter/domain records and typed terminal result contracts"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py"
      provides: "Accepted-v2-only, raw-byte-bound complete PR #2164 adapter batch"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py"
      provides: "Runnable adapt-pr2164 tracer entry point"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py"
      provides: "Task-owned TDD authority, completeness, disagreement, ordering, and immutability tests"
  key_links:
    - from: "experiments/specchoice-v1.3.2/phase2/source-authority.json"
      to: "experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py"
      via: "specchoice_evidence.cli authority validation plus verify_accepted_bundle before any semantic read"
      pattern: "validate_phase2_source_authority|verify_accepted_bundle"
    - from: "experiments/specchoice-v1.3.2/config/fixture-registry-pr2164-v1.json"
      to: "experiments/specchoice-v1.3.2/config/measurement/pr2164-adapter-rules-v1.json"
      via: "exact fixture/path/role/length/SHA-256 closure and versioned allowlisted transformation rules"
      pattern: "fixture_count|raw_file_count|adapter_version|rule_sha256"
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py"
      to: "canonical adapter batch"
      via: "canonical_json_bytes and sha256_bytes over original plus normalized records"
      pattern: "canonical_json_bytes|sha256_bytes|adapter_batch_sha256"
  prohibitions:
    - "flagged-unverified: The adapter must not silently alter authoritative fixture bytes or score-bearing semantics."
---

<objective>
**As a** human RISC-V reviewer, **I want to** inspect one complete versioned PR #2164 adapter batch, **so that** every later measurement is rooted in the exact accepted 11-fixture authority rather than an implicit or mutable interpretation.

Implement the first production tracer from the active accepted verifier-rooted v2 bundle through source-authority validation to one canonical all-fixture adapter batch, then harden its complete-batch disagreement and versioning behavior per D-01 through D-04.

Purpose: Establish TS-03's only score-eligible input boundary before parsing or scoring exists.
Output: Versioned adapter rules, typed adapter records, an `adapt-pr2164` CLI path, and task-owned adapter tests created before production code.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-VERIFICATION.md
@.planning/phases/02-deterministic-measurement-spine/02-CONTEXT.md
@.planning/phases/02-deterministic-measurement-spine/02-RESEARCH.md
@.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md
@.planning/phases/02-deterministic-measurement-spine/02-VALIDATION.md
@experiments/specchoice-v1.3.2/phase2/source-authority.json
@experiments/specchoice-v1.3.2/config/fixture-registry-pr2164-v1.json
@experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
@experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py
@experiments/specchoice-v1.3.2/src/specchoice_evidence/verify.py
@experiments/specchoice-v1.3.2/src/specchoice_evidence/cli.py

<interfaces>
Existing authority primitives to reuse without modifying Phase 1:

```python
from specchoice_evidence.canonical import canonical_json_bytes, sha256_bytes
from specchoice_evidence.filesystem import inspect_authoritative_path, require_relative_posix_path
from specchoice_evidence.verify import verify_accepted_bundle

def verify_accepted_bundle(bundle_root: Path) -> dict[str, str]: ...
def inspect_authoritative_path(root: Path, relative_path: str) -> FileEvidence: ...
```

Contracts this plan creates for later plans:

```python
@dataclass(frozen=True)
class CanonicalFixtureRecord: ...

@dataclass(frozen=True)
class AdapterBatch: ...

def build_pr2164_adapter_batch(*, authority_path: Path, bundle_root: Path, rules_path: Path) -> AdapterBatch: ...
```
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-01-01: Prove the accepted-v2-only adapter path end to end</name>
  <precondition>Phase 1's canonical source-authority file and the named accepted verifier-rooted v2 bundle exist, and the existing validate-phase2-source-authority command exits zero for that exact pair.</precondition>
  <reversibility rating="costly">The versioned adapter record shape is consumed by every later Phase 2 plan; changing it requires a new rule/version and symmetric all-11 regeneration, but no historical artifact is migrated.</reversibility>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py, experiments/specchoice-v1.3.2/config/measurement/pr2164-adapter-rules-v1.json, experiments/specchoice-v1.3.2/src/specchoice_measurement/__init__.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py</files>
  <behavior>
    - "RED first: the exact source-authority plus accepted v2 bundle produces one canonical batch with 11 unique fixture IDs, 28 raw identities, and 6/4/1 categories."
    - "A candidate, historical accepted generation, changed generation/root/snapshot-manifest/registry/commit/tree/count, or non-local/publication-authorized authority is rejected before record emission."
    - "Every declared authoritative path is checked component-wise as a regular file and matched by raw length and SHA-256 before it is decoded."
    - "The CLI emits a byte-stable batch to a caller-supplied new path and a repeated equivalent run yields identical bytes and digest."
  </behavior>
  <action>Create `tests/test_measurement_adapter.py` first and run the focused module to observe the missing adapter failure. Then create the sibling `specchoice_measurement` package and implement the production `adapt-pr2164` path. Reuse `verify_accepted_bundle`, canonical JSON/hash helpers, and the Phase 1 filesystem API; never add a second authority verifier or modify `specchoice_evidence`. Define `pr2164-adapter-rules-v1.json` as canonical JSON with adapter version, bounded expected/gold field grammar, the D-03 scoring allowlist, category derivation, fixture/order tie-break, and a rule hash computed from its canonical bytes. Per D-01, bind every record to adapter/rule identity, accepted generation/root/snapshot-manifest/registry identities, all declared raw path/role/length/SHA-256 records, original score-bearing fields, and normalized case/category/expect_extract/expected_parameter_count/expected_parameter_names/evidence-required values. Use a deliberately bounded reader for the observed PR #2164 YAML field forms; reject unsupported or duplicate score-bearing syntax instead of adding a generic YAML dependency. Keep every other upstream field as provenance that the scorer cannot consume.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>The real CLI validates the exact accepted v2 authority and writes one deterministic all-11 adapter batch without touching Phase 1 custody or any remote/model boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-01-02: Close adapter disagreement, completeness, and version-mixing paths</name>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py, experiments/specchoice-v1.3.2/config/measurement/pr2164-adapter-rules-v1.json, experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py</files>
  <behavior>
    - "Empty, singleton, proper-subset, extra, duplicate, adjacent duplicate, reordered-without-canonicalization, and mixed-adapter-version batches are not score eligible."
    - "A directory/registry/id/class/expected/gold/count/name/path/role/length/hash disagreement yields all applicable structured blockers in the declared stable order and zero records."
    - "Positive expected names come from exact gold name plus expected gold_name agreement; negative and candidate records normalize to zero expected names without fabricating gold."
    - "A rule correction changes adapter version and rule hash, leaves the prior rules/batch untouched, and requires a full symmetric 11-fixture rebuild."
  </behavior>
  <action>Extend the task-owned TDD matrix before implementation with every D-02 complete-batch conflict and the unresolved TS-03 adjacency/empty/ordering cases. Make validation collect all adapter diagnostics, sort by `(severity_rank, code, fixture_id, field, occurrence)`, and return an invalid batch with an explicit empty `records` collection whenever any blocker exists. Enforce exact fixture ID uniqueness, the finite 11-record set, 28 raw-file closure, sorted fixture order, and one adapter/rule identity for the batch. Per D-04, treat rules and emitted batches as immutable versioned artifacts: do not overwrite an existing output and do not permit a caller to mix records or silently reuse a stale rule hash. Assert original authoritative fixture bytes and all Phase 1 artifacts are byte-identical before and after the adapter tests.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>Only one complete, source-consistent, immutable, deterministically ordered 11-record adapter batch can cross into parsing/scoring; every disagreement is explicit and leaves no score-eligible records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted v2 bundle → adapter | Immutable untrusted bytes become derived canonical records only after accepted authority, path, length, and SHA-256 verification. |
| adapter rules → score-bearing record | Only the D-03 allowlist and versioned rule hash may define derived scoring semantics. |
| adapter result → later Phase 2 plans | Only a complete 11-record valid batch may be consumed; invalid results carry diagnostics but no authority. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | registry/rules/bounded expected-gold input | high | mitigate | Task 02-01-01/02 reject duplicate keys/fields, unknown fixture IDs, non-closed registry/rule shapes, and anything other than the exact 11/28 finite set before record emission. |
| T-02-02 | Tampering | adapter normalization and provenance | high | mitigate | D-01/D-03 records preserve raw hashes and original values, restrict score-bearing normalized fields to the explicit allowlist, and keep extra provenance outside scorer input. |
| T-02-03 | Tampering / Repudiation | source path/raw bytes and failed adapter attempts | high | mitigate | Reuse Phase 1 component-wise regular-file reads plus accepted-bundle verification; D-02 blockers carry expected/observed/source hashes and emit an explicit empty record set. |
| T-02-04 | Elevation of Privilege / Repudiation | adapter artifact role | high | mitigate | Closed adapter records have no attempt-authority, completion, H1 disposition, approval, or publication fields; tests reject such fields and later plans must separately bind attempts/H1. |
| T-02-PATH | Tampering / Elevation of Privilege | bundle-relative source paths | medium | mitigate | Reuse `require_relative_posix_path` and `inspect_authoritative_path`; reject traversal, links, special files, and mount-boundary drift before reads. |

Every high threat is mitigated by an automated test in `tests.test_measurement_adapter`; any failing mitigation blocks this plan and all later waves.
</threat_model>

<verification>
- Run Task 02-01-01/02's focused adapter suite after each task and the exact Phase 1 source-authority validator alongside it.
- Confirm two equivalent `adapt-pr2164` runs in separate temporary directories produce byte-identical canonical batches and digests.
- Confirm all candidate/historical/drift/subset/duplicate/mixed-version cases exit nonzero, yield no score-eligible records, and leave accepted raw bytes unchanged.
- Do not use repository-wide discovery as this phase gate and do not change Phase 1 live-boundary tests to make later artifacts appear historical.
</verification>

<success_criteria>
- TS-03 has a runnable adapter tracer rooted only in the active accepted verifier-rooted v2 bundle.
- The canonical adapter batch contains exactly 11 unique fixtures, 28 raw identities, one version/rule hash, stable ordering, and the frozen 6/4/1 partition.
- Any adapter conflict or incomplete input produces deterministic structured blockers and no records.
- All T-02-01 through T-02-04 high-risk mitigations pass.
- No model call, external API, network fetch, push, publication, deployment, Phase 1 custody change, or core UDB change occurs.
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-measurement-spine/02-01-SUMMARY.md` when done.
</output>
