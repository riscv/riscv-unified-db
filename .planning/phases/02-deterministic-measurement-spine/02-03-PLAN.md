---
phase: 02-deterministic-measurement-spine
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - experiments/specchoice-v1.3.2/fixtures/measurement/golden-predictions-v1.json
  - experiments/specchoice-v1.3.2/fixtures/measurement/adversarial/required-diagnostics-v1.json
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/scoring.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_scoring.py
autonomous: true
requirements: [TS-03, TS-05]
must_haves:
  truths:
    - "The formal golden input covers exactly the 11 unique adapter fixtures under one adapter/schema/prediction identity; empty, singleton, subset, duplicate, mixed-attempt, or diagnostic-only input cannot emit formal metrics."
    - "TS-03 golden scoring independently reports surfacing, disposition, identity, and evidence integrity for all 11 cases: six positives are surfaced/accepted with exact names, four negatives are not surfaced, and CAND_WARL_FIXED_LEGAL_SET is surfaced then classify_out."
    - "Per D-10, ACCEPTED_PARAMETER_NAME_MISSING changes only identity coverage, preserves surfacing/disposition, remains a warning, and may yield completed_with_warnings outside the warning-free formal golden attempt."
    - "Every surfaced prediction has at least one exact `{source_sha256,start_byte,end_byte,text}` span whose half-open byte range `[start_byte,end_byte)` is non-empty, in bounds, UTF-8-decodable, and equal to the authoritative raw source bytes before report normalization."
    - "Duplicate and adjacent evidence spans are never implicitly merged, trimmed, normalized, or deduplicated; each declared span is retained and independently validated exactly."
    - "The unresolved TS-05 adjacency/empty/ordering probe is retained defensively: span/diagnostic adjacency is explicit, no-diagnostic output is [], and cases/findings/spans/diagnostics use declared stable sort and tie-break rules."
    - "Every frozen required diagnostic code has a structured oracle containing stable code, severity, fixture/finding identity, field, occurrence, expected/observed values, and source identities where applicable."
  artifacts:
    - path: "experiments/specchoice-v1.3.2/fixtures/measurement/golden-predictions-v1.json"
      provides: "Hashable exact all-11 golden canonical predictions with candidate surfaced/classified out"
    - path: "experiments/specchoice-v1.3.2/fixtures/measurement/adversarial/required-diagnostics-v1.json"
      provides: "Frozen required-code adversarial inputs and complete structured diagnostic oracles"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/scoring.py"
      provides: "Pure independent case outcomes, aggregate denominators, and exact evidence verification"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_scoring.py"
      provides: "Task-owned TDD golden, candidate, warning separation, evidence, and required-code tests"
  key_links:
    - from: "Plan 02 PreflightResult"
      to: "experiments/specchoice-v1.3.2/src/specchoice_measurement/scoring.py"
      via: "score only when status is valid and formal coverage equals the exact adapter fixture set"
      pattern: "blocking_diagnostics|parsed_predictions|formal"
    - from: "canonical prediction evidence_spans"
      to: "accepted v2 raw fixture source"
      via: "adapter-bound source SHA-256 plus exact byte offsets and literal UTF-8 text"
      pattern: "source_sha256|start_byte|end_byte|text"
    - from: "per-case outcomes"
      to: "aggregate metrics"
      via: "separate surfacing/disposition/identity/evidence numerators and explicit denominators"
      pattern: "surfacing|disposition|identity|evidence_integrity"
  prohibitions:
    - "flagged-unverified: A targeted, partial, failed, or diagnostic-only result must not be presented as formal measurement authority."
    - "flagged-unverified: The scorer must not silently alter authoritative fixture bytes or score-bearing semantics."
---

<objective>
**As an** ML evaluation reviewer, **I want to** trace every golden and adversarial score to an exact case, diagnostic, and raw evidence span, **so that** candidate behavior and identity warnings cannot be hidden inside a blended result.

Implement the production all-11 golden scoring tracer, then expand it to exact evidence integrity, complete required diagnostic oracles, stable ordering, and independent metric dimensions per D-08 through D-10, D-12, and D-15.

Purpose: Complete TS-03 measurement semantics and TS-05 diagnostic behavior before immutable attempts/H1 custody.
Output: Golden/adversarial fixtures, deterministic scorer, stable scoring diagnostics, and task-owned scoring tests created before production code.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-deterministic-measurement-spine/02-CONTEXT.md
@.planning/phases/02-deterministic-measurement-spine/02-RESEARCH.md
@.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md
@.planning/phases/02-deterministic-measurement-spine/02-VALIDATION.md
@.planning/phases/02-deterministic-measurement-spine/02-01-SUMMARY.md
@.planning/phases/02-deterministic-measurement-spine/02-02-SUMMARY.md
@experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py

<interfaces>
Plan 02 contract consumed here:

```python
@dataclass(frozen=True)
class PreflightResult:
    status: str
    raw_prediction_sha256: str
    parsed_predictions: tuple[object, ...]
    diagnostics: tuple[Diagnostic, ...]
```

Contracts this plan creates for Plan 04:

```python
@dataclass(frozen=True)
class ScoreResult:
    status: str
    case_outcomes: tuple[object, ...]
    metrics: object | None
    diagnostics: tuple[Diagnostic, ...]

def score_prediction_batch(*, adapter_batch: AdapterBatch, preflight: PreflightResult, mode: str) -> ScoreResult: ...
```
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-03-01: Score the exact golden all-11 path with independent outcomes</name>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_scoring.py, experiments/specchoice-v1.3.2/fixtures/measurement/golden-predictions-v1.json, experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/scoring.py</files>
  <behavior>
    - "RED first: one current-schema golden set covers exactly the 11 adapter records and produces six correct positive accepts, four correct negative no-findings, and one surfaced classify_out candidate."
    - "Surfacing, disposition, identity, and evidence integrity are separate per-case fields and separate aggregate numerator/denominator objects."
    - "Candidate not surfaced, accepted, or left review produces CANDIDATE_NOT_SURFACED, CANDIDATE_ACCEPTED_AS_PARAMETER, or CANDIDATE_LEFT_UNRESOLVED respectively and cannot pass candidate disposition."
    - "Positive missing/classified-out and negative surfaced/accepted cases produce the frozen stable codes with exact structured fields."
  </behavior>
  <action>Create `tests/test_measurement_scoring.py` and the canonical golden prediction fixture first, then run the focused test red. Populate exactly one prediction for each adapter fixture using authoritative source hashes and exact evidence spans; the candidate must be surfaced with classify_out and must not gain a proposed parameter name. Implement a pure scorer that accepts only a zero-blocker PreflightResult and the exact all-11 adapter identity in formal mode. Derive independent per-case surfacing, disposition, identity, and evidence fields before aggregating explicit numerators/denominators; do not publish one blended headline score. Implement the frozen positive/negative/candidate diagnostic codes with stable structured fields. A formal coverage mismatch or diagnostic-only mode returns no formal metrics per D-09/D-12.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_scoring -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>The real scoring path produces correct, inspectable, independently denominated golden outcomes for all 11 fixtures, including surfaced-then-classify_out candidate behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-03-02: Verify exact spans and every required diagnostic oracle</name>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_scoring.py, experiments/specchoice-v1.3.2/fixtures/measurement/adversarial/required-diagnostics-v1.json, experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/scoring.py</files>
  <behavior>
    - "Each surfaced `{source_sha256,start_byte,end_byte,text}` span independently matches adapter-bound raw source SHA-256, a non-empty in-range half-open byte slice `[start_byte,end_byte)`, and exact text before canonical report normalization."
    - "Duplicate and adjacent valid spans remain distinct ordered records; empty, out-of-range, invalid-boundary, changed-source, or text-mismatch spans fail with exact code/fields and never get searched or repaired."
    - "ACCEPTED_PARAMETER_NAME_MISSING lowers only identity coverage, leaves surfacing/disposition unchanged, and yields a warning-only completed_with_warnings score outside the formal golden set."
    - "The adversarial oracle covers every frozen required runner code, parser codes from Plan 02, and any added stable evidence-range/hash codes with byte-stable diagnostic order and explicit empty arrays."
  </behavior>
  <action>Author `required-diagnostics-v1.json` as a canonical, versioned adversarial matrix whose entries include raw input identity and the complete expected diagnostic record, not prose-only assertions. Add D-08 exact raw-byte evidence tests before implementation, including duplicate and adjacent spans that must remain separate, empty spans, changed source hashes, invalid ranges/UTF-8 boundaries, and literal mismatch. Never trim, normalize, merge, deduplicate, or fuzzy-search evidence. Implement evidence outcomes and additional stable range/hash diagnostics as needed. Implement D-10 so the accepted-name warning changes identity only and can produce `completed_with_warnings`; assert the formal golden fixture has no warning/error per D-15. Permute cases, findings, spans, and diagnostics in tests to prove declared stable ordering and explicit empty diagnostic collections.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_scoring -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>All required codes/fields, identity-warning separation, exact evidence integrity, adjacency/empty/ordering behavior, and the warning-free golden contract are deterministic and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zero-blocker preflight → scorer | Only complete parsed input bound to one adapter/schema identity may be scored. |
| evidence span → authoritative raw source | Claimed source hash/range/text must equal exact accepted raw bytes before normalization. |
| case outcomes → aggregates | Independent metrics and explicit denominators prevent score-bearing omission or blending. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | formal prediction/case identity | high | mitigate | Task 02-03-01 requires exactly 11 unique adapter-bound case records and rejects duplicate/conflicting/unknown/subset coverage before metrics. |
| T-02-02 | Tampering | scoring dimensions and extra data | high | mitigate | Scoring reads only Plan 01 allowlisted gold plus Plan 02 canonical fields; separate dimension functions/denominators and tests prevent extra provenance or identity warnings from changing disposition. |
| T-02-03 | Tampering / Repudiation | raw evidence and partial results | high | mitigate | Task 02-03-02 validates each raw span hash/range/text independently; formal-mode completeness and null metrics on failure/diagnostic-only results prevent partial authority. |
| T-02-04 | Elevation of Privilege / Repudiation | ScoreResult status | high | mitigate | Closed ScoreResult contains case outcomes/metrics/diagnostics only, cannot create attempts or H1 approval, and marks targeted results diagnostic_only with no formal-authority field. |
| T-02-PATH | Tampering / Elevation of Privilege | evidence source lookup | medium | mitigate | Resolve evidence only through adapter-declared accepted-bundle relative paths and Phase 1 regular-file checks; no caller-controlled traversal or alternate file lookup. |

All high threats are exercised by golden/adversarial tests; any failed mitigation blocks attempt/H1 implementation.
</threat_model>

<verification>
- Run `tests.test_measurement_scoring` and source-authority validation after each task.
- Assert exact 11-case golden outcomes, independent denominators, candidate behavior, complete code/field oracles, warning-only identity behavior, and byte-stable order.
- Assert duplicate/adjacent spans remain distinct and each is verified against raw bytes; empty/not-found/range/hash failures never receive normalization or search repair.
- Assert subset, failed, and diagnostic-only results contain no formal metrics/authority and never appear in the H1-ready golden result.
- Keep Phase 1 live-boundary tests unchanged; the focused Phase 2 suite remains the acceptance gate.
</verification>

<success_criteria>
- Golden predictions correctly score all 11 fixtures with the exact 6/4/1 semantics and candidate surfaced/classified out.
- TS-05 required codes and structured fields are fully covered, stably ordered, and explicit when empty.
- Identity warnings cannot change disposition; evidence claims are raw-byte exact and independently retained.
- All T-02-01 through T-02-04 high-risk mitigations pass.
- No partial/formal authority confusion, model/API call, publication, push, Phase 1 mutation, or remote action occurs.
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-measurement-spine/02-03-SUMMARY.md` when done.
</output>
