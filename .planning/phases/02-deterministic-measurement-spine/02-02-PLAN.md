---
phase: 02-deterministic-measurement-spine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - experiments/specchoice-v1.3.2/config/measurement/canonical-adjudication-schema-v1.json
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_parsing.py
autonomous: true
requirements: [TS-04, TS-05]
must_haves:
  truths:
    - "Per D-05, D-06, and D-07, formal canonical prediction input is JSON-only and closed at payload, prediction, adjudication, and evidence-span levels; unknown, missing, duplicate, ill-typed, and invalid-enum data is rejected without defaults or coercion, while legacy compatibility is a separately declared ingress."
    - "Per D-07, the sole no-finding input representation explicitly contains surfaced=false, parameter_status=null, proposed_name=null, and evidence_spans=[]; parameter_status=not_surfaced and omitted null/empty fields are invalid."
    - "Per D-05, parameter_status=reject is rejected by the current schema and may become classify_out only through a named legacy ingress that preserves raw bytes plus exact before/after normalization diagnostics."
    - "Per D-06 and D-08, every evidence span is exactly `{source_sha256,start_byte,end_byte,text}` with a non-empty half-open byte range `[start_byte,end_byte)`; spans are independently checked against authoritative raw bytes and never implicitly merged, trimmed, case-folded, fuzzily matched, Unicode-normalized, deduplicated, or rewritten before validation."
    - "Per D-09, one side-effect-free complete-batch preflight collects all blockers before deciding; any blocker exits nonzero and exposes no metric or formal-report fields."
    - "All diagnostic and set-valued output uses the declared total ordering `(severity_rank, code, fixture_id, field, occurrence)` with fixture/finding tie-breaks; when no diagnostic exists the canonical output contains an explicit empty diagnostics collection."
    - "Flagged assumption: the TS-04 spec-less prohibition/edge probe remains unclassified and is not represented as a resolved probe result; its defensive implementation is the explicit D-05 through D-08 closed-schema matrix and exact no-repair tests."
  artifacts:
    - path: "experiments/specchoice-v1.3.2/config/measurement/canonical-adjudication-schema-v1.json"
      provides: "Reviewable closed canonical JSON contract and exact evidence-span shape"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py"
      provides: "Duplicate-key-safe JSON decoder, current-schema validator, and named legacy ingress"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py"
      provides: "Frozen stable diagnostic records, severity semantics, and total ordering"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py"
      provides: "Pure all-11 validation and single terminal decision"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_parsing.py"
      provides: "Task-owned TDD closed-schema, no-finding, duplicate, legacy, evidence-range, ordering, and empty-output matrix"
  key_links:
    - from: "raw prediction bytes"
      to: "experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py"
      via: "UTF-8 JSON decode with object_pairs_hook duplicate rejection and parse_constant rejection"
      pattern: "object_pairs_hook|parse_constant"
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py"
      to: "experiments/specchoice-v1.3.2/config/measurement/canonical-adjudication-schema-v1.json"
      via: "exact key/type/enum/cross-field checks with schema identity carried into parsed output"
      pattern: "schema_version|parameter_status|evidence_spans"
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py"
      to: "Plan 03 scoring"
      via: "zero-blocker parsed prediction set or invalid_preflight with no score-bearing payload"
      pattern: "invalid_preflight|blocking_diagnostics|parsed_predictions"
  prohibitions:
    - "flagged-unverified: The parser must not silently alter authoritative fixture bytes, evidence bytes, or score-bearing semantics."
---

<objective>
**As an** experiment operator, **I want to** receive a complete deterministic validation result for the raw prediction set, **so that** malformed or noncanonical adjudication can never be silently repaired into scoreable evidence.

Build the production raw-JSON-to-preflight tracer, then close the current/legacy schema boundary and stable diagnostic collection per D-05 through D-09.

Purpose: Satisfy TS-04's strict canonical adjudication contract and establish TS-05's diagnostic substrate before scoring exists.
Output: Canonical schema description, strict decoder/validator, typed diagnostics, pure preflight, and task-owned parsing tests created before production code.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-deterministic-measurement-spine/02-CONTEXT.md
@.planning/phases/02-deterministic-measurement-spine/02-RESEARCH.md
@.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md
@.planning/phases/02-deterministic-measurement-spine/02-VALIDATION.md
@.planning/phases/02-deterministic-measurement-spine/02-01-SUMMARY.md
@experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py

<interfaces>
Plan 01 contracts consumed here:

```python
@dataclass(frozen=True)
class AdapterBatch:
    adapter_version: str
    rule_sha256: str
    records: tuple[CanonicalFixtureRecord, ...]
    adapter_batch_sha256: str
```

Contracts this plan creates for Plans 03-05:

```python
@dataclass(frozen=True)
class Diagnostic:
    code: str
    severity: str
    fixture_id: str | None
    field: str
    occurrence: int
    expected: object | None
    observed: object | None
    source_sha256: str | None

@dataclass(frozen=True)
class PreflightResult:
    status: str
    raw_prediction_sha256: str
    parsed_predictions: tuple[object, ...]
    diagnostics: tuple[Diagnostic, ...]

def preflight_prediction_batch(*, raw: bytes, adapter_batch: AdapterBatch, ingress: str) -> PreflightResult: ...
```
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-02-01: Parse one complete current-schema prediction set without repair</name>
  <reversibility rating="costly">Later phases persist and hash this versioned adjudication/evidence contract; changing it requires a new schema version and symmetric reruns, while prior artifacts remain immutable.</reversibility>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_parsing.py, experiments/specchoice-v1.3.2/config/measurement/canonical-adjudication-schema-v1.json, experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py</files>
  <behavior>
    - "RED first: an exact current-schema JSON payload with 11 unique case records and the Plan 01 adapter hash produces a valid preflight with explicit diagnostics=[]."
    - "Duplicate JSON keys, duplicate/conflicting case or finding records, unknown keys at any level, missing fields, non-JSON constants, wrong types, unknown case IDs, invalid enums, and extra score-bearing values produce stable structured blockers."
    - "Every no-finding tuple variant except the four explicit canonical values is rejected, including parameter_status=not_surfaced and omitted null/empty fields."
    - "Every evidence span has exactly source_sha256/start_byte/end_byte/text; `[start_byte,end_byte)` is in-range and non-empty, and adjacent or duplicate spans remain separate records for independent validation."
    - "A blocker yields invalid_preflight, nonzero CLI semantics, complete sorted diagnostics, and no metrics/report/approval fields."
  </behavior>
  <action>Create `tests/test_measurement_parsing.py` first with the valid all-11 tracer and the D-06/D-07 adversarial matrix, then run it red. Define formal canonical input as versioned JSON-only; keep the declared legacy route separate and do not add an implicit YAML or compatibility parser. Implement strict UTF-8 decoding with `object_pairs_hook` duplicate detection and `parse_constant` rejection. Define exact keys for payload, prediction, adjudication, and evidence span; the span record is `{source_sha256,start_byte,end_byte,text}`, its half-open byte range is in bounds and non-empty, and each span remains independently addressable even when duplicate or adjacent. Enforce one prediction record per exact fixture ID for formal input, stable finding identity, explicit rationale, `surfaced=true` status rules, and the sole explicit no-finding tuple. Preserve raw bytes and their SHA-256 separately; use `canonical_json_bytes` only for a derived post-validation projection. Implement typed diagnostics and pure preflight so validation collects across the full set before returning one status per D-09.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_parsing -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>A complete current-schema JSON prediction set crosses raw bytes → strict decoder → closed validation → pure preflight, while malformed input yields all blockers and no scoring authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-02-02: Isolate the legacy alias and freeze diagnostic ordering</name>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_parsing.py, experiments/specchoice-v1.3.2/config/measurement/canonical-adjudication-schema-v1.json, experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py</files>
  <behavior>
    - "The current ingress rejects reject; only the explicitly selected legacy-pr2164-v1 ingress maps reject to classify_out."
    - "Legacy normalization preserves raw bytes, raw hash, fixture/finding identity, field path, before=reject, after=classify_out, and a stable non-silent normalization code."
    - "Equivalent blocker sets in different input order produce byte-identical diagnostic arrays sorted by the declared total key and stable occurrence tie-break."
    - "Zero-diagnostic results serialize an explicit empty array; no diagnostic, prediction, evidence span, or fixture set is dropped because it is empty or adjacent to another record."
  </behavior>
  <action>Add the D-05 legacy/current separation tests and the unresolved TS-05 adjacency/empty/ordering cases before implementation. Make ingress selection an explicit argument that is itself persisted in the parsed projection; no content sniffing or hidden compatibility branch is allowed. Emit a stable normalization diagnostic containing the exact raw-before/raw-after values without altering the stored raw bytes. Declare one total diagnostic sort key and one fixture/finding ordering contract in code/config, apply it once after complete collection, and retain explicit empty arrays. Per D-08, reject whitespace, case, Unicode, fuzzy, or evidence transformations rather than recording them as compatibility behavior. Keep all parsed/preflight structures free of scoring, attempt-completion, H1 disposition, approval, and external-publication fields.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_parsing -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>Legacy compatibility is a named, auditable one-field ingress; current parsing stays strict, all diagnostics/empty sets are explicit and deterministic, and no silent repair path exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw prediction bytes → strict JSON | Untrusted local bytes must retain duplicate keys/errors instead of inheriting permissive decoder behavior. |
| decoded object → canonical adjudication | Exact closed keys, types, enums, and cross-field invariants decide validity. |
| preflight → scorer | Only a complete zero-blocker result may expose parsed predictions; invalid results expose diagnostics only. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | strict_json decoder and closed schema | high | mitigate | Task 02-02-01 rejects duplicate JSON keys, duplicate/conflicting prediction identities, unknown/missing keys, wrong types, and invalid enums at every score-bearing level. |
| T-02-02 | Tampering | legacy ingress and semantic values | high | mitigate | Task 02-02-02 permits only declared reject→classify_out normalization with raw/before/after trace; all other semantic transformations reject under D-08. |
| T-02-03 | Tampering / Repudiation | raw prediction/evidence identity and preflight result | high | mitigate | Raw bytes/hash remain separate, all blockers are collected/sorted, invalid_preflight is nonzero, and no metric/report fields exist on partial/failed results. |
| T-02-04 | Elevation of Privilege / Repudiation | parsed/preflight artifact role | high | mitigate | Closed schemas forbid attempt completion, H1 disposition/approval, and publication fields; parsed output can authorize only later scoring after zero-blocker preflight. |
| T-02-PATH | Tampering / Elevation of Privilege | caller-supplied input paths | medium | mitigate | Consume paths through the existing Phase 1 regular-file/path primitives and persist repository-relative identities only. |

All high threats have exact adversarial tests in `tests.test_measurement_parsing`; any unmitigated high result blocks Plans 03-05.
</threat_model>

<verification>
- Run `tests.test_measurement_parsing` and the exact source-authority validator after each task.
- Assert the full closed-schema matrix, duplicate-key behavior, current/legacy separation, stable code/field payloads, explicit empty diagnostics, and byte-identical ordering across permuted inputs.
- Assert invalid preflight cannot serialize metrics, reports, formal completion, H1 disposition, or publication authority.
- Preserve the focused Phase 2 gate; do not weaken or rebase Phase 1 live-boundary assertions to make repository-wide discovery green.
</verification>

<success_criteria>
- TS-04's canonical adjudication, unique no-finding representation, unknown-key rejection, enum enforcement, and no-repair contract are executable.
- TS-05 has deterministic structured diagnostic records with explicit empty output and a declared total ordering.
- The legacy alias is visible and scoped; current input containing reject remains invalid.
- All T-02-01 through T-02-04 high-risk mitigations pass.
- No source-byte mutation, metric emission on blockers, model/API call, publication, push, Phase 1 change, or remote action occurs.
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-measurement-spine/02-02-SUMMARY.md` when done.
</output>
