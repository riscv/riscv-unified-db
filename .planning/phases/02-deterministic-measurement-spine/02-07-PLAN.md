---
phase: 02-deterministic-measurement-spine
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_attempts.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py
autonomous: true
gap_closure: true
requirements: [TS-03, TS-04, TS-05]
must_haves:
  truths:
    - "A standalone adversarial-report validation succeeds only when one real measurement attempt validates as role=formal and status=completed and its verified attempt_sha256 equals bindings.formal_attempt_sha256."
    - "Changing only an adversarial report's formal_attempt_sha256 to a different 64-hex digest yields ADVERSARIAL_REPORT_INVALID even when the report and every diagnostic-only attempt are otherwise canonical and valid."
    - "Per D-02, a source/gold/expected disagreement still returns an invalid adapter batch with records=[] while preserving the already-verified source_identity and one stable structured diagnostic containing fixture_id, field, expected, observed, and both relevant raw hashes."
    - "The valid accepted-v2 adapter path remains one exact 11-fixture/28-raw-file batch, and current prediction/scoring/H1 semantics remain unchanged per D-01 and D-03 through D-16."
    - "The current H1 v2 packet remains valid, human-authored authority remains local-only, and external_publication_authorized remains false after both repairs."
    - "The Phase 2 plus filesystem focused partition grows only by the two named regressions, from 59 to 61 passing tests; the repository discovery partition grows only from 130 to 132 discovered tests."
    - "The five exact Phase 1 live-boundary methods remain the only expected-red methods; excluding them leaves exactly 127 green tests after the two new regressions."
  artifacts:
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py"
      provides: "Adversarial-report validation rooted in a verified completed formal attempt plus an explicit standalone CLI formal-attempt input"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py"
      provides: "H1 call-site propagation of the exact formal attempt into adversarial validation"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py"
      provides: "Typed adapter failure propagation that retains verified source identity and complete source/gold conflict diagnostics"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py"
      provides: "Existing adapter Diagnostic extended with deterministic relevant-source hash fields rather than a second diagnostic system"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_attempts.py"
      provides: "CR-01 fail-first digest-only lineage-tamper regression and updated explicit formal-attempt call sites"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py"
      provides: "CR-02 fail-first public-builder conflict-provenance regression and same-interpreter subprocess coverage"
  key_links:
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py::validate_adversarial_report"
      to: "experiments/specchoice-v1.3.2/src/specchoice_measurement/attempts.py::validate_measurement_attempt"
      via: "validate the supplied formal attempt first, require formal/completed, and feed only its verified attempt_sha256 into _adversarial_bindings"
      pattern: "validate_measurement_attempt.*formal_attempt|formal_attempt_sha256"
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py::_expected_bindings"
      to: "experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py::validate_adversarial_report"
      via: "pass the same formal_attempt path already validated for the H1 packet"
      pattern: "validate_adversarial_report.*formal_attempt"
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py::_record_from_fixture"
      to: "experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py::_invalid_batch"
      via: "carry the existing typed Diagnostic through AdapterError and retain source_identity after authority verification"
      pattern: "GOLD_NAME_MISMATCH|source_hashes|source_identity"
  assumptions:
    - "flagged assumption — TS-03 adjacency remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-03 empty/null/single-element remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-03 stable ordering on equal elements remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-04 remains unclassified/manual review in the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-05 adjacency remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-05 empty/null/single-element remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-05 stable ordering on equal elements remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
  prohibitions:
    - "flagged-unverified: The repair must not regenerate or mutate accepted bundle bytes, source-authority bytes, formal/adversarial/H1 evidence, human decisions, Phase 1 custody, or core UDB schemas/generated data."
    - "flagged-unverified: The repair must not create or expand H1 approval, Phase 3 authority, model/API/remote capability, or publication authority; external_publication_authorized remains false."
---

<objective>
**As a** human RISC-V reviewer, **I want to** trust the experiment's adjudication semantics and diagnostics, **so that** I can review the measurement spine before any frame, retrieval, or model result is considered.

Close CR-01 and CR-02 with two production fail-first slices: root standalone adversarial validation in a real completed formal attempt, then preserve complete adapter conflict provenance without weakening fail-closed zero-record behavior. Include WR-01 only as the adjacent two-line `sys.executable` repair in the already-modified adapter test module.

Purpose: Restore the two failed TS-05 trust links while regression-preserving TS-03, TS-04, D-01 through D-16, the accepted-v2 11/28 authority, the local-only H1 boundary, and the exact Phase 1 expected-red classification.
Output: Six narrowly modified source/test files, two new fail-first regression methods, one required CLI flag, no new dependency, and no regenerated evidence.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-measurement-spine/02-CONTEXT.md
@.planning/phases/02-deterministic-measurement-spine/02-RESEARCH.md
@.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md
@.planning/phases/02-deterministic-measurement-spine/02-VALIDATION.md
@.planning/phases/02-deterministic-measurement-spine/02-VERIFICATION.md
@.planning/phases/02-deterministic-measurement-spine/02-REVIEW.md
@.planning/phases/02-deterministic-measurement-spine/02-01-SUMMARY.md
@.planning/phases/02-deterministic-measurement-spine/02-04-SUMMARY.md
@.planning/phases/02-deterministic-measurement-spine/02-05-SUMMARY.md

<interfaces>
Current attempt and report interfaces to reuse rather than duplicate:

```python
def validate_measurement_attempt(*, attempt_root: Path) -> dict[str, object]:
    # returns {"attempt_sha256": ..., "role": ..., "status": ...} after canonical,
    # raw-byte, binding, sibling-artifact, and replay validation

def load_measurement_attempt_manifest(*, attempt_root: Path) -> dict[str, object]: ...

def validate_adversarial_report(*, report_path: Path) -> dict[str, object]: ...
```

Current adapter/domain interfaces to extend in place:

```python
class AdapterError(ValueError): ...

@dataclass(frozen=True)
class Diagnostic:
    code: str
    severity: str
    fixture_id: str = ""
    field: str = ""
    occurrence: int = 0
    expected: object | None = None
    observed: object | None = None
    source_sha256: str | None = None

def build_pr2164_adapter_batch(*, authority_path: Path, bundle_root: Path, rules_path: Path) -> AdapterBatch: ...
```
</interfaces>
</context>

## Artifacts this phase produces

- New production modules, classes, public functions, persisted JSON schemas, evidence files, attempts, reports, packets, decisions, hashes, and dependencies: none.
- Modified production symbols in `cli.py`: `_adversarial_bindings`, `command_run_adversarial_oracles`, `validate_adversarial_report`, `command_validate_adversarial_report`, and `build_parser`'s `validate-adversarial-report` subcommand.
- Modified production symbol in `h1.py`: `_expected_bindings` passes its existing `formal_attempt` path through the standalone report validator.
- Modified production symbols in `adapter.py`: `AdapterError`, `_record_from_fixture`, `_invalid_batch`, and `build_pr2164_adapter_batch`.
- Modified production contract in `domain.py`: the existing adapter `Diagnostic` gains one deterministic `source_hashes` mapping for the relevant fixture roles; no parallel diagnostic type is added.
- New CLI flag: required `validate-adversarial-report --formal-attempt PATH`; the report's persisted `adversarial-oracle-results-v2` shape is unchanged because it already contains `bindings.formal_attempt_sha256`.
- New tests: `MeasurementAttemptTests.test_adversarial_report_rejects_forged_formal_attempt_binding` and `MeasurementAdapterTests.test_public_builder_preserves_conflict_provenance`.
- Updated tests/call sites: every existing `validate_adversarial_report` invocation receives the corresponding formal-attempt path; the two adapter CLI subprocess invocations use `sys.executable`.

## Probe Disposition

- Exactly seven unresolved spec-less items remain explicit in `must_haves.assumptions`: TS-03 adjacency; TS-03 empty/null/single-element; TS-03 stable ordering on equal elements; TS-04 unclassified/manual review; TS-05 adjacency; TS-05 empty/null/single-element; TS-05 stable ordering on equal elements.
- None is converted into new implementation scope, a positive verification claim, or a dismissed item by this gap plan.

## Prohibition Recall

Stage 1 surfaced nine candidates: mutation of frozen evidence; expansion of H1/publication authority; trust in a self-declared lineage digest; loss of conflict provenance; weakening Phase 1 expected-red behavior; broad test-count drift; path/symlink bypass; package-supply-chain expansion; and unnecessary new abstraction.

Stage 2 keeps the first two value/safety boundaries as the two descriptor-less `flagged-unverified` prohibitions above. It routes self-declared lineage, conflict provenance, expected-red identity, count drift, and abstraction control into concrete task behavior/acceptance gates; path/symlink concerns and supply-chain concerns remain in the ASVS L1 threat register. Prohibition no-silent-drop equality: 9 surfaced = 2 authored prohibitions + 7 explicitly routed/dropped candidates.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Reviewer trusts adjudication semantics and diagnostics before frame, retrieval, or model results | Tasks 02-07-01 and 02-07-02 | COVERED |
| REQ | TS-03 | Versioned accepted-v2 all-11 adapter and deterministic golden measurement remain intact | Task 02-07-02 plus 61-test focused gate | COVERED |
| REQ | TS-04 | Closed canonical validation and no silent repair remain intact | Both tasks plus 61-test focused gate | COVERED |
| REQ | TS-05 | Stable complete structured diagnostics and trustworthy report lineage | CR-01 in Task 02-07-01; CR-02 in Task 02-07-02 | COVERED |
| RESEARCH | — | Reuse stdlib, canonical hashing, existing attempt validation, AdapterBatch/Diagnostic, and no new dependency | Both tasks | COVERED |
| RESEARCH | — | Accepted verifier-rooted v2 source only; exact 11 fixtures/28 raw files | Task 02-07-02 source-authority gate | COVERED |
| RESEARCH | — | Immutable formal/diagnostic role separation and H1 binding invalidation | Task 02-07-01 | COVERED |
| RESEARCH | — | Focused Phase 2 gate without weakening Phase 1 live-boundary evidence | Task 02-07-02 exact partition gate | COVERED |
| CONTEXT | D-01 | Canonical adapter records retain exact lineage | Task 02-07-02 | COVERED |
| CONTEXT | D-02 | Source/gold/expected conflict blocks and preserves complete provenance | Task 02-07-02 | COVERED |
| CONTEXT | D-03 | Score-bearing allowlist remains unchanged | Task 02-07-02 regression gate | COVERED |
| CONTEXT | D-04 | Adapter version/rule identity remains unchanged and unmixed | Task 02-07-02 regression gate | COVERED |
| CONTEXT | D-05, D-06, D-07, D-08 | Legacy ingress, closed schema, sole no-finding form, and exact raw evidence remain unchanged | 61-test focused gate | COVERED |
| CONTEXT | D-09, D-10 | Complete preflight and warning-only identity separation remain unchanged | 61-test focused gate | COVERED |
| CONTEXT | D-11, D-12 | Immutable attempt validation and formal/diagnostic-only separation root CR-01 | Task 02-07-01 | COVERED |
| CONTEXT | D-13, D-14 | Human-only three-state H1 and all-11 semantic review remain unchanged | Task 02-07-01 H1 revalidation | COVERED |
| CONTEXT | D-15, D-16 | Clean formal attempt, exact adversarial diagnostics, immutable bindings, and local-only authority | Task 02-07-01 | COVERED |
| VERIFICATION | CR-01 | Standalone report validator trusts its own formal digest | Task 02-07-01 | COVERED |
| VERIFICATION | CR-02 | Adapter conflict discards provenance and verified identity | Task 02-07-02 | COVERED |
| REVIEW | WR-01 | Adapter test child interpreter can drift | Task 02-07-02 adjacent repair | COVERED |

All actionable gap sources are covered. CONTEXT has no deferred Phase 2 idea, and the seven spec-less probe items remain explicitly unresolved rather than becoming repair scope.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-07-01: Root standalone adversarial validation in verified formal-attempt bytes</name>
  <precondition>The committed formal-golden-pr2164-v1 attempt passes `validate-attempt` as formal/completed, and the current v2 adversarial report plus attempt directory exist as read-only Phase 2 evidence.</precondition>
  <files>experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py, experiments/specchoice-v1.3.2/tests/test_measurement_attempts.py</files>
  <read_first>
    - `src/specchoice_measurement/cli.py`: `_adversarial_bindings`, `command_run_adversarial_oracles`, `validate_adversarial_report`, `command_validate_adversarial_report`, and the `validate-adversarial-report` parser branch.
    - `experiments/specchoice-v1.3.2/src/specchoice_evidence/cli.py`: exact controller analog; reuse its `argparse` `Path` arguments, `set_defaults(handler=...)`, stable stderr error reporting, and return-code handling.
    - `src/specchoice_measurement/attempts.py`: `load_measurement_attempt_manifest` and `validate_measurement_attempt`; reuse them without modification.
    - `src/specchoice_measurement/h1.py`: `_expected_bindings`, whose `formal_attempt` argument is already the correct upstream path.
    - `tests/test_measurement_attempts.py`: `test_adversarial_cli_is_diagnostic_only_and_matches_every_frozen_oracle` and its temporary formal/report/attempt-tree helpers.
    - `experiments/specchoice-v1.3.2/tests/test_fixture_closure.py`: test analog; reuse its `unittest.TestCase`/`unittest.mock` adversarial-test structure and stable-code assertions.
    - Read-only identities in `runs/measurement-attempts/formal-golden-pr2164-v1/attempt.json` and `reports/h1/adversarial-oracle-results-v2.json`; both currently bind attempt digest `c81649ae4aaa4c29be289af1855934f66c907f8a0bf0a2e6c2ce2407bd3da756`.
  </read_first>
  <behavior>
    - "RED first: copy the canonical v2 report and its diagnostic-only attempt tree, change only bindings.formal_attempt_sha256 to 64 zeroes, and assert ADVERSARIAL_REPORT_INVALID; the current validator incorrectly returns success."
    - "The repaired validator validates the supplied formal attempt through validate_measurement_attempt, requires exactly role=formal/status=completed, and constructs expected bindings from only the returned verified attempt_sha256."
    - "A missing, invalid, diagnostic_only, completed_with_warnings, or digest-mismatched formal attempt rejects with ADVERSARIAL_REPORT_INVALID at the standalone boundary."
    - "The run-adversarial post-write self-check, standalone CLI, direct Python call sites, and H1 _expected_bindings all pass the exact formal-attempt path; no caller can substitute the report's declared digest as expected data."
    - "The existing report JSON shape, formal/adversarial attempt bytes, H1 v2 packet/Markdown, human decision state, and external_publication_authorized=false remain unchanged per D-11 through D-16."
  </behavior>
  <action>Add `test_adversarial_report_rejects_forged_formal_attempt_binding` before production changes, following `experiments/specchoice-v1.3.2/tests/test_fixture_closure.py`'s `unittest.TestCase`/`unittest.mock` adversarial-test structure and stable-code assertions. Its first RED form must use the current validator interface so the only failure is that no `ADVERSARIAL_REPORT_INVALID` is raised after the digest-only mutation; record that failing targeted command. Then change `validate_adversarial_report` to accept an explicit `formal_attempt: Path`, call the existing `validate_measurement_attempt(attempt_root=formal_attempt)` before binding comparison, require its returned role/status to be `formal`/`completed`, and pass its returned `attempt_sha256` into `_adversarial_bindings`. Translate attempt/path failures to `ADVERSARIAL_REPORT_INVALID`; never derive expected lineage from `report["bindings"]`. Add required `--formal-attempt` to only the `validate-adversarial-report` CLI contract, reusing `experiments/specchoice-v1.3.2/src/specchoice_evidence/cli.py`'s `argparse`/error-handling convention of typed `Path` arguments, `set_defaults(handler=...)`, stable stderr errors, and return code `2`; pass it through `command_validate_adversarial_report`, pass `args.formal_attempt` from `command_run_adversarial_oracles` into its post-write self-check, and pass `_expected_bindings`' existing `formal_attempt` argument from H1. Update every existing direct test call with its corresponding temporary formal attempt. Per D-11, D-12, D-15, and D-16, reuse current canonical hashing/replay validation and keep all persisted report/schema/evidence bytes untouched; H1's later comparison remains defense in depth rather than a substitute for standalone validation.</action>
  <acceptance_criteria>
    - The named regression fails on pre-fix HEAD because a report whose only changed value is `bindings.formal_attempt_sha256` validates; after implementation it passes with `ADVERSARIAL_REPORT_INVALID`.
    - `validate-adversarial-report` requires `--formal-attempt`; the function, generation self-check, CLI handler, existing attempt tests, and H1 call site all use the same explicit path.
    - Only a verified `formal/completed` attempt whose recomputed `attempt_sha256` equals the report binding is accepted; every other role/status/digest or invalid attempt rejects.
    - Current formal attempt, v2 adversarial report, all 12 diagnostic-only attempts, H1 v2 JSON/Markdown, and local-only publication boundary validate without regeneration or byte changes.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_attempts.MeasurementAttemptTests.test_adversarial_report_rejects_forged_formal_attempt_binding -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_attempts tests.test_measurement_h1 -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-attempt --attempt runs/measurement-attempts/formal-golden-pr2164-v1 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-adversarial-report --report reports/h1/adversarial-oracle-results-v2.json --formal-attempt runs/measurement-attempts/formal-golden-pr2164-v1 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-h1-packet --packet reports/h1/h1-source-gold-review-v2/h1-source-gold-review-v2.json --markdown reports/h1/h1-source-gold-review-v2/h1-source-gold-review-v2.md</automated>
  </verify>
  <done>Standalone adversarial validation is independently rooted in verified formal-attempt bytes, the digest-only forgery is rejected, every in-repository call site supplies the real attempt, and current local-only H1 evidence remains valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-07-02: Preserve source/gold conflict provenance through the public adapter builder</name>
  <precondition>The active Phase 2 source-authority validator exits zero for exactly 11 fixtures and 28 raw files before any adapter test mutation is introduced.</precondition>
  <files>experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/domain.py, experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py</files>
  <read_first>
    - `src/specchoice_measurement/adapter.py`: `AdapterError`, `_raw_identity`, `_record_from_fixture`, `_invalid_batch`, and `build_pr2164_adapter_batch`.
    - `experiments/specchoice-v1.3.2/src/specchoice_evidence/verify.py`: exact file-I/O analog; reuse its authority-first validation and declared-path inspection before reading or interpreting source bytes.
    - `src/specchoice_measurement/domain.py`: existing `Diagnostic`, `RawFileIdentity`, and `AdapterBatch.from_parts` canonical ordering/serialization.
    - `tests/test_measurement_adapter.py`: `setUp`, `build`, accepted-v2 partition test, CLI subprocess tests, and authoritative-byte preservation test.
    - `experiments/specchoice-v1.3.2/tests/test_fixture_closure.py`: test analog; reuse its `unittest.TestCase`/`unittest.mock` adversarial-test structure and complete stable-code assertions.
    - Read-only `fixture-registry-pr2164-v1.json` entry for `POS_CSR_RW_MTVEC_ACCESS`, whose relevant roles are `fixture_expected` and `fixture_gold` and whose raw hashes already enter `RawFileIdentity`.
  </read_first>
  <behavior>
    - "RED first: the public build_pr2164_adapter_batch path injects one parsed gold-name disagreement after real authority/raw-identity verification and expects valid=false, records=[], unchanged verified source_identity, and one exact GOLD_NAME_MISMATCH diagnostic; current code returns empty source_identity and code/severity only."
    - "The exact conflict diagnostic identifies POS_CSR_RW_MTVEC_ACCESS, field=gold_name, the expected expected.yaml gold_name, the observed gold.yaml name, severity=blocker, and a deterministic source_hashes mapping for fixture_expected and fixture_gold."
    - "AdapterError carries the existing domain Diagnostic for record-level conflicts; _invalid_batch and AdapterBatch.from_parts serialize/order that same type without a second diagnostic class or custody path."
    - "All adapter conflicts remain fail closed with records=[]; failures before authority verification may retain empty source identity, while conflicts after verification retain the verified source identity."
    - "The two CLI subprocess tests invoke [sys.executable, '-m', ...] while retaining the same arguments, PYTHONPATH, expected output, and overwrite/symlink assertions."
  </behavior>
  <action>Add `test_public_builder_preserves_conflict_provenance` first, reusing `experiments/specchoice-v1.3.2/tests/test_fixture_closure.py`'s `unittest.TestCase`/`unittest.mock` adversarial-test structure and complete stable-code assertions. Drive the public builder through the real authority, accepted bundle, registry, and raw identities, then use `unittest.mock.patch` only at the bounded parsed gold-value seam for `POS_CSR_RW_MTVEC_ACCESS`; derive the expected `fixture_expected`/`fixture_gold` hashes from the valid record rather than hardcoding them. Assert the complete `Diagnostic.as_dict()`, unchanged `source_identity`, and `records == ()`; run it RED before implementation. For adapter authority/path handling, reuse `experiments/specchoice-v1.3.2/src/specchoice_evidence/verify.py`'s convention: validate accepted authority/bundle identity first, inspect every declared source path through the existing filesystem policy before reading it, then preserve raw bytes and hashes before interpretation. Extend the existing `domain.Diagnostic` with one optional deterministic `source_hashes` mapping and let its current `as_dict`/canonical serialization carry it. Give `AdapterError` an optional typed Diagnostic payload, construct `GOLD_NAME_MISMATCH` inside `_record_from_fixture` with fixture ID, `field="gold_name"`, direct expected/observed values, and both role-keyed raw hashes, and teach `_invalid_batch` to accept that Diagnostic instead of rebuilding code-only data. Initialize and retain verified `source_identity` across the builder's exception boundary; preserve `{}` only for failures that occur before verification. Per D-01 through D-04, continue returning zero records for every blocker, keep the valid 11/28 batch and score-bearing allowlist unchanged, and reuse RawFileIdentity/canonical ordering rather than adding a new diagnostic or custody abstraction. In the same test file only, import `sys` and replace both literal child interpreter entries with `sys.executable`; do not alter production interpreter selection or broaden WR-01 beyond those two calls.</action>
  <acceptance_criteria>
    - The named public-builder regression fails on pre-fix HEAD because verified source identity and all conflict fields/hashes are absent; after implementation its exact structured assertion passes.
    - The conflict result remains invalid with zero records and contains the verified six-field source identity plus one stable GOLD_NAME_MISMATCH diagnostic with fixture ID, field, direct expected/observed values, and both relevant raw hashes.
    - No second Diagnostic class, authority verifier, bundle reader, canonical hashing scheme, parser, dependency, or persisted adapter/evidence artifact is created; the valid accepted-v2 batch remains 11 fixtures/28 raw files.
    - Both adapter CLI subprocess tests use `sys.executable` and preserve their existing behavioral assertions.
    - After exactly two new test methods across this plan, the focused six-module partition is 61/61, discovery is 132 methods, the five named live-boundary methods retain the exact expected-red signature, and the other 127 are green.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter.MeasurementAdapterTests.test_public_builder_preserves_conflict_provenance -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter tests.test_measurement_parsing tests.test_measurement_scoring tests.test_measurement_attempts tests.test_measurement_h1 tests.test_filesystem_boundary -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2 | python3 -c 'import json,sys; value=json.load(sys.stdin); assert value["status"] == "valid" and value["fixture_count"] == 11 and value["raw_file_count"] == 28' &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -c '
import contextlib
import io
import unittest

expected_red = {
    "tests.test_fixture_closure.FixtureClosureCandidateTests.test_fixture_accept_publish_rejects_racing_empty_target_without_overwrite",
    "tests.test_fixture_closure.FixtureClosureCandidateTests.test_fixture_publish_fails_closed_when_no_replace_primitive_is_unavailable",
    "tests.test_fixture_closure.FixtureClosureCandidateTests.test_public_fixture_acceptance_rejects_missing_mismatched_and_stale_authority",
    "tests.test_fixture_closure.FixtureClosureCandidateTests.test_public_fixture_acceptance_resolves_its_own_current_v7_basis",
    "tests.test_receipts.IntegrityReceiptTests.test_active_defaults_are_v7_from_experiment_and_repository_roots",
}

def flatten(suite):
    for item in suite:
        if isinstance(item, unittest.TestSuite):
            yield from flatten(item)
        else:
            yield item

discovered = list(flatten(unittest.defaultTestLoader.discover("tests")))
by_id = {test.id(): test for test in discovered}
assert len(discovered) == 132, len(discovered)
assert expected_red.issubset(by_id)
green = [test for test in discovered if test.id() not in expected_red]
assert len(green) == 127, len(green)
with contextlib.redirect_stdout(io.StringIO()), contextlib.redirect_stderr(io.StringIO()):
    green_result = unittest.TextTestRunner(stream=io.StringIO(), verbosity=0).run(unittest.TestSuite(green))
assert green_result.wasSuccessful() and green_result.testsRun == 127
red_suite = unittest.TestSuite(unittest.defaultTestLoader.loadTestsFromName(test_id) for test_id in sorted(expected_red))
with contextlib.redirect_stdout(io.StringIO()), contextlib.redirect_stderr(io.StringIO()):
    red_result = unittest.TextTestRunner(stream=io.StringIO(), verbosity=0).run(red_suite)
observed_red = {test.id() for test, _ in [*red_result.failures, *red_result.errors]}
assert red_result.testsRun == 5 and len(red_result.failures) == 5 and len(red_result.errors) == 1
assert observed_red == expected_red
' </automated>
  </verify>
  <done>The public adapter builder remains fail closed while exposing complete reproducible conflict provenance, test subprocesses use the active interpreter, and all Phase 2/authority/H1/discovery partitions retain their declared semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| adversarial report → formal attempt | An untrusted report binding crosses into evidence authority only after the independently supplied attempt validates and yields its own digest. |
| verified source bytes → invalid adapter batch | A source/gold disagreement must retain enough verified identity and raw-hash provenance for a reviewer to reproduce the rejection while exposing no score-eligible records. |
| gap repair → H1/later phases | Internal validation fixes may restore trust but cannot create human approval, model/remote capability, publication authority, or new evidence bytes. |

## STRIDE Threat Register — ASVS L1, blocking threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-07-01 | Tampering / Spoofing | `validate_adversarial_report` formal lineage | high | mitigate | Task 02-07-01 validates the supplied attempt through the existing replay validator, requires formal/completed, derives the expected digest from verified attempt bytes, and covers digest-only forgery fail-first. |
| T-02-07-02 | Repudiation / Tampering | adapter source/gold conflict path | high | mitigate | Task 02-07-02 carries one existing typed Diagnostic with fixture/field/value/raw-hash provenance and retains verified source identity while keeping records empty. |
| T-02-07-03 | Elevation of Privilege | repaired validation status → H1/model/publication authority | high | mitigate | Both tasks preserve D-13 through D-16, human-only local H1, external_publication_authorized=false, unchanged evidence bytes, and no model/API/remote command or authority field. |
| T-02-07-04 | Tampering | Phase 1 expected-red classification | medium | mitigate | Task 02-07-02 discovers 132 methods, asserts the exact five-method red identity and 5-failure/1-error signature, and requires all other 127 methods green without modifying Phase 1 tests. |
| T-02-07-SC | Tampering | npm/pip/cargo supply chain | low | accept | The plan installs no package, changes no dependency/lockfile, and uses only Python stdlib plus existing repository modules. |

Every high threat is mitigated by a named fail-first regression and artifact/authority validator. Any high-threat regression failure blocks completion.
</threat_model>

<verification>
- Observe and record RED for exactly the two new targeted methods before production edits, then run each targeted method GREEN after its owning repair.
- Run the six-module Phase 2 plus filesystem suite and require exactly 61/61 after the two new test methods.
- Validate source authority as status=valid with fixture_count=11 and raw_file_count=28.
- Validate the formal attempt, v2 adversarial report with explicit formal-attempt input, and current H1 v2 JSON/Markdown without running any generator or writer command.
- Run the embedded discovery partition: exactly 132 discovered; the five named Phase 1 methods yield 5 failures plus 1 error; all other 127 tests pass.
- Review both task commits against `files_modified`; no accepted/source-authority/formal/adversarial/H1/decision/Phase 1/core-UDB artifact may appear in a commit.
</verification>

<success_criteria>
- CR-01 is closed by verified formal-attempt lineage rather than self-reference, and its digest-only tamper regression passes.
- CR-02 is closed by a zero-record invalid batch that preserves verified source identity and complete fixture/field/value/raw-hash diagnostics, and its public-builder regression passes.
- WR-01 is closed only in the two adjacent adapter-test subprocess calls through `sys.executable`.
- TS-03, TS-04, TS-05 and D-01 through D-16 remain satisfied under the 61-test focused gate, exact 11/28 authority gate, H1 v2 validation, and 127-test green discovery partition.
- The exact five Phase 1 expected-red method identities and aggregate 5-failure/1-error signature remain unchanged.
- The seven probe assumptions and two value/safety prohibitions remain explicit; no new dependency, schema, evidence, approval, model/remote, publication, or core-UDB scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-measurement-spine/02-07-SUMMARY.md` when done. Record both RED→GREEN regressions, the exact modified symbols/CLI flag, 61/61 focused result, 11/28 source-authority result, 132 = 5 expected-red + 127 green discovery classification, H1 v2 validation, unchanged frozen-evidence boundary, and no external/publication authority.
</output>
