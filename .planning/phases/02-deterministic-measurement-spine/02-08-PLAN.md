---
phase: 02-deterministic-measurement-spine
plan: 08
type: execute
wave: 8
depends_on: [02-07]
files_modified:
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_parsing.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_h1.py
autonomous: true
gap_closure: true
requirements: [TS-03, TS-04, TS-05]
must_haves:
  truths:
    - "Per D-01 and D-02, every score-bearing adapter raw leaf is inspected and consumed through one read_authoritative_file descriptor; a final-component symlink swap or FIFO yields a stable invalid zero-record batch without consuming external bytes or blocking."
    - "Per D-06, D-08, and D-09, every preflight fixture source is consumed only from read_authoritative_file returned bytes; a swapped or special leaf yields deterministic blocking evidence diagnostics and no parsed score-bearing result."
    - "Per D-15 and D-16, H1 packet, canonical supporting values, canonical adjudication schema, H1 review schema, Markdown projection, and decision leaves are hashed, parsed, or compared only from read_authoritative_file returned bytes; substituted leaves fail closed before any H1 use."
    - "All existing byte-length, SHA-256, canonical-JSON, exact Markdown, evidence-span, identity, and binding comparisons remain in force after the descriptor-bound read replacement."
    - "The active source authority remains valid with exactly 11 fixtures and 28 raw files, and the stored formal attempt, explicit-formal adversarial report, and H1 packet/Markdown validate without regeneration."
    - "H1 remains independently human-authored and local-only, external_publication_authorized remains false, and this repair creates no Phase 3, model, remote, API, or publication authority."
    - "Exactly three public-boundary regression methods are added, growing the six-module focused suite from 61 to 64 passing tests and discovery from 132 to 135 methods."
    - "Runtime-discovered test IDs partition the 135 methods into exactly 130 green tests plus the unchanged five Phase 1 expected-red methods, whose aggregate remains five failures and one error without any 'tests.' prefix assumption."
  artifacts:
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py"
      provides: "Descriptor-bound adapter raw-leaf consumption through the existing Phase 1 helper"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py"
      provides: "Descriptor-bound accepted fixture-source bytes for evidence-span validation"
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py"
      provides: "Descriptor-bound canonical, supporting-schema, Markdown, packet, and decision reads for H1 validation"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py"
      provides: "Public adapter raw-leaf symlink-swap and FIFO fail-closed regression"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_parsing.py"
      provides: "Public preflight fixture-source symlink-swap and FIFO fail-closed regression"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_h1.py"
      provides: "One public H1 packet, Markdown, decision, canonical-schema, and H1-schema symlink-swap/FIFO fail-closed regression"
  key_links:
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py::_raw_identity"
      to: "experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py::read_authoritative_file"
      via: "one no-follow descriptor returns both FileEvidence and the only bytes parsed by the bounded adapter"
      pattern: "read_authoritative_file.*bundle_root"
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py::_source_bytes_by_fixture"
      to: "experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py::read_authoritative_file"
      via: "one descriptor supplies the fixture-owned byte map used by exact evidence-span validation"
      pattern: "read_authoritative_file.*root"
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py::_read_canonical_value, _expected_bindings, validate_h1_packet"
      to: "experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py::validate_h1_packet"
      via: "descriptor-returned canonical bytes feed packet/decision/evidence parsing, one descriptor-returned _SCHEMA byte string feeds all three schema hash uses, descriptor-returned _H1_SCHEMA bytes feed its binding, and descriptor-returned Markdown bytes feed exact projection comparison"
      pattern: "read_authoritative_file.*_ROOT"
  assumptions:
    - "flagged assumption — TS-03 adjacency remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-03 empty/null/single-element remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-03 stable ordering on equal elements remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-04 remains unclassified/manual review in the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-05 adjacency remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-05 empty/null/single-element remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
    - "flagged assumption — TS-05 stable ordering on equal elements remains unresolved by the spec-less probe; this repair neither classifies nor drops it."
  prohibitions:
    - "flagged-unverified: The repair must not regenerate or mutate accepted bundle bytes, source-authority bytes, formal/adversarial/H1 evidence, human decisions, Phase 1 custody, or core UDB schemas/generated data."
    - "flagged-unverified: The repair must not create or expand H1 approval, Phase 3 authority, model/API/remote capability, or publication authority; external_publication_authorized remains false."
---

<objective>
**As a** human RISC-V reviewer, **I want to** trust the experiment's adjudication semantics and diagnostics, **so that** I can review the measurement spine before any frame, retrieval, or model result is considered.

Close the remaining TS-03/TS-04/TS-05 custody gap by routing adapter, preflight, and every H1 authoritative regular-file consumer, including both supporting schemas, through the existing descriptor-bound Phase 1 reader, with fail-first public-boundary regressions for final-component symlink swaps and nonblocking FIFO rejection.

Decision continuity: D-01: canonical adapter records retain exact accepted raw identities; D-02: source disagreements remain blocking with zero records; D-03: the score-bearing allowlist is unchanged; D-04: adapter version/rule identity remains immutable and unmixed; D-05: compatibility remains confined to the named legacy ingress; D-06: score-bearing objects remain closed; D-07: the explicit no-finding form remains unique; D-08: evidence remains exact raw bytes before projection; D-09: complete preflight remains the scoring gate; D-10: accepted-name absence remains warning-only; D-11: attempts remain immutable and authority-gated; D-12: diagnostic-only and formal evidence remain separate; D-13: H1 remains a human-only three-state decision; D-14: every fixture semantic remains individually reviewed; D-15: formal evidence remains clean and adversarial diagnostics remain exact; D-16: H1 bindings remain immutable, local-only, and non-public.

Purpose: Make the checked authoritative leaf and the consumed bytes one inseparable trust decision while preserving every existing identity, hash, canonicalization, diagnostic, H1, and local-only authority contract.
Output: Six narrowly modified source/test files, three new public-boundary regression methods, no new production symbol or dependency, and no regenerated evidence.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-measurement-spine/02-CONTEXT.md
@.planning/phases/02-deterministic-measurement-spine/02-RESEARCH.md
@.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md
@.planning/phases/02-deterministic-measurement-spine/02-VALIDATION.md
@.planning/phases/02-deterministic-measurement-spine/02-VERIFICATION.md
@.planning/phases/02-deterministic-measurement-spine/02-REVIEW.md
@.planning/phases/02-deterministic-measurement-spine/02-07-SUMMARY.md

<interfaces>
Existing Phase 1 interface to reuse exactly:

```python
@dataclass(frozen=True)
class FileEvidence:
    path: str
    file_kind: str
    byte_length: int | None = None
    sha256: str | None = None
    hardlink_count: int | None = None

def read_authoritative_file(root: Path, relative_path: str) -> tuple[FileEvidence, bytes]: ...
```

Existing consumer interfaces whose public behavior must remain stable:

```python
def build_pr2164_adapter_batch(*, authority_path: Path, bundle_root: Path, rules_path: Path) -> AdapterBatch: ...
def preflight_prediction_batch(*, raw: bytes, adapter_batch: object, ingress: str) -> PreflightResult: ...
def validate_h1_packet(*, packet: Path, markdown: Path) -> dict[str, Any]: ...
def validate_h1_decision(*, packet: Path, decision: Path) -> dict[str, Any]: ...
```
</interfaces>
</context>

## Artifacts this phase produces

- New production modules, classes, functions, schemas, CLI flags, dependencies, accepted bytes, source-authority records, attempts, reports, packets, decisions, hashes, baselines, allowlists, restart artifacts, core UDB data, and generated UDB data: none.
- Modified production symbol in `adapter.py`: `_raw_identity` obtains `FileEvidence` and raw bytes from `read_authoritative_file` and retains the declared length/SHA-256 checks.
- Modified production symbol in `preflight.py`: `_source_bytes_by_fixture` obtains the fixture-owned raw bytes from `read_authoritative_file` and retains the adapter-declared SHA-256 comparison.
- Modified production symbols in `h1.py`: `_read_canonical_value`, `_expected_bindings`, and the Markdown branch of `validate_h1_packet` consume only bytes returned by `read_authoritative_file`; `_expected_bindings` replaces its three `_SCHEMA.read_bytes()` uses and one `_H1_SCHEMA.read_bytes()` use with hashes derived from two descriptor-returned byte strings, and `validate_h1_decision` inherits the canonical reader without a second path.
- New test `MeasurementAdapterTests.test_public_builder_rejects_swapped_raw_leaf_and_fifo_without_consuming_or_blocking`.
- New test `MeasurementParsingTests.test_public_preflight_rejects_swapped_fixture_source_and_fifo_without_consuming_or_blocking`.
- New test `H1PacketTests.test_public_h1_validators_reject_swapped_packet_markdown_and_decision_leaves_and_fifos_without_consuming_or_blocking`, whose single parameterized method also covers `_SCHEMA` and `_H1_SCHEMA` swaps/FIFOs.
- Disposable temporary files created inside tests are test fixtures only; no repository evidence writer or regeneration command is run.

## Assumption Delta

`assumption_delta_detected: false`. The gap adds no new implementation assumption. The seven spec-less probe fallbacks remain explicit in `must_haves.assumptions` and are neither promoted to evidence nor silently dismissed.

## Probe Disposition

- TS-03 adjacency remains unresolved.
- TS-03 empty/null/single-element remains unresolved.
- TS-03 stable ordering on equal elements remains unresolved.
- TS-04 remains unclassified/manual review.
- TS-05 adjacency remains unresolved.
- TS-05 empty/null/single-element remains unresolved.
- TS-05 stable ordering on equal elements remains unresolved.

Exactly seven unresolved items enter and exactly seven remain flagged. None changes the descriptor-bound custody repair or its acceptance gates.

## Prohibition Recall

The two existing descriptor-less value/safety prohibitions are retained verbatim in frontmatter as `flagged-unverified`. The gap adds no generic OWASP prohibition and does not resolve either prohibition by assertion. Concrete TOCTOU, FIFO, evidence-mutation, authority-expansion, expected-red, dependency, and core-UDB risks are routed to task behavior, automated gates, and the ASVS L1 threat register below.

## Reversibility Assessment

Both implementation choices are reversible local call-site substitutions behind an existing stable helper; they create no format, schema, public API, persisted-data, or external-service migration. Reversibility gates are enabled, and no one-way or costly decision requires a checkpoint.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Reviewer trusts deterministic adjudication semantics and diagnostics before frame, retrieval, or model results | Tasks 02-08-01 and 02-08-02 | COVERED |
| REQ | TS-03 | Accepted-source adapter and deterministic all-11 measurement remain custody-bound | Task 02-08-01 plus 64-test gate | COVERED |
| REQ | TS-04 | Closed canonical adjudication and no silent repair remain unchanged | Task 02-08-01 plus 64-test gate | COVERED |
| REQ | TS-05 | Stable diagnostics and trustworthy H1/evidence provenance consume only checked bytes | Both tasks | COVERED |
| RESEARCH | — | Reuse Python stdlib and the existing Phase 1 filesystem helper; add no package or parallel abstraction | Both tasks | COVERED |
| RESEARCH | — | Preserve active accepted generation, exactly 11 fixtures/28 raw files, and raw-byte-before-interpretation custody | Task 02-08-01 | COVERED |
| RESEARCH | — | Canonical JSON is H1 authority, both supporting schemas are binding inputs, and Markdown is a pure deterministic projection | Task 02-08-02 | COVERED |
| RESEARCH | — | Focused Phase 2 gate must not weaken Phase 1 live-boundary evidence | Task 02-08-02 discovery partition | COVERED |
| CONTEXT | D-01, D-02 | Canonical adapter lineage and conflict blocking use the exact accepted raw bytes | Task 02-08-01 | COVERED |
| CONTEXT | D-03, D-04 | Score-bearing allowlist and immutable unmixed adapter identity remain unchanged | 64-test focused gate | COVERED |
| CONTEXT | D-05, D-07 | Named legacy ingress and sole explicit no-finding form remain unchanged | 64-test focused gate | COVERED |
| CONTEXT | D-06, D-08 | Closed score-bearing objects and exact unnormalized evidence use descriptor-returned bytes | Task 02-08-01 | COVERED |
| CONTEXT | D-09, D-10 | Complete preflight and warning-only identity separation remain unchanged | Task 02-08-01 plus focused gate | COVERED |
| CONTEXT | D-11, D-12 | Immutable formal and diagnostic-only role separation remains unchanged | Stored artifact validators | COVERED |
| CONTEXT | D-13, D-14 | Human-only three-state H1 and signed all-11 semantics remain unchanged | Task 02-08-02 | COVERED |
| CONTEXT | D-15, D-16 | Clean formal/adversarial evidence, descriptor-bound schema/evidence/H1 leaves, immutable H1 bindings, local-only authority, and no publication | Task 02-08-02 plus stored artifact validators | COVERED |
| VERIFICATION | remaining gap | Adapter, preflight, and H1 inspect a leaf and then reopen it by pathname | Both tasks | COVERED |
| REVIEW | CR-01 | H1 and evidence-span readers reopen checked leaves; adapter is included by verifier evidence | Both tasks | COVERED |

All actionable gap sources are covered. CONTEXT has no deferred Phase 2 idea, and no later-phase, external integration, schema, UI, model, remote, publication, or optional discovery item enters this plan.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-08-01: Bind accepted adapter and preflight source bytes to one checked descriptor</name>
  <precondition>The active Phase 2 source-authority validator exits zero for exactly 11 fixtures and 28 raw files, and the six-module focused suite is 61/61 before new tests are added.</precondition>
  <files>experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py, experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py, experiments/specchoice-v1.3.2/tests/test_measurement_parsing.py</files>
  <read_first>
    - `experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py`: `read_authoritative_file`, `_read_authoritative_regular_file`, and `FileEvidence`; reuse the one `O_NOFOLLOW` descriptor result without changing this Phase 1 module.
    - `experiments/specchoice-v1.3.2/src/specchoice_measurement/adapter.py`: `_raw_identity` and `build_pr2164_adapter_batch`; preserve public invalid-batch translation, zero records, and raw identity checks.
    - `experiments/specchoice-v1.3.2/src/specchoice_measurement/preflight.py`: `_source_bytes_by_fixture` and `preflight_prediction_batch`; preserve complete diagnostic collection and parsed-result withholding on blockers.
    - `.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md`: the adapter authority-read assignment and Shared Patterns / Filesystem/path safety rule; update the old inspect-then-read analog by applying the already hardened helper.
    - `experiments/specchoice-v1.3.2/tests/test_filesystem_boundary.py`: `test_descriptor_backed_read_requires_no_follow_support`, `test_escape_and_symlink_fail_before_open`, and `test_fifo_and_unproven_mount_boundary_fail_closed`; reuse their stdlib `unittest`, temporary-root, `unittest.mock.patch`, stable-code, and no-follow/FIFO patterns.
    - `experiments/specchoice-v1.3.2/tests/test_measurement_adapter.py`: `setUp`, `build`, accepted 11/28 partition, and `test_public_builder_preserves_conflict_provenance`; exercise the public builder rather than `_raw_identity` directly.
    - `experiments/specchoice-v1.3.2/tests/test_measurement_parsing.py`: `payload`, `preflight`, exact evidence-span tests, and fixture-ownership diagnostic; exercise `preflight_prediction_batch` rather than the private source-map helper directly.
  </read_first>
  <behavior>
    - "RED first: test_public_builder_rejects_swapped_raw_leaf_and_fifo_without_consuming_or_blocking drives build_pr2164_adapter_batch while a deterministic helper seam swaps one declared raw leaf to an external symlink immediately before the helper's final-component os.open; pre-fix code does not use that helper seam and incorrectly remains eligible."
    - "The adapter symlink and FIFO subtests return valid=false, records=(), and RAW_PATH_OR_IDENTITY_INVALID; the external sentinel never appears in returned records or diagnostics, and the FIFO path is rejected by lstat before any target os.open can block."
    - "RED first: test_public_preflight_rejects_swapped_fixture_source_and_fifo_without_consuming_or_blocking drives preflight_prediction_batch with a valid all-11 payload while the same deterministic seam targets one fixture_source; pre-fix code bypasses the seam and incorrectly remains valid."
    - "The preflight symlink and FIFO subtests return invalid_preflight with EVIDENCE_SOURCE_NOT_DECLARED_FOR_FIXTURE, parsed_predictions=(), and no external sentinel bytes; FIFO rejection occurs before any target open."
    - "Temporary mirrors and external sentinels are disposable test inputs; no active accepted leaf, source-authority file, formal/adversarial/H1 artifact, human decision, baseline, allowlist, restart record, spec file, or generated UDB file is written."
    - "Per D-01, D-02, D-06, D-08, and D-09, only FileEvidence and bytes returned together by read_authoritative_file enter identity checks, bounded parsing, fixture ownership, or evidence validation."
  </behavior>
  <action>Create the two named public-boundary test methods before production changes. Use a temporary regular leaf and an external sentinel outside its authoritative root. At the module-imported helper seam, delegate to the real `read_authoritative_file`; for the symlink subtest, patch `specchoice_evidence.filesystem.os.open` only for the target final component so the hook atomically replaces the temporary regular leaf with a symlink immediately before delegating to the real open, then assert the public adapter/preflight stable failure and absence of sentinel bytes. For the FIFO subtest, replace the temporary target with `os.mkfifo` before invoking the real helper and install a target-specific open trap that fails the test if reached; successful stable rejection therefore proves no blocking FIFO open occurred. Use `mock.patch(..., create=True)` at the consumer-module helper name so each test runs RED against the current code path that has not imported the helper. Delegate every non-target read to the real helper, restore only disposable temporary paths, and do not rename or write any repository authority/evidence leaf. Then replace `_raw_identity`'s inspection plus later pathname read with one `read_authoritative_file(bundle_root, path)` call. Parse only its returned bytes, compare its returned FileEvidence and returned bytes to the declared length and SHA-256, retain `RAW_PATH_OR_IDENTITY_INVALID`, `RAW_IDENTITY_MISMATCH`, and `RAW_IDENTITY_CHANGED_DURING_READ`, and translate helper `OSError` failures to the existing stable invalid-batch boundary. Replace `_source_bytes_by_fixture`'s inspection plus later pathname read with one helper call and use only its returned bytes after the existing adapter-declared SHA-256 check. D-01: retain exact canonical adapter raw identity. D-02: preserve stable blocking zero-record batches. D-06: keep score-bearing objects closed. D-08: use exact unnormalized descriptor-returned evidence bytes. D-09: keep complete preflight as the sole scoring gate. Remove only imports made obsolete by these substitutions; add no reader, wrapper, retry, fallback, dependency, schema, or evidence mutation.</action>
  <acceptance_criteria>
    - Both named tests fail on pre-fix HEAD because the public consumers bypass the patched descriptor-bound helper seam, then pass after the call-site substitutions.
    - Adapter raw-leaf symlink/FIFO cases return one stable invalid zero-record batch; preflight fixture-source cases return deterministic blocking evidence diagnostics and withhold parsed predictions.
    - The symlink hook runs immediately before the target final-component `os.open`, and external sentinel bytes are never parsed, hashed as accepted input, returned, or included in score-bearing output.
    - The FIFO subtest proves the target open trap is never called, so special-file rejection cannot block waiting for a writer.
    - Successful paths retain the exact active 11-fixture/28-raw-file adapter batch, byte-length/SHA-256 comparisons, evidence-span behavior, and diagnostic ordering.
    - No Phase 1 file, accepted/source-authority/evidence/decision byte, baseline, allowlist, restart record, core UDB schema/data, generated data, dependency, or external capability changes.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter.MeasurementAdapterTests.test_public_builder_rejects_swapped_raw_leaf_and_fifo_without_consuming_or_blocking tests.test_measurement_parsing.MeasurementParsingTests.test_public_preflight_rejects_swapped_fixture_source_and_fifo_without_consuming_or_blocking -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter tests.test_measurement_parsing tests.test_filesystem_boundary -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2 | python3 -c 'import json,sys; value=json.load(sys.stdin); assert value["status"] == "valid" and value["fixture_count"] == 11 and value["raw_file_count"] == 28'</automated>
  </verify>
  <done>The accepted adapter and preflight paths consume only descriptor-bound returned bytes, deterministic symlink/FIFO regressions fail closed without external-byte use or blocking, and the valid 11/28 measurement path remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-08-02: Bind every H1 evidence, schema, packet, Markdown, and decision leaf to one checked descriptor</name>
  <precondition>The stored formal attempt, explicit-formal adversarial report, and current H1 packet/Markdown validate without regeneration, and external_publication_authorized is false.</precondition>
  <files>experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py, experiments/specchoice-v1.3.2/tests/test_measurement_h1.py</files>
  <read_first>
    - `experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py`: `read_authoritative_file`, `_read_authoritative_regular_file`, and stable symlink/special-file behavior.
    - `experiments/specchoice-v1.3.2/src/specchoice_measurement/h1.py`: `_relative`, `_read_canonical_value`, `_read_canonical`, `_expected_bindings`, `validate_h1_packet`, and `validate_h1_decision`; enumerate every active pathname read before editing. `_read_canonical_value` serves packet, decision, formal-attempt, formal-diagnostics, and adversarial-report JSON; `validate_h1_packet` directly reads Markdown; `_expected_bindings` directly calls `_SCHEMA.read_bytes()` three times and `_H1_SCHEMA.read_bytes()` once. `_publish_packet_pair` only inspects the output parent directory and remains on `inspect_authoritative_path`; it is not an authoritative leaf-byte consumer. Preserve canonical JSON, exact Markdown, packet/schema/decision bindings, and manual-authorization errors while eliminating every authoritative `Path.read_bytes()` consumer.
    - `.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md`: `h1.py` canonical-JSON-authority/Markdown-projection assignment plus Shared Patterns / Filesystem/path safety.
    - `experiments/specchoice-v1.3.2/tests/test_filesystem_boundary.py`: the descriptor-backed read, symlink, and FIFO analogs named in Task 02-08-01.
    - `experiments/specchoice-v1.3.2/tests/test_measurement_h1.py`: `_build`, `_decision`, packet/Markdown mutation rejection, and existing human-decision validation; extend the public validators without creating a second H1 fixture system.
    - Read-only stored inputs: `runs/measurement-attempts/formal-golden-pr2164-v1`, `reports/h1/adversarial-oracle-results-v2.json`, and `reports/h1/h1-source-gold-review-v2/`; validate them but do not invoke writers.
  </read_first>
  <behavior>
    - "RED first: one new test method parameterizes packet JSON, Markdown, decision, _SCHEMA, and _H1_SCHEMA leaves through validate_h1_packet or validate_h1_decision, and uses the same immediate-before-os.open symlink swap plus pre-open FIFO trap; every current pathname read bypasses the helper seam."
    - "A swapped packet reports H1_PACKET_INVALID, swapped Markdown reports H1_MARKDOWN_INVALID, and swapped decision reports H1_DECISION_INVALID before external bytes can influence canonical parsing, projection comparison, bindings, or disposition."
    - "Each packet, Markdown, decision, _SCHEMA, and _H1_SCHEMA FIFO variant yields its stable enclosing H1 error and never reaches a target os.open, proving nonblocking special-file rejection."
    - "_read_canonical_value parses only the bytes returned with FileEvidence; _expected_bindings calls read_authoritative_file(_ROOT, relative) once for _SCHEMA and once for _H1_SCHEMA, reuses the returned schema bytes for all three existing schema-hash sites and the returned H1-schema bytes for its one binding site, and translates custody failures to H1_BINDINGS_INVALID; validate_h1_packet compares Markdown only from the helper return; validate_h1_decision reuses _read_canonical without a parallel read path."
    - "Per D-13 through D-16, approved/disputed/incomplete rules, all-11 review semantics, H1_MANUAL_AUTHORIZATION_REQUIRED, stored bindings, local progression only, and external_publication_authorized=false remain unchanged."
  </behavior>
  <action>Extend the named single parameterized public H1 regression before production changes; do not add another test method. Reuse the existing packet/decision helpers only for disposable temporary inputs, create byte-identical disposable copies of `_SCHEMA` and `_H1_SCHEMA` under the test's temporary directory inside `_ROOT`, and patch the H1 module constants to those copies so no repository schema is ever swapped or rewritten. Apply `mock.patch(..., create=True)` to the H1 module's helper name so pre-fix execution reaches each current pathname reopen and fails the new expectation. Cover packet, Markdown, decision, `_SCHEMA`, and `_H1_SCHEMA` leaves with the Task 02-08-01 race harness: an external sentinel, a target-specific `os.open` hook that swaps the regular leaf to a symlink immediately before the final open, and a FIFO variant whose target-specific open trap must remain untouched. Assert the packet/Markdown/decision leaf-specific errors, `H1_BINDINGS_INVALID` for either supporting-schema custody failure, absence of external content from parsed values, schema hashes, bindings, or projection comparison, and no repository H1/formal/adversarial/decision/schema byte mutation. Then change `_read_canonical_value` to compute `_relative(path, code)` once, call `read_authoritative_file(_ROOT, relative)`, and parse only returned bytes while retaining the canonical-byte comparison and existing error translation. In `_expected_bindings`, compute relative paths for `_SCHEMA` and `_H1_SCHEMA`, call `read_authoritative_file(_ROOT, relative)` once per leaf, translate helper failures to `H1_BINDINGS_INVALID`, compute the two digests only from returned bytes, use the one schema digest at both formal/adversarial comparison sites and the returned binding site, and use the H1-schema digest at its returned binding site; remove all four direct schema `Path.read_bytes()` uses without modifying either schema. Change the Markdown branch of `validate_h1_packet` to call the helper and compare only returned bytes to `render_h1_markdown(value).encode("utf-8")`. Keep `validate_h1_decision` on `_read_canonical`, so packet and decision validation share one custody path, and retain `inspect_authoritative_path` only for `_publish_packet_pair`'s output-directory check. D-13: preserve the human-only three-state decision. D-14: retain every signed fixture semantic. D-15: keep formal evidence clean and adversarial diagnostics exact. D-16: retain immutable local-only bindings and external_publication_authorized=false. Remove obsolete imports only; add no reader, wrapper, dependency, schema rewrite, evidence mutation, or authority expansion.</action>
  <acceptance_criteria>
    - The single named method fails on pre-fix HEAD and passes all packet/Markdown/decision/`_SCHEMA`/`_H1_SCHEMA` symlink and FIFO subtests after the H1 substitutions; total new test methods across the plan remains exactly three.
    - Packet, Markdown, and decision failures use H1_PACKET_INVALID, H1_MARKDOWN_INVALID, and H1_DECISION_INVALID respectively; no external sentinel data is consumed.
    - Every FIFO subtest proves its target open trap was not reached, and no thread, process, timeout, or writer is needed to make the test finish; either supporting-schema custody failure reports H1_BINDINGS_INVALID.
    - `_expected_bindings` has no direct `_SCHEMA` or `_H1_SCHEMA` pathname read: its three schema digest uses share the digest of descriptor-returned `_SCHEMA` bytes, and its H1-schema binding uses the digest of descriptor-returned `_H1_SCHEMA` bytes.
    - Canonical JSON, packet self-hash, exact Markdown projection, all H1 bindings, fixture semantics, dispositions, and H1_MANUAL_AUTHORIZATION_REQUIRED behavior remain unchanged.
    - The stored formal attempt, explicit-formal adversarial report, and H1 packet/Markdown validate without regeneration; external_publication_authorized remains false.
    - Exactly three new methods across the plan yield 64/64 focused tests and 135 discovered methods partitioned as 130 green plus the exact five preserved expected-red methods.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_h1.H1PacketTests.test_public_h1_validators_reject_swapped_packet_markdown_and_decision_leaves_and_fifos_without_consuming_or_blocking -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter tests.test_measurement_parsing tests.test_measurement_scoring tests.test_measurement_attempts tests.test_measurement_h1 tests.test_filesystem_boundary -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-attempt --attempt runs/measurement-attempts/formal-golden-pr2164-v1 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-adversarial-report --report reports/h1/adversarial-oracle-results-v2.json --formal-attempt runs/measurement-attempts/formal-golden-pr2164-v1 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-h1-packet --packet reports/h1/h1-source-gold-review-v2/h1-source-gold-review-v2.json --markdown reports/h1/h1-source-gold-review-v2/h1-source-gold-review-v2.md &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -c '
import contextlib
import io
import unittest

expected_red = {
    "test_fixture_closure.FixtureClosureCandidateTests.test_fixture_accept_publish_rejects_racing_empty_target_without_overwrite",
    "test_fixture_closure.FixtureClosureCandidateTests.test_fixture_publish_fails_closed_when_no_replace_primitive_is_unavailable",
    "test_fixture_closure.FixtureClosureCandidateTests.test_public_fixture_acceptance_rejects_missing_mismatched_and_stale_authority",
    "test_fixture_closure.FixtureClosureCandidateTests.test_public_fixture_acceptance_resolves_its_own_current_v7_basis",
    "test_receipts.IntegrityReceiptTests.test_active_defaults_are_v7_from_experiment_and_repository_roots",
}

def flatten(suite):
    for item in suite:
        if isinstance(item, unittest.TestSuite):
            yield from flatten(item)
        else:
            yield item

discovered = list(flatten(unittest.defaultTestLoader.discover("tests")))
by_id = {test.id(): test for test in discovered}
assert len(discovered) == 135, len(discovered)
assert expected_red.issubset(by_id), sorted(expected_red - set(by_id))
green = [test for test in discovered if test.id() not in expected_red]
assert len(green) == 130, len(green)
with contextlib.redirect_stdout(io.StringIO()), contextlib.redirect_stderr(io.StringIO()):
    green_result = unittest.TextTestRunner(stream=io.StringIO(), verbosity=0).run(unittest.TestSuite(green))
assert green_result.wasSuccessful() and green_result.testsRun == 130
red_suite = unittest.TestSuite(by_id[test_id] for test_id in sorted(expected_red))
with contextlib.redirect_stdout(io.StringIO()), contextlib.redirect_stderr(io.StringIO()):
    red_result = unittest.TextTestRunner(stream=io.StringIO(), verbosity=0).run(red_suite)
observed_red = {test.id() for test, _ in [*red_result.failures, *red_result.errors]}
assert red_result.testsRun == 5 and len(red_result.failures) == 5 and len(red_result.errors) == 1
assert observed_red == expected_red
' &amp;&amp; git diff --quiet bdc6e0a3..HEAD -- bundles/accepted phase2/source-authority.json runs/measurement-attempts/formal-golden-pr2164-v1 reports/h1/adversarial-oracle-results-v2.json reports/h1/h1-source-gold-review-v2 reviews/h1-source-gold-decision-v1.json baselines config/boundary_allowlist.json ../../.planning/phases/01-isolated-evidence-boundary-and-source-integrity ../../spec ../../gen</automated>
  </verify>
  <done>Every H1 canonical evidence, packet, supporting-schema, Markdown, and decision consumer uses descriptor-returned bytes only; all five substituted/special-file leaf classes fail closed without external-byte use or blocking, and stored local-only H1 evidence and schema bytes remain valid and unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authoritative adapter path -> checked descriptor -> bounded parser | A declared score-bearing raw leaf becomes canonical adapter input only when FileEvidence and bytes come from the same no-follow descriptor. |
| authoritative fixture path -> checked descriptor -> preflight evidence map | A fixture-owned source becomes evidence-span input only through descriptor-bound bytes that retain the adapter-declared SHA-256. |
| H1 evidence/schema/packet/Markdown/decision path -> checked descriptor -> validator | Local evidence, both supporting schemas, and review material cross into hashing, canonical parsing, projection comparison, bindings, or disposition checks only through one checked descriptor. |
| custody repair -> later phase or publication authority | Closing a local read vulnerability must not create H1 approval, Phase 3 authority, model/API/remote capability, or publication authority. |

## STRIDE Threat Register — ASVS L1, blocking threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-08-01 | Tampering / Spoofing | `adapter._raw_identity` | high | mitigate | Task 02-08-01 consumes helper-returned bytes only and names a public-builder regression that swaps the final leaf to an external symlink immediately before open and asserts an invalid zero-record batch. |
| T-02-08-02 | Tampering / Denial of Service | `preflight._source_bytes_by_fixture` | high | mitigate | Task 02-08-01 names a public-preflight symlink/FIFO regression that requires blocking evidence diagnostics, no parsed predictions, no external bytes, and no target FIFO open. |
| T-02-08-03 | Spoofing / Tampering / Denial of Service | H1 evidence, `_SCHEMA`, `_H1_SCHEMA`, packet, Markdown, and decision readers | high | mitigate | Task 02-08-02 names one public-validator regression covering packet, Markdown, decision, and both supporting-schema leaves, exact enclosing H1 errors, immediate-before-open symlink swaps, pre-open FIFO rejection, and descriptor-returned bytes at every active read. |
| T-02-08-04 | Elevation of Privilege | repaired custody status -> H1/model/publication authority | high | mitigate | Task 02-08-02 revalidates stored H1 evidence, preserves human-only authorization and external_publication_authorized=false, and forbids evidence, decision, model, API, remote, or publication writes. |
| T-02-08-05 | Repudiation | Phase 1 live-boundary expected-red classification | medium | mitigate | Task 02-08-02 discovers runtime IDs, requires exactly 135 = 130 green + the exact five preserved methods, and asserts their five-failure/one-error signature without modifying Phase 1. |
| T-02-08-SC | Tampering | npm/pip/cargo supply chain | low | accept | The plan performs no install, changes no package or lockfile, and reuses only Python stdlib plus existing repository modules. |

Every high threat is mitigated by a named fail-first public-boundary regression and an existing authority/artifact validator. Any high-threat regression failure blocks completion.
</threat_model>

<verification>
- Observe and record RED for the two Task 02-08-01 methods together and the one Task 02-08-02 method before their production changes, then run all three GREEN.
- Run the six-module Phase 2 plus filesystem suite and require exactly 64/64 after exactly three new methods; the one H1 method must cover packet, Markdown, decision, `_SCHEMA`, and `_H1_SCHEMA` leaves.
- Validate active source authority as status=valid with fixture_count=11 and raw_file_count=28.
- Validate the stored formal attempt, explicit-formal adversarial report, and H1 packet/Markdown without invoking any generator, writer, packet builder, decision builder, or regeneration command.
- Discover exactly 135 runtime IDs; require 130 green and the exact five Phase 1 expected-red identities with five failures plus one error, using their discovered top-level module names rather than adding a package prefix.
- Require no diff since the final 02-07 implementation commit in accepted bundle, source-authority, formal/adversarial/H1/decision evidence, baselines, allowlist, Phase 1 planning, core UDB `spec/`, or generated data.
- Run `git diff --check` and inspect both task commits against `files_modified`; no unrelated or excluded file may enter either commit.
</verification>

<success_criteria>
- Adapter, preflight, and every H1 authoritative regular-file consumer use `read_authoritative_file` and consume only its descriptor-bound returned bytes while retaining all existing identity/hash/canonical comparisons; `_expected_bindings` derives all three `_SCHEMA` digest uses and the one `_H1_SCHEMA` digest use from those returned bytes.
- Three fail-first public-boundary methods cover adapter raw leaves, preflight fixture sources, and H1 packet/Markdown/decision/`_SCHEMA`/`_H1_SCHEMA` leaves for immediate-before-open symlink swaps plus nonblocking FIFO rejection.
- The exact valid 11/28 authority, all-11 measurement behavior, stable diagnostics, stored formal/adversarial/H1 evidence, and human-local-only H1 boundary remain valid without regeneration.
- The focused partition is exactly 64/64; discovery is exactly 135 = 130 green + five unchanged expected-red methods with five failures and one error.
- TS-03, TS-04, TS-05 and D-01 through D-16 remain covered; all seven probe assumptions and both descriptor-less value/safety prohibitions remain explicit.
- No new production abstraction, dependency, schema, evidence byte, baseline, allowlist, restart record, core UDB data, generated data, model, API, remote action, UI, publication action, or external authority is introduced.
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-measurement-spine/02-08-SUMMARY.md` when done. Record all three RED->GREEN method identities; packet/Markdown/decision/`_SCHEMA`/`_H1_SCHEMA` coverage inside the single H1 method; exact modified symbols/import substitutions and removal of all four direct supporting-schema reads; 64/64 focused result; valid 11/28 source authority; stored formal/adversarial/H1 validation without regeneration; 135 = 130 green + five expected-red discovery classification with the exact five-failure/one-error signature; unchanged schema/evidence/baseline/allowlist/restart/core-UDB boundaries; and unchanged human-local-only H1/external_publication_authorized=false authority.
</output>
