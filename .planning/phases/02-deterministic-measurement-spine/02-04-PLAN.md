---
phase: 02-deterministic-measurement-spine
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/attempts.py
  - experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py
  - experiments/specchoice-v1.3.2/tests/test_measurement_attempts.py
  - experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/attempt.json
  - experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/parsed-predictions.json
  - experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/diagnostics.json
  - experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/case-outcomes.json
  - experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/metrics.json
  - experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/report.json
  - experiments/specchoice-v1.3.2/reports/h1/adversarial-oracle-results-v1.json
autonomous: true
requirements: [TS-03, TS-04, TS-05]
must_haves:
  truths:
    - "Per D-11, every invocation publishes one immutable no-replace attempt containing bound input hashes, one adapter/schema identity, raw predictions, parsed projection, separate diagnostics, and terminal status; failed attempts remain audit evidence but have no metrics or formal authority."
    - "Per D-09 and D-12, only an exact all-11 zero-blocker formal invocation emits case outcomes, independent metrics, and a report; targeted, partial, warning-bearing golden, failed, or diagnostic-only material cannot be spliced, promoted, or represented as formal measurement authority."
    - "Per D-15, the generated formal golden attempt has diagnostics=[], completed status, and no unexpected warning/error; the separate adversarial artifact passes only when every expected stable code and structured field matches its frozen oracle."
    - "Attempt publication uses exclusive creation, file and directory fsync, and native no-replace publication; an existing or race-created target is preserved and never overwritten, merged, or replaced."
    - "The formal and adversarial artifacts bind only the active accepted verifier-rooted v2 source authority, exact adapter/rule/batch, current schema, and prediction identities; candidates, historical/revoked generations, live Git, and network fallback are rejected."
    - "Attempt completion and adversarial success carry no H1 disposition, approval, Phase 3 progression, model-call, remote, or publication authority."
    - "The focused four-module pre-H1 suite passes without modifying or weakening any Phase 1 custody module, accepted byte, source-authority record, or live-boundary assertion."
  artifacts:
    - path: "experiments/specchoice-v1.3.2/src/specchoice_measurement/attempts.py"
      provides: "Staged, fsynced, exclusive-create, native no-replace attempt publication with closed terminal roles"
    - path: "experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/attempt.json"
      provides: "Warning-free formal all-11 golden attempt authority embedding lossless base64 raw-prediction bytes plus the complete binding map and binding the separate parsed/diagnostic/outcome/metric/report artifacts"
    - path: "experiments/specchoice-v1.3.2/reports/h1/adversarial-oracle-results-v1.json"
      provides: "Separate diagnostic-only exact code/field verification artifact with no formal or H1 authority"
    - path: "experiments/specchoice-v1.3.2/tests/test_measurement_attempts.py"
      provides: "Task-owned TDD coverage for attempt immutability, collision/race preservation, terminal roles, formal generation, and adversarial generation"
  key_links:
    - from: "Plan 01 adapter + Plan 02 preflight + Plan 03 ScoreResult"
      to: "experiments/specchoice-v1.3.2/src/specchoice_measurement/attempts.py"
      via: "one run-measurement command captures raw input first, persists validation/scoring outputs separately, and publishes a terminal directory no-replace"
      pattern: "formal|diagnostic_only|invalid_preflight|completed"
    - from: "experiments/specchoice-v1.3.2/src/specchoice_measurement/attempts.py"
      to: "experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/attempt.json"
      via: "one non-cyclic canonical manifest embeds exact raw-prediction bytes and the complete binding map, then binds every separate sibling artifact SHA-256"
      pattern: "attempt_sha256|bindings_sha256|diagnostics_sha256|adapter_batch_sha256|schema_sha256"
    - from: "experiments/specchoice-v1.3.2/fixtures/measurement/adversarial/required-diagnostics-v1.json"
      to: "experiments/specchoice-v1.3.2/reports/h1/adversarial-oracle-results-v1.json"
      via: "diagnostic-only execution compares each exact code and structured field without emitting metrics or formal authority"
      pattern: "diagnostic_only|oracle_sha256|expected|observed"
  prohibitions:
    - "flagged-unverified: A targeted, partial, warning-bearing golden, failed, or diagnostic-only attempt must not be presented as formal measurement authority."
    - "flagged-unverified: Attempt completion or adversarial success must not create H1 approval, Phase 3 authority, model/remote capability, or publication authority."
    - "flagged-unverified: Phase 1 custody and live-boundary checks must not be weakened, rebased, or suppressed to make Phase 2 pass."
---

<objective>
**As an** experiment operator, **I want to** preserve the formal golden and adversarial executions as immutable, role-separated evidence, **so that** the later H1 review can bind exact machine evidence without accepting partial results or mutable output.

Implement no-replace attempt custody, generate the warning-free formal all-11 attempt, and generate the separate exact-code adversarial artifact per D-09, D-11, D-12, and D-15.

Purpose: Convert TS-03/04/05 scoring results into immutable machine evidence while stopping before H1 packet construction or any human disposition.
Output: Attempt custody code/tests, one validated formal golden attempt, and one separately validated diagnostic-only adversarial artifact.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-VERIFICATION.md
@.planning/phases/02-deterministic-measurement-spine/02-CONTEXT.md
@.planning/phases/02-deterministic-measurement-spine/02-RESEARCH.md
@.planning/phases/02-deterministic-measurement-spine/02-PATTERNS.md
@.planning/phases/02-deterministic-measurement-spine/02-VALIDATION.md
@.planning/phases/02-deterministic-measurement-spine/02-01-SUMMARY.md
@.planning/phases/02-deterministic-measurement-spine/02-02-SUMMARY.md
@.planning/phases/02-deterministic-measurement-spine/02-03-SUMMARY.md
@experiments/specchoice-v1.3.2/src/specchoice_evidence/bundle.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/scoring.py
@experiments/specchoice-v1.3.2/fixtures/measurement/adversarial/required-diagnostics-v1.json

<interfaces>
Existing immutable publication primitives to reuse without changing Phase 1:

```python
def _write_exact(path: Path, content: bytes) -> None: ...
def _sync_directory(directory: Path) -> None: ...
def _publish_directory_no_replace(source: Path, target: Path, collision_code: str) -> None: ...
```

Plan 03 contract consumed here and Plan 05 attempt contract created here:

```python
@dataclass(frozen=True)
class ScoreResult:
    status: str
    case_outcomes: tuple[object, ...]
    metrics: object | None
    diagnostics: tuple[Diagnostic, ...]

def run_measurement_attempt(*, mode: str, attempt_id: str, attempt_root: Path, inputs: object) -> object: ...
def validate_measurement_attempt(*, attempt_root: Path) -> object: ...
```
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-04-01: Prove immutable no-replace attempt custody end to end</name>
  <reversibility rating="costly">The versioned attempt format is consumed by H1 and later phases; changing it requires new immutable attempts while historical terminal directories remain untouched.</reversibility>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_attempts.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/attempts.py</files>
  <behavior>
    - "RED first: one complete zero-blocker ScoreResult stages, fsyncs, and no-replace publishes a closed attempt manifest containing lossless base64 raw-prediction bytes, their SHA-256, the complete binding map, terminal status, and a non-cyclic attempt digest, plus separate parsed projection, diagnostics, case outcomes, metrics, and report artifacts."
    - "Blocking preflight publishes an immutable invalid attempt whose manifest still preserves raw-prediction bytes, their SHA-256, complete bindings, and status alongside separate parsed/diagnostics artifacts, but no case outcomes, metrics, or report, and returns nonzero CLI semantics."
    - "Targeted execution is permanently diagnostic_only and cannot gain a formal role; warning-bearing golden execution may complete_with_warnings but cannot become the formal H1 input per D-12/D-15."
    - "An existing target and a race-created target retain their original bytes and produce stable collision/unavailable-primitive diagnostics rather than overwrite, merge, or fallback replacement."
  </behavior>
  <action>Create `tests/test_measurement_attempts.py` first and run it red against the missing custody module. Reuse the existing Phase 1 exclusive-create, file/directory fsync, and native no-replace publication primitives by import without editing or relaxing them. Per D-11, capture raw prediction bytes and all adapter/schema/input identities before parsing; stage one closed `attempt.json` manifest containing the exact raw bytes as lossless base64, their SHA-256, the complete binding map, terminal role/status, and a digest over the projection without `attempt_sha256`, plus separate parsed, diagnostics, and permitted score artifacts under a sibling temporary directory; publish exactly once after self-validation. Decoding the manifest's base64 must reproduce the original raw bytes exactly before its SHA-256 is compared, with no text normalization or JSON reserialization. Per D-09/D-12, keep formal, diagnostic_only, invalid_preflight, completed, and completed_with_warnings as closed non-promotable roles/statuses, and omit every score artifact after a blocker. Test pre-existing targets, race-created targets, interrupted staging, missing native no-replace support, digest tampering, and exact preservation of Phase 1/source-authority bytes.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_attempts -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>Every invocation can become one immutable, collision-safe, fully bound terminal attempt, while failed or diagnostic-only evidence remains auditable and permanently non-formal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-04-02: Generate and validate the warning-free formal golden attempt</name>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_attempts.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py, experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/attempt.json, experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/parsed-predictions.json, experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/diagnostics.json, experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/case-outcomes.json, experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/metrics.json, experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v1/report.json</files>
  <behavior>
    - "The formal command consumes only the active accepted verifier-rooted source authority, one adapter/rule/batch, current JSON schema, and exact golden prediction set."
    - "The generated attempt covers all 11 unique fixtures, contains diagnostics=[], completed status, exact independent outcomes, and no unexpected warning or error."
    - "Every separate sibling artifact is canonical/hash-bound and the attempt digest is non-cyclic; changing any manifest-embedded raw-prediction byte or binding, or any parsed, diagnostic, outcome, metric, or report byte, invalidates validation."
    - "Regeneration at the same target fails without changing the existing formal attempt; a new version requires a new attempt identity rather than replacement."
  </behavior>
  <action>Extend the owning tests first with the exact formal all-11 CLI path, clean-golden D-15 assertions, complete manifest-and-sibling binding matrix, lossless recovery of the embedded raw prediction bytes, and repeated-target preservation, then run them red. Wire the CLI through the accepted source-authority validator, Plan 01 adapter, Plan 02 preflight, Plan 03 scorer, and Task 02-04-01 publisher. Generate the declared formal attempt from the exact golden prediction set only after the four-module pre-H1 suite passes. Validate exact 11-case coverage, diagnostics=[], independent metrics, canonical bytes, non-cyclic digests, every embedded input/binding, and every sibling hash. Stop and preserve a failed immutable attempt if any source, adapter, schema, prediction, preflight, score, or custody check fails; emit no H1 packet or decision field.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter tests.test_measurement_parsing tests.test_measurement_scoring tests.test_measurement_attempts -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-attempt --attempt runs/measurement-attempts/formal-golden-pr2164-v1 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>The exact accepted-v2 all-11 golden run exists as a warning-free, byte-stable, immutable formal attempt whose every input and sibling artifact validates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-04-03: Generate the separate exact-code adversarial artifact</name>
  <files>experiments/specchoice-v1.3.2/tests/test_measurement_attempts.py, experiments/specchoice-v1.3.2/src/specchoice_measurement/cli.py, experiments/specchoice-v1.3.2/reports/h1/adversarial-oracle-results-v1.json</files>
  <behavior>
    - "Every adversarial case executes as diagnostic_only and passes only when the complete stable diagnostic record equals the frozen oracle's code, severity, fixture/finding identity, field, occurrence, expected/observed values, and source identity."
    - "Parser, duplicate/conflict, no-finding, candidate, identity-warning, evidence hash/range/text, empty/adjacent, ordering, and attempt-role cases remain separate oracle entries."
    - "Expected adversarial diagnostics do not become golden warnings, formal metrics, H1 disposition, or machine approval."
    - "Equivalent oracle execution is byte-stable; changed oracle, missing case, extra diagnostic, reordered-without-canonicalization, or mixed-attempt evidence fails validation."
  </behavior>
  <action>Extend the owning tests first with complete adversarial execution and artifact validation, then run them red. Add CLI support that executes every frozen adversarial entry only through diagnostic_only attempts, compares the complete structured diagnostic array to the oracle, and writes one canonical result binding the oracle plus each case outcome. Preserve explicit empty collections and declared ordering; do not splice diagnostics between attempts or convert expected failures into formal metrics. Generate the declared result only after the four-module pre-H1 suite and formal-attempt validator pass. Keep H1 packet construction, Markdown, decision schema, dispositions, signatures, and human checkpoint entirely out of this plan.</action>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_measurement_adapter tests.test_measurement_parsing tests.test_measurement_scoring tests.test_measurement_attempts -q &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-attempt --attempt runs/measurement-attempts/formal-golden-pr2164-v1 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_measurement.cli validate-adversarial-report --report reports/h1/adversarial-oracle-results-v1.json &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m specchoice_evidence.cli validate-phase2-source-authority --authority phase2/source-authority.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v2</automated>
  </verify>
  <done>The separate adversarial artifact proves every expected code and structured field without contaminating the warning-free formal attempt or creating H1 authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline result → immutable attempt | Mutable in-process results become terminal evidence only through exclusive writes, fsync, self-validation, binding, and no-replace publication. |
| formal inputs → formal attempt | Only the accepted-v2 all-11 zero-blocker golden path may emit formal outcomes/metrics/report. |
| adversarial oracle → diagnostic result | Expected failures are exact diagnostic-only evidence and cannot be promoted or spliced into formal authority. |
| attempt/report status → later plans | Machine completion proves evidence integrity only; it carries no H1, model, remote, or publication capability. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | attempt/adversarial closed inputs | high | mitigate | Tasks 02-04-01/02/03 rerun the adapter/parser/scorer closure tests and reject duplicate, missing, unknown, mixed-identity, subset, or extra score-bearing input before publication. |
| T-02-02 | Tampering | score-bearing attempt and adversarial oracle | high | mitigate | The formal attempt binds only allowlisted canonical score results; the separate adversarial artifact compares exact diagnostics and cannot alter metrics or formal status. |
| T-02-03 | Tampering / Repudiation | raw bytes, paths, fsync/no-replace attempts | high | mitigate | Exclusive creation, file/directory fsync, native no-replace publication, lossless raw-byte recovery plus complete manifest/sibling hashes, terminal roles, and no score files after blockers preserve drift and failed evidence. |
| T-02-04 | Elevation of Privilege / Repudiation | formal/diagnostic completion role | high | mitigate | Closed attempt/report schemas omit H1 disposition, approval, Phase 3, model, remote, and publication authority; diagnostic_only can never be promoted. |
| T-02-PATH | Tampering / Elevation of Privilege | attempt/report roots | medium | mitigate | Validate experiment-relative roots through Phase 1 filesystem primitives and preserve any pre-existing, linked, special-file, traversal, mount-drift, or race-created target. |

All high threats are mitigated by the four-module pre-H1 suite and artifact validators. Any failed high mitigation or unexpected golden diagnostic blocks Plan 05.
</threat_model>

<verification>
- Run `test_measurement_attempts` after Task 02-04-01, then the accumulated adapter/parsing/scoring/attempts suite after Tasks 02-04-02/03.
- Validate the exact source authority, formal attempt, separate adversarial result, no-replace collision/race behavior, complete bindings, and absence of score files on invalid/diagnostic-only attempts.
- Confirm no H1 packet, Markdown, decision schema, decision writer, disposition, signature, Phase 3 progression, model call, or publication authority is created in this plan.
- Keep Phase 1 custody and live-boundary tests unchanged; repository-wide discovery is not substituted for the focused Phase 2 gate.
</verification>

<success_criteria>
- TS-03/04/05 machine evidence is preserved as one warning-free formal all-11 attempt plus one separate exact-code adversarial result.
- D-09, D-11, D-12, and D-15 role, completeness, exit, and warning-separation contracts pass.
- Attempt directories and reports are deterministic, hash-bound, fsynced, collision-safe, and no-replace.
- All T-02-01 through T-02-04 high-risk mitigations pass under the focused pre-H1 suite.
- No H1 approval, human decision artifact, Phase 3 authority, model/API call, publication, push, Phase 1 custody change, ROADMAP/STATE mutation, or remote action occurs.
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-measurement-spine/02-04-SUMMARY.md` when done, recording only machine-generated formal/adversarial evidence and their exact immutable bindings.
</output>
