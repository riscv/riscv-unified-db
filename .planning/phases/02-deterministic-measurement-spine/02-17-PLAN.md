---
phase: 02-deterministic-measurement-spine
plan: 17
type: execute
wave: 17
depends_on: [02-16]
files_modified:
  - experiments/specchoice-v1.3.2/receipts/h1-review-readiness-v3.json
  - experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v2.json
  - .planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-SECURITY.md
  - .planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-VERIFICATION.md
  - .planning/phases/02-deterministic-measurement-spine/02-REVIEW.md
  - .planning/phases/02-deterministic-measurement-spine/02-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [TS-03, TS-04, TS-05]
must_haves:
  truths:
    - "Readiness binds the actual 02-16 summary through a normalized absolute repository-root route and the canonical 72/150/145 phase-gate stdout."
    - "Only the independent human reviewer supplies the seven semantic responses and approved/disputed/incomplete H1 disposition."
    - "Four fresh reports audit the same captured commit and enforce Phase 1 security/verification plus Phase 2 deep-review/verification closure."
    - "Approved may pass Phase 2 only at score 35/35 must-haves verified; disputed or incomplete must produce gaps_found."
  artifacts:
    - path: experiments/specchoice-v1.3.2/receipts/h1-review-readiness-v3.json
      provides: "Decision-free final readiness bound to 02-16 and the exact phase gate"
    - path: experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v2.json
      provides: "Independent local-only seven-response H1 disposition"
    - path: .planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-SECURITY.md
      provides: "Fresh Phase 1 secure-phase contract report"
    - path: .planning/phases/02-deterministic-measurement-spine/02-VERIFICATION.md
      provides: "Disposition-aware 35/35 must-haves Phase 2 verdict"
  key_links:
    - from: .planning/phases/02-deterministic-measurement-spine/02-16-SUMMARY.md
      to: experiments/specchoice-v1.3.2/receipts/h1-review-readiness-v3.json
      via: "normalized absolute repository root and descriptor-held summary bytes"
    - from: experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v2.json
      to: .planning/phases/02-deterministic-measurement-spine/02-VERIFICATION.md
      via: "approved requires passed score 35/35 must-haves verified; disputed/incomplete requires gaps_found"
    - from: .planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-SECURITY.md
      to: .planning/phases/02-deterministic-measurement-spine/02-VERIFICATION.md
      via: "same audited_commit and zero open Phase 1 threats"
---

<objective>
Create final hash-bound readiness, obtain the independent H1 disposition, and regenerate four same-commit reports that close or explicitly keep open Phase 2.

Purpose: Convert completed machine evidence into an auditable human-governed phase verdict without publication authority.
Output: Readiness-v3, human decision-v2, fresh Phase 1 security/verification and Phase 2 review/verification reports.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
@/Users/zhdeng/.codex/gsd-core/workflows/secure-phase.md
@/Users/zhdeng/.codex/gsd-core/workflows/verify-phase.md
@/Users/zhdeng/.codex/gsd-core/workflows/code-review.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-measurement-spine/02-CONTEXT.md
@.planning/phases/02-deterministic-measurement-spine/02-16-SUMMARY.md
@experiments/specchoice-v1.3.2/phase2/source-authority.json
@experiments/specchoice-v1.3.2/receipts/fixture-closure-revocation-v2.json
@experiments/specchoice-v1.3.2/receipts/fixture-closure-offline-replay-v3.json
@experiments/specchoice-v1.3.2/bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v3
@experiments/specchoice-v1.3.2/config/measurement/pr2164-adapter-rules-v1.json
@experiments/specchoice-v1.3.2/fixtures/measurement/golden-predictions-v2.json
@experiments/specchoice-v1.3.2/fixtures/measurement/adversarial/required-diagnostics-v2.json
@experiments/specchoice-v1.3.2/runs/measurement-attempts/formal-golden-pr2164-v2/**
@experiments/specchoice-v1.3.2/reports/h1/adversarial-oracle-results-v3.json
@experiments/specchoice-v1.3.2/reports/h1/adversarial-oracle-results-v3-attempts/**
@experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.json
@experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.md
@experiments/specchoice-v1.3.2/config/measurement/h1-review-schema-v2.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Bind canonical phase-gate stdout and absolute 02-16 summary into readiness-v3</name>
  <files>experiments/specchoice-v1.3.2/receipts/h1-review-readiness-v3.json</files>
  <precondition>02-16-SUMMARY records exact active-v3/revocation/evidence hashes and successful 72/150/145 validation; no post-cutover open-state failure remains.</precondition>
  <read_first>
    - .planning/phases/02-deterministic-measurement-spine/02-16-SUMMARY.md — authoritative cutover/evidence completion record
    - experiments/specchoice-v1.3.2/reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.json — decision-free packet-v3
    - experiments/specchoice-v1.3.2/config/measurement/h1-review-schema-v2.json — closed readiness and decision bindings
    - experiments/specchoice-v1.3.2/receipts/fixture-closure-revocation-v2.json — effective anti-rollback binding
    - experiments/specchoice-v1.3.2/phase2/source-authority.json — active-v3 identity
    - experiments/specchoice-v1.3.2/bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v3 — exact active-v3 source consumed by H1 packet validation
    - experiments/specchoice-v1.3.2/fixtures/measurement/golden-predictions-v2.json — successor golden bytes bound by formal/adversarial/H1
    - experiments/specchoice-v1.3.2/fixtures/measurement/adversarial/required-diagnostics-v2.json — successor oracle with exact golden-v2 base identity
  </read_first>
  <action>
From `experiments/specchoice-v1.3.2`, derive `SPECCHOICE_REPO_ROOT="$(git rev-parse --show-toplevel)"`, require a normalized absolute directory, and reject authoritative inputs containing any lexical `..` path component. Run the exact 72/150/145 phase oracle and pipe its one canonical stdout object directly into the public readiness writer through `--phase-gate-stdin`; do not parse or recreate counts from prose. When the target is absent, execute the exact creation command below once. Pass the summary as the exact absolute argument `$SPECCHOICE_REPO_ROOT/.planning/phases/02-deterministic-measurement-spine/02-16-SUMMARY.md`. Pass the same explicit accepted-v3 bundle, adapter rules, golden-v2 and oracle-v2 used by packet-v3 so readiness internally revalidates the complete active-v3 evidence graph; no predecessor default is permitted. The writer must open that summary and all repository/experiment artifacts by descriptor-rooted absolute routing from the held repository root, bind their actual canonical bytes/hashes, and publish readiness-v3 exclusively with no-replace plus file/parent fsync. Do not invoke the writer again during verification or resume. If readiness already exists after an interrupted run, skip the creation command and pipe a fresh oracle receipt into `validate-h1-readiness-v3 --phase-gate-stdin`; continue only when all exact bytes validate, otherwise fail without overwrite, repair, or deletion. Readiness is decision-free, contains no disposition/reviewer/semantic response, and records `external_publication_authorized: false`. Preserve D-01 through D-16.

```bash
set -o pipefail &amp;&amp;
SPECCHOICE_REPO_ROOT="$(git rev-parse --show-toplevel)" &amp;&amp;
test "${SPECCHOICE_REPO_ROOT#/}" != "$SPECCHOICE_REPO_ROOT" &amp;&amp;
cd "$SPECCHOICE_REPO_ROOT/experiments/specchoice-v1.3.2" &amp;&amp;
PYTHONPATH=src python3 tests/phase1_expected_red_oracle.py --expected-focused 72 --expected-discovered 150 --expected-green 145 |
PYTHONPATH=src python3 -m specchoice_measurement.cli write-h1-readiness-v3 --phase-gate-stdin --schema config/measurement/h1-review-schema-v2.json --source-authority phase2/source-authority.json --canonical-revocation receipts/fixture-closure-revocation-v2.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v3 --rules config/measurement/pr2164-adapter-rules-v1.json --predictions fixtures/measurement/golden-predictions-v2.json --oracle fixtures/measurement/adversarial/required-diagnostics-v2.json --offline-replay receipts/fixture-closure-offline-replay-v3.json --formal-attempt runs/measurement-attempts/formal-golden-pr2164-v2 --adversarial-result reports/h1/adversarial-oracle-results-v3.json --packet reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.json --markdown reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.md --plan-summary "$SPECCHOICE_REPO_ROOT/.planning/phases/02-deterministic-measurement-spine/02-16-SUMMARY.md" --output receipts/h1-review-readiness-v3.json
```
  </action>
  <verify><automated>set -o pipefail &amp;&amp; SPECCHOICE_REPO_ROOT="$(git rev-parse --show-toplevel)" &amp;&amp; test "${SPECCHOICE_REPO_ROOT#/}" != "$SPECCHOICE_REPO_ROOT" &amp;&amp; cd "$SPECCHOICE_REPO_ROOT/experiments/specchoice-v1.3.2" &amp;&amp; PYTHONPATH=src python3 tests/phase1_expected_red_oracle.py --expected-focused 72 --expected-discovered 150 --expected-green 145 | PYTHONPATH=src python3 -m specchoice_measurement.cli validate-h1-readiness-v3 --phase-gate-stdin --schema config/measurement/h1-review-schema-v2.json --source-authority phase2/source-authority.json --canonical-revocation receipts/fixture-closure-revocation-v2.json --bundle bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v3 --rules config/measurement/pr2164-adapter-rules-v1.json --predictions fixtures/measurement/golden-predictions-v2.json --oracle fixtures/measurement/adversarial/required-diagnostics-v2.json --offline-replay receipts/fixture-closure-offline-replay-v3.json --formal-attempt runs/measurement-attempts/formal-golden-pr2164-v2 --adversarial-result reports/h1/adversarial-oracle-results-v3.json --packet reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.json --markdown reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.md --plan-summary "$SPECCHOICE_REPO_ROOT/.planning/phases/02-deterministic-measurement-spine/02-16-SUMMARY.md" --readiness receipts/h1-review-readiness-v3.json</automated></verify>
  <acceptance_criteria>
    - The writer receives canonical oracle stdout by pipe and the 02-16 summary by normalized absolute path with no lexical `..`.
    - Readiness validates exact active-v3, revocation, formal/adversarial/packet/schema/summary and phase-gate bytes.
    - Readiness contains no human disposition or publication authority.
  </acceptance_criteria>
  <done>One immutable decision-free readiness-v3 exists and is descriptor-bound to the actual completed cutover/evidence state.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Independently record the seven-response H1 disposition</name>
  <what-built>Machine-validated readiness-v3 and packet-v3 binding active-v3, formal-v2, adversarial-v3, exact 72/150/145 gate and 02-16 summary.</what-built>
  <instructions>The independent reviewer creates a closed `fixture_reviews` array with exactly one human review object for each of the packet's 11 `fixture_id` and `reviewed_semantics_sha256` pairs. Every object explicitly records `approved|disputed|incomplete`, reviewer, and signature; signature may be null only for an incomplete decision. The reviewer also supplies a closed `semantic_responses` object with exactly these seven keys: `ts03_adjacency`, `ts03_empty_null_single_element`, `ts03_equal_element_stable_order`, `ts04_unclassified_manual_review`, `ts05_adjacency`, `ts05_empty_null_single_element`, and `ts05_equal_element_stable_order`; every value explicitly records `approved|disputed|incomplete` plus a human response, nullable only for incomplete. The reviewer alone creates `experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v2.json` with exact readiness/schema/packet/phase-gate hashes, reviewer identity, timestamp, rationale, those 11 fixture reviews, all seven semantic responses, one consistent `aggregate_disposition`, and `external_publication_authorized: false`. The executor must not generate, copy, infer or default any human field.</instructions>
  <action>Pause for the independent reviewer to perform the instructions and create the decision file; automation may validate the result but must not author or alter it.</action>
  <verification>
    Run `cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONPATH=src python3 -m specchoice_measurement.cli validate-h1-decision-v2 --schema config/measurement/h1-review-schema-v2.json --packet reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.json --readiness receipts/h1-review-readiness-v3.json --decision reviews/h1-source-gold-decision-v2.json`. Confirm the closed canonical stdout receipt reports `fixture_count: 11`, the exact seven sorted response IDs, the selected aggregate, and `external_publication_authorized: false`; approved requires every fixture and response approved/non-null, disputed requires at least one disputed and none incomplete, and incomplete requires at least one explicitly incomplete/null leaf. Disputed/incomplete validate structurally but remain blocking for a passing phase verdict.
  </verification>
  <verify><automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONPATH=src python3 -m specchoice_measurement.cli validate-h1-decision-v2 --schema config/measurement/h1-review-schema-v2.json --packet reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.json --readiness receipts/h1-review-readiness-v3.json --decision reviews/h1-source-gold-decision-v2.json</automated></verify>
  <acceptance_criteria>
    - The decision contains exactly 11 packet-bound fixture review entries and the seven exact semantic response objects.
    - Aggregate disposition is consistent with all leaf dispositions/nullability and external publication authority is exactly false.
    - Validation emits the single closed canonical receipt consumed by Task 3; no executor-authored human field exists.
  </acceptance_criteria>
  <resume-signal>Provide `approved`, `disputed`, or `incomplete` after the decision file validates; include issues when not approved.</resume-signal>
  <done>A human-authored, hash-valid local-only H1 decision exists; automation has not conferred approval or publication authority.</done>
</task>

<task type="auto">
  <name>Task 3: Produce four fresh same-commit security, review and verification reports</name>
  <files>.planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-SECURITY.md, .planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-VERIFICATION.md, .planning/phases/02-deterministic-measurement-spine/02-REVIEW.md, .planning/phases/02-deterministic-measurement-spine/02-VERIFICATION.md</files>
  <precondition>The human decision-v2 validates against `receipts/h1-review-readiness-v3.json`; approved, disputed and incomplete are all reportable, but only approved can yield a passed Phase 2 verdict.</precondition>
  <read_first>
    - /Users/zhdeng/.codex/gsd-core/workflows/secure-phase.md — SECURITY report contract and executor handoff
    - /Users/zhdeng/.codex/gsd-core/workflows/verify-phase.md — goal-backward phase verification contract
    - /Users/zhdeng/.codex/gsd-core/workflows/code-review.md — deep code-review report contract
    - .planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-SECURITY.md — prior report is superseded, not treated as fresh evidence
    - .planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-VERIFICATION.md — prior report is superseded, not treated as fresh evidence
    - .planning/phases/02-deterministic-measurement-spine/02-REVIEW.md — prior findings, including WR-01, must be re-audited
    - .planning/phases/02-deterministic-measurement-spine/02-09-PLAN.md and 02-09-SUMMARY.md — descriptor-rooted source-custody repair contract and implementation receipt
    - .planning/phases/02-deterministic-measurement-spine/02-10-PLAN.md and 02-10-SUMMARY.md — verifier-rooted-v3 construction and candidate audit
    - .planning/phases/02-deterministic-measurement-spine/02-11-PLAN.md and 02-11-SUMMARY.md — accepted-v3 and human-reviewed pending cutover
    - .planning/phases/02-deterministic-measurement-spine/02-12-PLAN.md and 02-12-SUMMARY.md — accepted-v3 receipt and copied-offline replay custody
    - experiments/specchoice-v1.3.2/bundles/accepted/source-contract-v3-pr2164-fixture-closure-22e84458-verifier-rooted-v3 — accepted successor source
    - experiments/specchoice-v1.3.2/receipts/fixture-closure-revocation-v2.json — effective anti-rollback receipt
    - experiments/specchoice-v1.3.2/phase2/source-authority.json — active-v3 authority
    - experiments/specchoice-v1.3.2/reviews/h1-source-gold-decision-v2.json — validated human disposition
  </read_first>
  <action>
Before any report write, capture one `AUDITED_COMMIT=$(git rev-parse HEAD)` and require all four reports to record that exact full commit in frontmatter `audited_commit`. Dispatch fresh, independent role runs in this order: (1) `gsd-security-auditor` for Phase 1, then have the executor write `01-SECURITY.md` exactly to the secure-phase report contract from that result; (2) `gsd-verifier` for Phase 1 writing `01-VERIFICATION.md`; (3) `gsd-code-reviewer` in deep mode for the exact Phase 2 gap-closure code/evidence scope through 02-16 plus readiness/decision, writing `02-REVIEW.md`; (4) `gsd-verifier` for Phase 2 writing `02-VERIFICATION.md`. The Phase 1 security and verifier dispatch inputs must explicitly include corrective source-custody Plans and SUMMARYs 02-09 through 02-12, accepted-v3, canonical revocation and active-v3, so each role re-audits the original lstat/open vulnerability and the successor closure rather than limiting itself to historical Phase 1 plans. Do not reuse an earlier report verdict or audit different commits. Require Phase 1 SECURITY `status: verified` and `threats_open: 0`; Phase 1 VERIFICATION `status: passed`; Phase 2 REVIEW `status: clean`, `critical: 0`, `warning: 0`, `info: 0`, `total: 0`, with WR-01 rechecked; and disposition-aware Phase 2 VERIFICATION: human `approved` requires `status: passed` and `score: "35/35 must-haves verified"`, while `disputed|incomplete` requires `status: gaps_found` and must enumerate all seven exact `semantic_responses` identifiers. The executable check below must consume the four reports and decision through the hardened descriptor-held reader from 02-09, not `Path.read_text()` or another pathname reopen. Do not edit ROADMAP.md or STATE.md. Preserve D-01 through D-16 and local-only/no-publication authority.
  </action>
  <verify><automated>
SPECCHOICE_REPO_ROOT="$(git rev-parse --show-toplevel)" &amp;&amp;
test "${SPECCHOICE_REPO_ROOT#/}" != "$SPECCHOICE_REPO_ROOT" &amp;&amp;
cd "$SPECCHOICE_REPO_ROOT/experiments/specchoice-v1.3.2" &amp;&amp;
PYTHONPATH=src python3 tests/phase1_expected_red_oracle.py --expected-focused 72 --expected-discovered 150 --expected-green 145 &amp;&amp;
cd "$SPECCHOICE_REPO_ROOT" &amp;&amp;
PYTHONPATH=experiments/specchoice-v1.3.2/src python3 - &lt;&lt;'PY'
import json
import os
import re
import subprocess
import sys
from pathlib import Path

from specchoice_evidence.canonical import canonical_json_bytes
from specchoice_evidence.filesystem import read_authoritative_file

root = Path(".")
report_paths = [
    ".planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-SECURITY.md",
    ".planning/phases/01-isolated-evidence-boundary-and-source-integrity/01-VERIFICATION.md",
    ".planning/phases/02-deterministic-measurement-spine/02-REVIEW.md",
    ".planning/phases/02-deterministic-measurement-spine/02-VERIFICATION.md",
]
texts = [
    read_authoritative_file(root, path)[1].decode("utf-8", "strict")
    for path in report_paths
]

def unquote(value):
    value = value.strip()
    if len(value) &gt;= 2 and value[0] == value[-1] == '"':
        return value[1:-1]
    return value

def frontmatter(text):
    assert text.startswith("---\n")
    block, marker, _ = text[4:].partition("\n---\n")
    assert marker
    root_keys = []
    for line in block.splitlines():
        if line and line[0] not in " \t#" and ":" in line:
            key = line.split(":", 1)[0]
            assert key not in root_keys
            root_keys.append(key)
    return block

def scalar(block, key, indent=""):
    pattern = rf"(?m)^{re.escape(indent + key)}:\s*(.*?)\s*$"
    matches = re.findall(pattern, block)
    assert len(matches) == 1, (key, matches)
    return unquote(matches[0])

def findings_block(block):
    assert len(re.findall(r"(?m)^findings:\s*$", block)) == 1
    match = re.search(r"(?m)^findings:\s*\n((?:^[ \t]+[^\n]*(?:\n|$))*)", block)
    assert match
    return match.group(1)

frontmatters = [frontmatter(text) for text in texts]
audited_commit = subprocess.check_output(
    ["git", "rev-parse", "HEAD"], text=True
).strip()
assert {scalar(block, "audited_commit") for block in frontmatters} == {
    audited_commit
}
assert scalar(frontmatters[0], "status") == "verified"
assert scalar(frontmatters[0], "threats_open") == "0"
assert scalar(frontmatters[1], "status") == "passed"
assert scalar(frontmatters[2], "status") == "clean"
review_findings = findings_block(frontmatters[2])
for key in ("critical", "warning", "info", "total"):
    assert scalar(review_findings, key, "  ") == "0"

experiment = root / "experiments/specchoice-v1.3.2"
environment = dict(os.environ)
environment["PYTHONPATH"] = str((experiment / "src").resolve())
validated = subprocess.run(
    [
        sys.executable,
        "-m",
        "specchoice_measurement.cli",
        "validate-h1-decision-v2",
        "--schema",
        "config/measurement/h1-review-schema-v2.json",
        "--packet",
        "reports/h1/h1-source-gold-review-v3/h1-source-gold-review-v3.json",
        "--readiness",
        "receipts/h1-review-readiness-v3.json",
        "--decision",
        "reviews/h1-source-gold-decision-v2.json",
    ],
    cwd=experiment,
    env=environment,
    stdout=subprocess.PIPE,
    stderr=subprocess.PIPE,
)
assert validated.returncode == 0
assert validated.stderr == b""

def unique_object(pairs):
    result = {}
    for key, value in pairs:
        assert key not in result
        result[key] = value
    return result

decision_receipt = json.loads(
    validated.stdout.decode("utf-8", "strict"),
    object_pairs_hook=unique_object,
)
assert canonical_json_bytes(decision_receipt) == validated.stdout
assert set(decision_receipt) == {
    "aggregate_disposition",
    "decision_sha256",
    "external_publication_authorized",
    "fixture_count",
    "semantic_response_ids",
    "valid",
}
response_ids = {
    "ts03_adjacency",
    "ts03_empty_null_single_element",
    "ts03_equal_element_stable_order",
    "ts04_unclassified_manual_review",
    "ts05_adjacency",
    "ts05_empty_null_single_element",
    "ts05_equal_element_stable_order",
}
assert decision_receipt["valid"] is True
assert decision_receipt["external_publication_authorized"] is False
assert decision_receipt["fixture_count"] == 11
assert decision_receipt["semantic_response_ids"] == sorted(response_ids)
disposition = decision_receipt["aggregate_disposition"]
if disposition == "approved":
    assert scalar(frontmatters[3], "status") == "passed"
    assert scalar(frontmatters[3], "score") == "35/35 must-haves verified"
else:
    assert disposition in {"disputed", "incomplete"}
    assert scalar(frontmatters[3], "status") == "gaps_found"
    assert scalar(frontmatters[3], "score") != "35/35 must-haves verified"
    assert all(response_id in texts[3] for response_id in response_ids)
PY
  </automated></verify>
  <acceptance_criteria>
    - All four reports are fresh, schema-valid and record the same existing audited commit in their unique first YAML frontmatter; body text cannot satisfy a gate field.
    - Phase 1 security is verified with zero threats and Phase 1 verification passes.
    - Phase 2 deep review is clean with zero critical, warning, info and total findings, including WR-01 closure.
    - The final gate reruns the descriptor-held, closed decision validator and consumes its one canonical stdout receipt; it never trusts a bare JSON load.
    - Phase 2 verification has score `35/35 must-haves verified` only for approved; disputed/incomplete is gaps_found and enumerates all seven exact response IDs.
    - ROADMAP.md and STATE.md remain unchanged.
  </acceptance_criteria>
  <done>The four-report contract passes for one audited commit and the Phase 2 verdict exactly follows the validated human disposition.</done>
</task>

</tasks>

<protected_path_gate>
Use the 02-16 final task commit as baseline and separately inventory staged/worktree/untracked/ignored paths. Allow only `receipts/h1-review-readiness-v3.json`, decision-v2 and the four report paths. Freeze active/revocation/accepted/evidence/schema/code/test bytes, every predecessor summary, ROADMAP.md, STATE.md, `gen/`, and `.DS_Store`. Record repo-root normalization, exact summary absolute path, readiness/decision hashes, human disposition, shared audited commit, four verdict fields, phase-gate receipt and final task commit in 02-17-SUMMARY.md.
</protected_path_gate>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Machine readiness to human decision | Automation presents bindings; only the reviewer supplies semantics and disposition. |
| Repository state to audit reports | Four independent workflows must assess one captured commit. |

## STRIDE Threat Register — OWASP ASVS L1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-17-01 | Spoofing | Planning-summary route | high | mitigate | Normalized absolute repo root, no lexical parent components, descriptor-rooted open. |
| T-02-17-02 | Elevation of Privilege | Human H1 disposition | critical | mitigate | Human-only file plus read-only exact-binding validator. |
| T-02-17-03 | Repudiation | Four reports | high | mitigate | Fresh role dispatches and identical full audited_commit. |
| T-02-17-04 | Tampering | Disposition-to-verdict | high | mitigate | Executable contract enforces approved score 35/35 must-haves verified versus gaps_found. |
| T-02-17-SC | Tampering | Supply chain | low | accept | No install or dependency change. |
</threat_model>

<verification>Readiness/decision validators, exact 72/150/145 phase gate and executable four-report same-commit/disposition contract all pass.</verification>

<source_coverage_audit>
GOAL, TS-03/TS-04/TS-05, RESEARCH custody/report requirements, REVIEW findings and CONTEXT D-01..D-16 are covered. No deferred idea, publication authority, ROADMAP or STATE mutation is included.
</source_coverage_audit>

<success_criteria>
- Readiness uses canonical oracle stdout and absolute descriptor-rooted 02-16 summary binding.
- Independent decision validates exactly seven named responses and local-only disposition.
- Four same-commit reports satisfy the disposition-aware closure contract.
</success_criteria>

<output>Create `.planning/phases/02-deterministic-measurement-spine/02-17-SUMMARY.md` when done.</output>
