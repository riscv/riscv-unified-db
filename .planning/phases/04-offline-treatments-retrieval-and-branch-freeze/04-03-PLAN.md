---
phase: 04-offline-treatments-retrieval-and-branch-freeze
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - experiments/specchoice-v1.3.2/src/specchoice_treatments/__init__.py
  - experiments/specchoice-v1.3.2/src/specchoice_treatments/retrieval.py
  - experiments/specchoice-v1.3.2/src/specchoice_treatments/cli.py
  - experiments/specchoice-v1.3.2/config/treatments/lexical-retrieval-contract-v1.json
  - experiments/specchoice-v1.3.2/reports/h3/test-only-retrieval-contract-v1.json
  - experiments/specchoice-v1.3.2/tests/test_treatments_retrieval.py
autonomous: true
requirements:
  - H2-02
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05/D-06: the retrieval proof uses only the Wave 2 corpus of at least three complete, distinct, synthetic pairs and a synthetic target; every root and item is test_only=true and count_eligible=false."
    - "D-07: the ranking query is exactly frozen target source_text; recursive presence of case identity, gold, frame, primary family, decisive axes, relevance, final disposition, or other forbidden authority fields rejects the input before tokenization."
    - "D-08/H2-02: at least two distinct eligible pairs returns exactly two complete pair records and their scores, ordered by full-precision cosine descending then pair_id ascending, including zero-score ties and with no similarity threshold."
    - "D-08: zero or one distinct eligible pair raises INSUFFICIENT_RETRIEVAL_PAIRS and exposes no partial ranking; duplicate IDs and incomplete pair sides are invalid rather than silently deduplicated."
    - "The closed lexical config fixes NFC then casefold tokenization, the regex, pair-document field order/separator, raw term frequency, smoothed IDF, L2 cosine zero-vector policy, score serialization, and tie rule; equivalent input/member order reproduces identical output."
    - "D-16: the sole Phase 4 command is verify-retrieval-contract with required --target, --corpus, and --config relative-path flags; it emits canonical JSON to stdout and rejects every non-test-only or Phase 3 input before ranking."
    - "FLAGGED ASSUMPTION — H2-02 unclassified probe: the frozen test contract requires a non-empty target source_text and at least two distinct complete pair IDs; this is grounded in D-05/D-08/D-09, but the probe supplied no class, so verification must retain this flag rather than silently declaring the probe resolved."
  artifacts:
    - path: experiments/specchoice-v1.3.2/src/specchoice_treatments/retrieval.py
      provides: "Closed standard-library pair-level TF-IDF/cosine validation, ranking, and report construction."
      exports: [RetrievalContractError, RetrievedPair, load_retrieval_contract_v1, validate_test_only_target_v1, validate_test_only_corpus_v1, tokenize_retrieval_text_v1, rank_complete_pairs_v1, verify_retrieval_contract_v1, build_retrieval_report_v1]
    - path: experiments/specchoice-v1.3.2/src/specchoice_treatments/cli.py
      provides: "The only Phase 4 CLI surface: test-only retrieval-contract verification."
      exports: [build_parser, main]
    - path: experiments/specchoice-v1.3.2/config/treatments/lexical-retrieval-contract-v1.json
      provides: "Frozen token, TF, IDF, cosine, zero-vector, serialization, document-construction, and ordering rules."
    - path: experiments/specchoice-v1.3.2/reports/h3/test-only-retrieval-contract-v1.json
      provides: "Canonical expected and observed target-dependent top-two results with exact config/target/corpus bindings."
    - path: experiments/specchoice-v1.3.2/tests/test_treatments_retrieval.py
      provides: "H2-02 boundary, tie, zero, leakage, test-only, CLI, filesystem, and no-network proofs."
  key_links:
    - from: experiments/specchoice-v1.3.2/src/specchoice_treatments/cli.py
      to: experiments/specchoice-v1.3.2/src/specchoice_treatments/retrieval.py
      via: "verify-retrieval-contract calls one strict library entry point and writes canonical stdout"
      pattern: "verify_retrieval_contract_v1"
    - from: experiments/specchoice-v1.3.2/src/specchoice_treatments/retrieval.py
      to: experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-complete-pairs-v1.json
      via: "descriptor-bound canonical read followed by test_only/count_eligible gate before tokenization"
      pattern: "test_only|count_eligible"
    - from: experiments/specchoice-v1.3.2/reports/h3/test-only-retrieval-contract-v1.json
      to: experiments/specchoice-v1.3.2/prompts/treatments/prompt-bundle-manifest-v1.json
      via: "top-two pair IDs must equal C's preregistered retrieved_contract_pair_ids"
      pattern: "retrieved_contract_pair_ids"
  prohibitions:
    - statement: "Retrieval must not use gold, case identity, DelegationFrame, family, decisive-axis, relevance, or final-disposition information as query material."
      status: unverified
      verification: flagged
    - statement: "The synthetic retrieval proof must not become H2 authority, count evidence, production retrieval, or model evidence."
      status: unverified
      verification: flagged
    - statement: "The frozen lexical treatment must not silently become embedding-, vector-store-, query-expansion-, or learned-ranking behavior."
      status: unverified
      verification: flagged
---

## Phase Goal

**As a** human RISC-V reviewer, **I want to** inspect the exact offline treatment contracts and authorize one immutable execution branch, **so that** no retrieval rank or model output can influence the frozen semantic and branch decisions.

<objective>
Prove the deterministic complete-pair TF-IDF/cosine retrieval contract through the sole test-only CLI.

Purpose: Satisfy H2-02's algorithmic contract without reusing the insufficient Phase 3 natural corpus, authorizing retrieval, or creating a production/model-facing entry point.
Output: One frozen lexical config, pure retrieval module, one-command CLI, canonical verification report, and focused adversarial tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-CONTEXT.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-RESEARCH.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-PATTERNS.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-02-PLAN.md
@.planning/phases/03-human-reviewed-data-preregistration/03-03-SUMMARY.md
@.planning/phases/03-human-reviewed-data-preregistration/03-04-SUMMARY.md
@experiments/specchoice-v1.3.2/src/specchoice_data/relevance.py
@experiments/specchoice-v1.3.2/src/specchoice_data/cli.py
@experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
@experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py
@experiments/specchoice-v1.3.2/phase3/data-authority-v1.json
@experiments/specchoice-v1.3.2/prompts/treatments/prompt-bundle-manifest-v1.json

<interfaces>
- Reuse the recursive forbidden-field boundary from `specchoice_data.relevance`; extend it with the exact D-07 field set rather than adding a new generic sanitizer.
- Read config/target/corpus as descriptor-bound canonical files with `read_authoritative_file` plus `decode_strict_json`; raw target `source_text` is the only query value.
- Reuse `canonical_json_bytes` and `sha256_bytes` for config/report identities; no external numerical or retrieval library is permitted.
- Follow `specchoice_data.cli` required-subparser, handler, canonical stdout, stderr stable error, and exit-2 pattern, but expose exactly one subcommand.
</interfaces>
</context>

## Artifacts this phase produces

- Symbols: `RetrievalContractError`, `RetrievedPair`, `FORBIDDEN_QUERY_FIELDS`, `load_retrieval_contract_v1`, `validate_test_only_target_v1`, `validate_test_only_corpus_v1`, `tokenize_retrieval_text_v1`, `construct_pair_document_v1`, `rank_complete_pairs_v1`, `verify_retrieval_contract_v1`, `build_retrieval_report_v1`, CLI `_verify_retrieval_contract`, `build_parser`, and `main`.
- Lexical config fields: `schema_version`, `unicode_normalization=NFC`, `case_normalization=casefold`, `token_pattern=(?u)\\b\\w+\\b`, `pair_document_fields=[shared_structure,positive_source_text,contrast_source_text,discriminating_axes]`, `field_separator=LF`, `term_frequency=raw_count`, `inverse_document_frequency=ln((1+N)/(1+df))+1`, `vector_normalization=l2`, `zero_vector_cosine=0`, `ranking=cosine_desc_pair_id_asc`, `result_count=2`, `score_serialization=.17g`, and `contract_sha256`.
- Report/result fields: `schema_version`, `test_only`, `count_eligible`, `config_sha256`, `target_sha256`, `corpus_sha256`, `query_source_field=source_text`, `eligible_pair_count`, `results`, `pair_id`, `cosine_score`, `positive_source_text`, `contrast_source_text`, `ordering_rule`, `prompt_c_pair_ids_match`, and `report_sha256`.
- Stable errors: `RETRIEVAL_CONFIG_INVALID`, `RETRIEVAL_TARGET_INVALID`, `RETRIEVAL_CORPUS_INVALID`, `RETRIEVAL_PAIR_INCOMPLETE`, `RETRIEVAL_PAIR_ID_DUPLICATE`, `RETRIEVAL_QUERY_FIELD_FORBIDDEN`, `RETRIEVAL_TEST_ONLY_REQUIRED`, `INSUFFICIENT_RETRIEVAL_PAIRS`, `RETRIEVAL_PROMPT_SELECTION_MISMATCH`, and `RETRIEVAL_CLI_INPUT_INVALID`.
- CLI command/flags: `verify-retrieval-contract --target <relative-json> --corpus <relative-json> --config <relative-json>`; canonical report is stdout, failures are stderr plus exit 2. There is no output, retrieval-authority, model, provider, run, network, or H3 command.
- Files: `src/specchoice_treatments/{retrieval.py,cli.py}`, `config/treatments/lexical-retrieval-contract-v1.json`, `reports/h3/test-only-retrieval-contract-v1.json`, and `tests/test_treatments_retrieval.py`.

<tasks>

<task type="tracer" tdd="true">
  <name>04-03-01: Rank one synthetic target through the sole CLI to two whole pairs</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_treatments/retrieval.py, experiments/specchoice-v1.3.2/src/specchoice_treatments/cli.py, experiments/specchoice-v1.3.2/config/treatments/lexical-retrieval-contract-v1.json, experiments/specchoice-v1.3.2/reports/h3/test-only-retrieval-contract-v1.json, experiments/specchoice-v1.3.2/tests/test_treatments_retrieval.py</files>
  <read_first>
    - .planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-CONTEXT.md
    - experiments/specchoice-v1.3.2/src/specchoice_data/relevance.py
    - experiments/specchoice-v1.3.2/src/specchoice_data/cli.py
    - experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py
    - experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-target-v1.json
    - experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-complete-pairs-v1.json
    - experiments/specchoice-v1.3.2/prompts/treatments/prompt-bundle-manifest-v1.json
  </read_first>
  <behavior>
    - Test 1: the frozen target and three-plus-pair corpus return exactly two complete pair records in expected order with serialized scores.
    - Test 2: changing only target source_text changes the expected ranking while corpus/config stay fixed.
    - Test 3: the CLI has exactly one subcommand, prints canonical report bytes, exits zero, and selected IDs equal C's prompt-contract selection.
  </behavior>
  <action>
1. Write failing end-to-end CLI tests before production files. Invoke `main` with the exact command/flags and compare stdout to `canonical_json_bytes(build_retrieval_report_v1(...))`.
2. Freeze canonical `lexical-retrieval-contract-v1.json` with every field/value listed above. Tokenize NFC-normalized then casefolded text with the exact regex; construct one document per complete pair by joining the four configured fields in order, sorting discriminating axes before joining. Fit vocabulary/document frequencies on eligible pair documents only.
3. Compute raw-count TF; compute IDF with natural log `ln((1+N)/(1+df))+1`; construct query using only target `source_text`; calculate L2 cosine with exact zero-vector score 0.0. Rank with the full unrounded float, serialize afterward using Python `format(score, ".17g")`, then order by negative score and ascending `pair_id` and take two.
4. Validate config/target/corpus exact keys, self-hashes, sorted/distinct IDs, complete positive+contrast content, root/item isolation flags, and at least two distinct eligible pairs before tokenization. The production contract fixture contains at least three. Never duplicate a single pair to satisfy the result count.
5. Implement the one-command argparse surface with descriptor-bound relative reads from the fixed experiment root and canonical stdout. Build the canonical report with exact bindings, whole pair records, scores, ordering rule, and assertion that result IDs equal Wave 2 C selection. Materialize the committed report from the same library result and prove byte equality in the test.
  </action>
  <acceptance_criteria>
    - H2-02 happy path returns exactly two distinct complete pairs and scores from a three-plus-pair test-only corpus.
    - Target-only text can change rankings; no non-target field is consulted or serialized into query construction.
    - Ranking uses full precision before `.17g` output and applies pair_id only after cosine equality.
    - CLI help/subparser inventory contains only verify-retrieval-contract and the three required relative-path flags.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_retrieval.RetrievalContractTests.test_cli_tracer_returns_exact_two_complete_pairs tests.test_treatments_retrieval.RetrievalContractTests.test_target_text_changes_rank tests.test_treatments_retrieval.RetrievalContractTests.test_report_matches_prompt_c_selection -q</automated>
  </verify>
  <done>One real offline command carries a frozen synthetic target through validation, standard-library TF-IDF/cosine, whole-pair ranking, and canonical output.</done>
</task>

<task type="auto" tdd="true">
  <name>04-03-02: Close zero/tie/insufficient/leakage boundaries and prove no production reachability</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_treatments/__init__.py, experiments/specchoice-v1.3.2/src/specchoice_treatments/retrieval.py, experiments/specchoice-v1.3.2/src/specchoice_treatments/cli.py, experiments/specchoice-v1.3.2/reports/h3/test-only-retrieval-contract-v1.json, experiments/specchoice-v1.3.2/tests/test_treatments_retrieval.py</files>
  <read_first>
    - experiments/specchoice-v1.3.2/src/specchoice_treatments/retrieval.py
    - experiments/specchoice-v1.3.2/src/specchoice_treatments/cli.py
    - experiments/specchoice-v1.3.2/config/treatments/lexical-retrieval-contract-v1.json
    - experiments/specchoice-v1.3.2/phase3/data-authority-v1.json
    - experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/candidate-inventory.json
    - experiments/specchoice-v1.3.2/tests/test_filesystem_boundary.py
  </read_first>
  <behavior>
    - Test 1: two or more zero-score pairs still return exactly two, and exact score ties use ascending pair_id independent of input order.
    - Test 2: zero/one distinct pair raises INSUFFICIENT_RETRIEVAL_PAIRS and returns/writes no partial list; duplicate IDs and incomplete pair sides have distinct errors.
    - Test 3: empty target and every recursive forbidden query field, non-test/count-eligible input, Phase 3 authority/corpus path, traversal/absolute/symlink path, and unknown command fail before ranking.
    - Test 4: patching socket connection primitives to raise observes zero calls around success and failure CLI paths.
  </behavior>
  <action>
1. Add the zero/tie/insufficient/order/leakage/path/CLI/network adversarial tests before expanding the implementation.
2. Enforce D-08 at the pre-ranking boundary: fewer than two distinct eligible IDs raises only `INSUFFICIENT_RETRIEVAL_PAIRS`; never return one, never add a threshold, and never drop zero scores. Reject duplicates before the distinct count rather than silently coalescing them.
3. Recursively reject the exact forbidden query names `case_id`, `fixture_id`, `gold`, `gold_label`, `delegation_frame`, `frame`, `primary_family`, `decisive_axes`, `relevance`, `relevant_pair_ids`, `final_disposition`, `final_status`, `parameter_status`, `rank`, `score`, `similarity`, and `top_k` anywhere in target/query material. Require non-empty target `source_text`; retain the H2-02 unclassified-probe assumption in the plan/summary.
4. Reject corpus/target roots or items unless both isolation flags have exact false/true values, and explicitly test the accepted Phase 3 authority and candidate inventory as forbidden inputs. Validate all paths with the existing relative descriptor boundary; no absolute, parent traversal, symlink, or special file may be read.
5. Introspect `build_parser()` and assert its subparser choices equal the singleton set containing `verify-retrieval-contract`. Exercise representative unknown execution-like commands under patched socket connection functions and prove argparse exits 2 before any network observation. Export only the named retrieval/library/CLI symbols.
  </action>
  <acceptance_criteria>
    - 0/1/2/3-pair boundaries, zero vectors, exact ties, duplicate IDs, and input order have the frozen outcomes with no partial ranking.
    - Empty/null/forbidden/non-test/Phase-3 inputs are rejected before tokenization and emit no canonical success report.
    - CLI and runtime tests prove there is no authoritative/production retrieval entry point and no network activity.
    - The complete retrieval, prompt, frame, canonical, and filesystem test partition passes without dependency installation.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_retrieval tests.test_filesystem_boundary -q</automated>
  </verify>
  <done>The sole CLI proves the complete H2-02 retrieval contract on isolated test data while every insufficient, leaking, authority-bearing, production, and network path fails closed.</done>
</task>

</tasks>

<threat_model>
## Assets

| Asset | Integrity or safety property |
|---|---|
| Target source text | It is the only ranking query signal. |
| Complete pair corpus | Pair sides remain atomic, synthetic, test-only, and non-counting. |
| Ranking/config/report | Formula, full-precision order, ties, scores, and bindings are reproducible. |
| CLI surface | Only contract verification is reachable, with no production/network route. |

## Trust Boundaries

| Boundary | Untrusted input crossing it |
|---|---|
| config/target/corpus files -> validator | Malformed, authority-bearing, symlinked, duplicate, incomplete, or non-test data. |
| validated text -> ranker | Leakage fields or insertion order could influence scores/ties. |
| CLI argv -> library/stdout | Unknown commands/paths could expose a broader retrieval or network surface. |

## STRIDE Threat Register — ASVS L1, block on High

| Threat ID | Category | Asset / abuse case | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|---|
| T-04-03-01 | Tampering | Gold/frame/family/relevance fields leak into ranking. | high | mitigate | Recursive exact forbidden-field gate before tokenization plus mutation tests. |
| T-04-03-02 | Elevation of Privilege | Synthetic test corpus/result is treated as H2 or experiment authority. | high | mitigate | Root/item isolation flags, physical separation, report flags, and Phase 3 input rejection. |
| T-04-03-03 | Tampering | Threshold, partial list, duplicate pair, or insertion order changes top two. | high | mitigate | Exact 0/1/2/3, zero, tie, duplicate, and permutation tests against D-08. |
| T-04-03-04 | Elevation of Privilege | CLI expands into authoritative retrieval or model execution. | high | mitigate | Singleton subparser inventory, fixed flags, and unknown-command tests. |
| T-04-03-05 | Information Disclosure | A crafted command causes local network activity. | high | mitigate | No network imports plus patched socket connection sentinels on success/failure paths. |

No High threat is accepted, transferred, or left unresolved.
</threat_model>

<verification>
Quick: `cd experiments/specchoice-v1.3.2 && PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_retrieval -q`.

Cumulative Wave 3: `cd experiments/specchoice-v1.3.2 && PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_data_admission tests.test_data_splits tests.test_data_relevance tests.test_data_h2 tests.test_treatments_frame tests.test_treatments_prompts tests.test_treatments_retrieval tests.test_canonical tests.test_filesystem_boundary -q && ruff check --isolated src/specchoice_treatments tests/test_treatments_frame.py tests/test_treatments_prompts.py tests/test_treatments_retrieval.py`.

Both commands are intended to remain below the 10-second feedback ceiling in `04-VALIDATION.md`.
</verification>

<success_criteria>
- D-05 through D-08 and D-16 are implemented exactly, including top-two zero/tie behavior and insufficient-pair no-partial failure.
- H2-02 is proven with frozen standard-library math, complete atomic pairs, target dependence, deterministic ties, scores, and exact C-selection linkage.
- The unclassified fallback probe remains visibly flagged, while all classified boundary/empty/order/precision cases have concrete tests.
- No learned component, external dependency, production retrieval entry point, network action, authority mutation, or model surface exists.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 4 | Deterministic offline complete-pair retrieval proof | 04-03 | COVERED | Sole test-only CLI produces exact top two. |
| REQ | H2-02 | Frozen TF-IDF/cosine, target-dependent rank, deterministic pair_id tie, no learned retrieval | 04-03 | COVERED | Formula, edges, scores, and boundaries are explicit. |
| RESEARCH | lexical retrieval | Freeze all token/document/TF/IDF/cosine/serialization choices | 04-03 | COVERED | Planner-owned A1 is resolved in the canonical config. |
| CONTEXT | D-05..D-08 | Synthetic three-pair proof and target-only exact-two behavior | 04-03 | COVERED | Both tasks cite these decisions. |
| CONTEXT | D-16 | Sole test-only verify-retrieval-contract CLI | 04-03 | COVERED | Singleton parser and rejection tests enforce it. |

<output>
Create `.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-03-SUMMARY.md` when done, recording the lexical contract hash, exact scores/order/target variants, report hash, CLI inventory, rejection/no-network evidence, and retained flagged assumption.
</output>
