---
phase: 04-offline-treatments-retrieval-and-branch-freeze
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - experiments/specchoice-v1.3.2/src/specchoice_treatments/__init__.py
  - experiments/specchoice-v1.3.2/src/specchoice_treatments/prompts.py
  - experiments/specchoice-v1.3.2/config/treatments/prompt-contract-v1.json
  - experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-target-v1.json
  - experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-complete-pairs-v1.json
  - experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-a-v1.json
  - experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-b-v1.json
  - experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-c-v1.json
  - experiments/specchoice-v1.3.2/prompts/treatments/system-a-v1.txt
  - experiments/specchoice-v1.3.2/prompts/treatments/system-b-v1.txt
  - experiments/specchoice-v1.3.2/prompts/treatments/system-c-v1.txt
  - experiments/specchoice-v1.3.2/prompts/treatments/prompt-bundle-manifest-v1.json
  - experiments/specchoice-v1.3.2/tests/test_treatments_prompts.py
autonomous: true
requirements:
  - H1-01
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-09/D-10: one synthetic target, at least three complete synthetic pairs, and all A/B/C contract responses are explicitly test_only=true, count_eligible=false; responses also record origin=contract_fixture and model_generated=false."
    - "D-11: A/B/C prompt authority is the exact raw UTF-8/LF bytes and each prompt is hashed without normalization; response and manifest bytes equal existing NFC/sorted-key canonical JSON and every prompt/response has its own SHA-256."
    - "D-12: A and B use the same two fixed pair IDs; B and C share byte-identical frame/adjudication, target, guidance, decision-space, evidence, serialization, and pair-count sections; only A-vs-B frame/output presence and B-vs-C demonstration selection may differ."
    - "D-12: any unrelated shared-section delta, demonstration-count delta, reordered selection, or neutral padding produces TREATMENT_DIFF_NOT_ALLOWLISTED and no publishable manifest."
    - "D-13: every raw prompt records exact UTF-8 byte, Unicode code-point, logical LF-line, and frozen offline lexical-token counts; all provider token/budget fields are exactly not_applicable_red."
    - "Empty target text, fewer than three fixture pairs, non-distinct pair IDs, invalid origin flags, CR bytes, missing final LF, and non-canonical response/manifest JSON fail closed rather than producing partial artifacts."
  artifacts:
    - path: experiments/specchoice-v1.3.2/src/specchoice_treatments/prompts.py
      provides: "Named-section renderer, raw-byte publisher, response isolation, diff allowlist, canonical manifest, and offline accounting."
      exports: [PromptBundleError, render_prompt_sections_v1, render_treatment_prompt_v1, offline_lexical_token_count_v1, validate_contract_response_origin_v1, validate_treatment_diffs_v1, build_prompt_bundle_manifest_v1, write_offline_prompt_bundle_v1]
    - path: experiments/specchoice-v1.3.2/prompts/treatments/prompt-bundle-manifest-v1.json
      provides: "Canonical hashes, section identities, selections, allowed deltas, and offline count records for exact A/B/C bytes and responses."
    - path: experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-complete-pairs-v1.json
      provides: "Three or more visibly synthetic complete pairs used only for prompt/retrieval contract proof."
    - path: experiments/specchoice-v1.3.2/tests/test_treatments_prompts.py
      provides: "Raw/canonical byte, diff, isolation, empty, order, count, and exact-resume coverage."
  key_links:
    - from: experiments/specchoice-v1.3.2/src/specchoice_treatments/prompts.py
      to: experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
      via: "parse every human-authored response against the Wave 1 system/frame contract"
      pattern: "parse_treatment_response_v1"
    - from: experiments/specchoice-v1.3.2/prompts/treatments/system-b-v1.txt
      to: experiments/specchoice-v1.3.2/prompts/treatments/system-c-v1.txt
      via: "named-section hashes prove all sections except demonstrations are byte-identical"
      pattern: "allowed_differences"
    - from: experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-a-v1.json
      to: experiments/specchoice-v1.3.2/prompts/treatments/prompt-bundle-manifest-v1.json
      via: "canonical response file hash plus origin/model-generation isolation record"
      pattern: "contract_fixture|model_generated"
  prohibitions:
    - statement: "Human-authored contract responses must not be represented, admitted, or later reclassified as raw model output or experiment-run evidence."
      status: unverified
      verification: flagged
    - statement: "Prompt comparability must not be manufactured with semantically irrelevant padding, unequal demonstrations, repeated guidance, or hidden shared-section drift."
      status: unverified
      verification: flagged
---

## Phase Goal

**As a** human RISC-V reviewer, **I want to** inspect the exact offline treatment contracts and authorize one immutable execution branch, **so that** no retrieval rank or model output can influence the frozen semantic and branch decisions.

<objective>
Build and freeze the exact offline A/B/C prompt-and-response contract bundle.

Purpose: Make every intended treatment difference and every naturally occurring byte/lexical difference independently inspectable before retrieval verification or H3 authority.
Output: One closed prompt contract, one synthetic target, one three-pair corpus, three human contract responses, three raw prompt files, one canonical bundle manifest, and focused tests.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-CONTEXT.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-RESEARCH.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-PATTERNS.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-01-PLAN.md
@experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
@experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
@experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py
@experiments/specchoice-v1.3.2/src/specchoice_data/review.py

<interfaces>
- Consume Wave 1 `parse_treatment_response_v1(raw, target_raw)`; do not duplicate frame/adjudication validation.
- Use `sha256_bytes` directly on raw prompt bytes. Use `canonical_json_bytes` only for response/config/manifest JSON.
- Use `write_exact_descriptor_files(output_root, payloads)` for related prompt/manifest publication and byte-identical resume; do not precheck with pathname existence or create a custom writer.
- Markdown projection patterns are informative only; A/B/C prompt authority remains the three `.txt` byte streams.
</interfaces>
</context>

## Artifacts this phase produces

- Symbols: `PromptBundleError`, `PROMPT_SECTION_ORDER`, `render_prompt_sections_v1`, `render_treatment_prompt_v1`, `offline_lexical_token_count_v1`, `count_prompt_bytes_v1`, `validate_contract_response_origin_v1`, `validate_treatment_diffs_v1`, `build_prompt_bundle_manifest_v1`, and `write_offline_prompt_bundle_v1`.
- Prompt-contract fields: `schema_version`, `section_order`, `shared_guidance`, `frame_instructions`, `adjudication_instructions`, `output_schema`, `evidence_rules`, `demonstration_count=2`, `fixed_pair_ids`, `retrieved_contract_pair_ids`, `allowed_differences`, `offline_lexical_rule`, and `provider_fields=not_applicable_red`.
- Manifest fields: `schema_version`, `contract_sha256`, `target_sha256`, `corpus_sha256`, `prompt_records`, `response_records`, `section_hashes`, `pair_selection`, `structural_comparison`, `offline_accounting`, `provider_input_tokens`, `provider_output_tokens`, `maximum_output_tokens`, and `manifest_sha256`.
- Count fields per prompt: `utf8_byte_count`, `unicode_code_point_count`, `logical_line_count`, and `offline_lexical_token_count`; lexical rule is the count of Python standard-library `re.findall(r"(?u)\\b\\w+\\b", raw.decode("utf-8"))` matches over unnormalized raw prompt text.
- Stable errors: `PROMPT_CONTRACT_INVALID`, `PROMPT_TARGET_INVALID`, `PROMPT_PAIR_CORPUS_INVALID`, `PROMPT_PAIR_ID_DUPLICATE`, `PROMPT_RESPONSE_INVALID`, `CONTRACT_FIXTURE_EVIDENCE_FORBIDDEN`, `PROMPT_RAW_BYTES_INVALID`, `TREATMENT_DIFF_NOT_ALLOWLISTED`, `PROMPT_ACCOUNTING_INVALID`, and `PROMPT_BUNDLE_WRITE_INVALID`.
- Files: `src/specchoice_treatments/prompts.py`, `config/treatments/prompt-contract-v1.json`, `fixtures/treatments/{synthetic-target-v1.json,synthetic-complete-pairs-v1.json,contract-response-a-v1.json,contract-response-b-v1.json,contract-response-c-v1.json}`, `prompts/treatments/{system-a-v1.txt,system-b-v1.txt,system-c-v1.txt,prompt-bundle-manifest-v1.json}`, and `tests/test_treatments_prompts.py`.
- CLI flags: none; prompt rendering and publication remain library-only.

## Task-local file ownership

- **04-02-01 — renderer/config contract:** owns `prompt-contract-v1.json`, `synthetic-target-v1.json`, the renderer/config/target-validation symbols in `prompts.py`, and only the three `PromptBundleTests` methods listed in Task 01's `<verify>`.
- **04-02-02 — synthetic fixture/response contract:** owns `synthetic-complete-pairs-v1.json`, the three `contract-response-*-v1.json` files, the corpus/response-isolation symbols in `prompts.py`, and only the three `PromptBundleTests` methods listed in Task 02's `<verify>`. It does not modify the Wave 1 parser or either Task 01 contract file.
- **04-02-03 — prompt publication:** owns the three raw prompt files, `prompt-bundle-manifest-v1.json`, the accounting/diff/manifest/writer symbols in `prompts.py`, the final prompt exports in `__init__.py`, and the remaining publication/diff/accounting methods exercised by Task 03's full-module `<verify>`.
- `prompts.py` and `test_treatments_prompts.py` are shared physical files with the named symbol/class regions above as disjoint ownership boundaries. A task must not edit another task's region, config, fixtures, or published outputs; all other listed files have exactly one task owner.

<tasks>

<task type="tracer" tdd="true">
  <name>04-02-01: Render one complete named-section A/B/C bundle in memory</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_treatments/prompts.py, experiments/specchoice-v1.3.2/config/treatments/prompt-contract-v1.json, experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-target-v1.json, experiments/specchoice-v1.3.2/tests/test_treatments_prompts.py</files>
  <ownership>Own only the renderer/config/target-validation symbols in `prompts.py`, the config and target files, and the three `PromptBundleTests` methods named in this task's verify command.</ownership>
  <read_first>
    - .planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-CONTEXT.md
    - experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
    - experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
    - experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py
    - /Users/zhdeng/Downloads/LFX_RISCV_SpecChoice_v1.3.2_frozen_execution_baseline.md
  </read_first>
  <behavior>
    - Test 1: one synthetic non-empty target yields A/B/C raw UTF-8 with LF-only endings and a fixed named-section order.
    - Test 2: A omits frame instructions/output; B/C share identical frame/adjudication/output/evidence sections; every system has exactly two demonstration slots.
    - Test 3: empty/null target, CR bytes, missing final LF, unknown section/config keys, or non-canonical config fail before bundle construction.
  </behavior>
  <action>
1. Start with failing in-memory renderer tests in the three owned `PromptBundleTests` methods named by `<verify>`; no pair/response fixture or prompt artifact is written by this task.
2. Freeze canonical `prompt-contract-v1.json`. Use section order `shared_guidance`, `demonstrations`, `target`, `frame_instructions`, `adjudication_instructions`, `output_schema`, `evidence_rules`. A renders an empty/omitted frame section according to the manifest schema and an A-only output schema; B/C render byte-identical frame, adjudication, output, and evidence content from this one config.
3. Encode the frozen shared guidance from baseline section 11.1, exact three-axis fragment, strict adjudication decision space, verbatim evidence rule, and two demonstration slots. Hold the target raw source text and all shared sections byte-identical. Do not introduce token equalization text.
4. Define raw prompt validity as UTF-8 decodable, CR-free, non-empty, exactly one final LF, and concatenated only in `PROMPT_SECTION_ORDER`; section hashes are over each exact section byte string and prompt hashes are over the final exact bytes.
5. Create `synthetic-target-v1.json` with exact keys `schema_version`, `target_id`, `source_text`, `test_only`, `count_eligible`, and `target_sha256`; require non-empty source text, `test_only=true`, `count_eligible=false`, and a self-consistent canonical hash. Text must be obviously contract-only and contain no RISC-V semantics. Limit `prompts.py` edits to the renderer/config/target-validation symbols assigned above; Task 03 owns final `__init__.py` exports, and this task adds no command surface.
  </action>
  <acceptance_criteria>
    - One renderer call produces three complete in-memory raw prompts with two demonstrations and exact section identities.
    - A/B and B/C contracts differ only in the sections permitted by D-12; no renderer branch may modify a shared section.
    - Target and config empty/null/encoding/key boundaries fail before prompt hashes are emitted.
    - No file is published until later tasks supply and validate the complete fixture/response set.
    - Only the renderer/config/target files and named renderer test region are changed; pair/response fixture, publication, and export regions remain untouched.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_prompts.PromptBundleTests.test_named_section_tracer_renders_three_systems tests.test_treatments_prompts.PromptBundleTests.test_frame_and_shared_sections_are_exact tests.test_treatments_prompts.PromptBundleTests.test_target_and_raw_text_boundaries_fail_closed -q</automated>
  </verify>
  <done>The exact A/B/C section architecture is proven in memory from one closed config and one visibly synthetic target.</done>
</task>

<task type="auto" tdd="true">
  <name>04-02-02: Freeze complete-pair and human contract-response isolation</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_treatments/prompts.py, experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-complete-pairs-v1.json, experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-a-v1.json, experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-b-v1.json, experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-c-v1.json, experiments/specchoice-v1.3.2/tests/test_treatments_prompts.py</files>
  <ownership>Own only the corpus/response-isolation symbols in `prompts.py`, the four synthetic fixture/response files, and the three `PromptBundleTests` methods named in this task's verify command.</ownership>
  <read_first>
    - experiments/specchoice-v1.3.2/config/treatments/prompt-contract-v1.json
    - experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-target-v1.json
    - experiments/specchoice-v1.3.2/config/treatments/delegation-frame-contract-v1.json
    - experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
    - experiments/specchoice-v1.3.2/src/specchoice_treatments/prompts.py
    - experiments/specchoice-v1.3.2/data/preregistration/candidates-v1/candidate-inventory.json
  </read_first>
  <behavior>
    - Test 1: at least three distinct, complete, synthetic pairs validate with test_only true and count_eligible false; A/B fixed selection and C expected retrieval selection each contain two distinct whole pair IDs.
    - Test 2: A/B/C human responses parse under Wave 1 and each preserves origin contract_fixture and model_generated false.
    - Test 3: changing either isolation flag, using a Phase 3 ID/path/semantic field, making a pair incomplete, duplicating an ID, or admitting a contract response to run evidence raises the declared code.
  </behavior>
  <action>
1. Add table-driven cases only to the three owned `PromptBundleTests` methods named by `<verify>` for corpus completeness, pair identity, response origin, and forbidden evidence admission before authoring fixtures.
2. Author `synthetic-complete-pairs-v1.json` as canonical JSON with at least three sorted distinct `pair_id` records. Each pair has exact keys `pair_id`, `shared_structure`, `positive_source_text`, `contrast_source_text`, `discriminating_axes`, `test_only`, and `count_eligible`; every semantic text is obviously generic contract text, both sides are non-empty, and every record has `test_only=true` and `count_eligible=false`.
3. Require the corpus root itself to carry `schema_version`, `test_only=true`, `count_eligible=false`, `pairs`, and `corpus_sha256`. Reject any Phase 3 natural pair ID, accepted/preregistration path, RISC-V semantic authority field, gold/frame/family/relevance/final-disposition field, or attempt to count/admit the corpus.
4. Author one canonical response per system against the synthetic target. A has no frame; B/C have exact three-axis frames with real target byte spans. All three have `origin=contract_fixture`, `model_generated=false`, `test_only=true`, and `count_eligible=false`. Keep `schema.py` and the Wave 1 contract files unchanged: `validate_contract_response_origin_v1` must validate the exact fixture envelope and pass only its Wave 1 response projection to `parse_treatment_response_v1`, so the added isolation keys cannot weaken or silently extend the closed parser contract.
5. Implement/verify only the assigned corpus/response-isolation symbols in `prompts.py`. Any attempt to label or pass a contract response as raw/model/run evidence raises `CONTRACT_FIXTURE_EVIDENCE_FORBIDDEN`; the function never creates an alternate evidence representation. Do not edit renderer/config/accounting/diff/publication symbols or any prompt output.
  </action>
  <acceptance_criteria>
    - The complete fixture corpus has at least three distinct whole pairs, while each treatment selects exactly two distinct IDs.
    - Every target/pair/response isolation marker is exact and cannot be overridden by a caller.
    - A/B/C responses pass the same Wave 1 parser contract and their canonical bytes/hashes reproduce.
    - Phase 3 natural data and human contract responses cannot enter eligibility, H2 authority, raw model output, or run evidence.
    - Only the four owned fixture files plus the assigned isolation symbol/test regions change; Wave 1 schema/config and prompt publication files remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_prompts.PromptBundleTests.test_three_complete_pairs_and_two_pair_selections tests.test_treatments_prompts.PromptBundleTests.test_contract_responses_parse_and_remain_human_authored tests.test_treatments_prompts.PromptBundleTests.test_fixture_or_response_authority_escalation_is_rejected -q</automated>
  </verify>
  <done>The synthetic pair bank and all human-authored responses are complete contract fixtures that cannot become count-, authority-, or model-evidence inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>04-02-03: Publish exact prompt bytes, allowlisted diffs, hashes, and offline counts</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_treatments/__init__.py, experiments/specchoice-v1.3.2/src/specchoice_treatments/prompts.py, experiments/specchoice-v1.3.2/prompts/treatments/system-a-v1.txt, experiments/specchoice-v1.3.2/prompts/treatments/system-b-v1.txt, experiments/specchoice-v1.3.2/prompts/treatments/system-c-v1.txt, experiments/specchoice-v1.3.2/prompts/treatments/prompt-bundle-manifest-v1.json, experiments/specchoice-v1.3.2/tests/test_treatments_prompts.py</files>
  <ownership>Own only final prompt exports in `__init__.py`, the accounting/diff/manifest/writer symbols in `prompts.py`, the four published prompt outputs, and the remaining publication/diff/accounting `PromptBundleTests` methods exercised by this task's full-module verify command.</ownership>
  <read_first>
    - experiments/specchoice-v1.3.2/src/specchoice_treatments/prompts.py
    - experiments/specchoice-v1.3.2/config/treatments/prompt-contract-v1.json
    - experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-target-v1.json
    - experiments/specchoice-v1.3.2/fixtures/treatments/synthetic-complete-pairs-v1.json
    - experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-a-v1.json
    - experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-b-v1.json
    - experiments/specchoice-v1.3.2/fixtures/treatments/contract-response-c-v1.json
    - experiments/specchoice-v1.3.2/src/specchoice_evidence/filesystem.py
  </read_first>
  <behavior>
    - Test 1: fresh construction and exact resume yield identical three prompt byte streams and canonical manifest.
    - Test 2: A/B allowed deltas are exactly frame_instructions and output_schema; B/C allowed delta is exactly demonstrations; every other section hash matches and both sides select two complete pairs.
    - Test 3: raw SHA/counts equal independent recomputation, provider fields equal not_applicable_red, and a padding/unrelated/reordered/divergent target fails with no partial publication.
  </behavior>
  <action>
1. Add only the remaining publication/diff/accounting `PromptBundleTests` methods owned by this task, using temporary roots and independent `len`, decode, LF, regex, and SHA-256 calculations.
2. Implement the frozen offline lexical count exactly as listed in the artifacts section over decoded raw prompt text; `utf8_byte_count=len(raw)`, `unicode_code_point_count=len(text)`, and `logical_line_count=raw.count(b"\n")` because valid prompts have exactly one final LF and no CR. Do not normalize prompt text before any count or hash.
3. Build a canonical manifest that records contract/target/corpus hashes, raw prompt hashes/counts, canonical response file hashes, two pair IDs per system, every named-section hash, and structural comparisons. Allow exactly A-vs-B `{frame_instructions, output_schema}` and B-vs-C `{demonstrations}`; compare target, guidance, adjudication, evidence, serialization, and demonstration count explicitly.
4. Set `provider_input_tokens`, `provider_output_tokens`, and `maximum_output_tokens` to string `not_applicable_red`; reject numbers, null, planned values, tokenizer metadata, or caller overrides. Record no provider/model identity or configuration.
5. Publish the three raw prompt files and canonical manifest as one `write_exact_descriptor_files` batch. Validate all inputs and diffs before the call; allow byte-identical resume and map any collision, partial destination, symlink, or divergent leaf to `PROMPT_BUNDLE_WRITE_INVALID` without replacing prior bytes. Export the final named prompt-library symbols from `__init__.py`, but do not alter the renderer/config, fixture/isolation, or source fixture regions owned by Tasks 01 and 02.
  </action>
  <acceptance_criteria>
    - The committed A/B/C `.txt` files equal freshly rendered raw bytes and every manifest SHA/count equals independent recomputation.
    - A/B share fixed pair IDs; B/C schemas and shared sections are byte-identical; only the exact allowlisted treatment sections differ.
    - No padding, silent normalization, provider-token surrogate, contract-response promotion, partial write, or divergent resume can validate.
    - The complete Wave 2 suite and predecessor/canonical/filesystem regressions pass offline.
    - Only the final export, accounting/diff/manifest/writer, publication test, and four prompt-output regions change; frozen config/target/pair/response inputs remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_prompts tests.test_treatments_frame tests.test_canonical tests.test_filesystem_boundary -q</automated>
  </verify>
  <done>Exact A/B/C prompt bytes, contract responses, treatment-only diffs, raw hashes, and offline counts are immutable, reproducible, and visibly non-model evidence.</done>
</task>

</tasks>

<threat_model>
## Assets

| Asset | Integrity or safety property |
|---|---|
| Raw A/B/C prompt bytes | Treatment comparisons use exact bytes, not normalized projections. |
| Human contract responses | They remain fixtures and never become model/run evidence. |
| Diff/count manifest | It fully discloses the permitted intervention and natural size differences. |

## Trust Boundaries

| Boundary | Untrusted input crossing it |
|---|---|
| config/target/pair fixtures -> renderer | Caller-controlled fields could alter shared prompt sections or pair count. |
| rendered sections -> raw files/manifest | A projection could hide byte drift or normalize authority bytes. |
| contract responses -> later evidence consumers | Human fixtures could be relabeled as model output. |

## STRIDE Threat Register — ASVS L1, block on High

| Threat ID | Category | Asset / abuse case | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|---|
| T-04-02-01 | Tampering | A non-treatment shared-section change is concealed inside a prompt. | high | mitigate | Named-section raw hashes, exact allowlist, independent mutation tests, and batch validation before publication. |
| T-04-02-02 | Elevation of Privilege | Synthetic corpus or response is promoted into eligibility or model evidence. | high | mitigate | Closed isolation fields, physical fixture separation, recursive forbidden-field checks, and rejection tests. |
| T-04-02-03 | Repudiation | Natural prompt-length changes are hidden or equalized. | high | mitigate | Exact byte/code-point/line/lexical counts plus padding and unequal-count adversarial tests. |
| T-04-02-04 | Tampering | Canonical projection or Unicode normalization changes raw prompt authority. | high | mitigate | Hash/count untouched `.txt` bytes; canonicalization is limited to JSON records and tested separately. |
| T-04-02-05 | Tampering | Partial/divergent output files produce a mixed bundle. | high | mitigate | Existing descriptor-held `write_exact_descriptor_files` exact-resume boundary plus filesystem regressions. |

No High threat is accepted, transferred, or left unresolved.
</threat_model>

<verification>
Quick: `cd experiments/specchoice-v1.3.2 && PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_prompts tests.test_canonical -q`.

Cumulative Wave 2: `cd experiments/specchoice-v1.3.2 && PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_data_admission tests.test_data_splits tests.test_data_relevance tests.test_data_h2 tests.test_treatments_frame tests.test_treatments_prompts tests.test_canonical tests.test_filesystem_boundary -q && ruff check --isolated src/specchoice_treatments tests/test_treatments_frame.py tests/test_treatments_prompts.py`.

Both commands are intended to remain below the 10-second feedback ceiling in `04-VALIDATION.md`.
</verification>

<success_criteria>
- D-09 through D-13 are implemented exactly and cited in the task actions.
- A/B/C artifacts reproduce byte-for-byte from closed offline inputs and reveal only intended treatment changes.
- Contract fixtures remain human-authored, test-only, non-counting, and unusable as model/run evidence.
- No provider tokenizer, model configuration, command surface, dependency, network action, or external mutation exists.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 4 | Replayable offline A/B/C treatment bundle | 04-02 | COVERED | Raw prompts, responses, manifest, diff, and counts are complete. |
| REQ | H1-01 | B/C exact frame outputs remain inspectable | 04-02 | COVERED | Human B/C responses run through Wave 1 parser. |
| RESEARCH | prompt bundle | Raw authority, canonical JSON projection, allowlisted diffs, standard-library counts | 04-02 | COVERED | No new dependency or provider tokenizer. |
| CONTEXT | D-09..D-13 | Synthetic isolation, raw hashes, structural equality, offline accounting | 04-02 | COVERED | Every decision is cited in must-haves/actions. |

<output>
Create `.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-02-SUMMARY.md` when done, recording every raw/canonical artifact hash, exact allowed deltas, prompt/response counts, test results, and contract-fixture isolation proof.
</output>
