---
phase: 04-offline-treatments-retrieval-and-branch-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/specchoice-v1.3.2/src/specchoice_treatments/__init__.py
  - experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
  - experiments/specchoice-v1.3.2/config/treatments/delegation-frame-contract-v1.json
  - experiments/specchoice-v1.3.2/config/treatments/frame-advisory-patterns-v1.json
  - experiments/specchoice-v1.3.2/fixtures/treatments/frame-source-v1.txt
  - experiments/specchoice-v1.3.2/fixtures/treatments/frame-response-b-valid-v1.json
  - experiments/specchoice-v1.3.2/fixtures/treatments/frame-response-adversarial-v1.json
  - experiments/specchoice-v1.3.2/tests/test_treatments_frame.py
autonomous: true
requirements:
  - H1-01
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "H1-01: a valid B or C contract response contains exactly authority, choice_object, and choice_space_origin; authority and choice_space_origin accept their frozen unknown value, and no fourth axis is accepted."
    - "H1-01: every axis owns one non-empty [start_byte,end_byte) span whose UTF-8 decoding equals the exact raw target byte slice; equal or touching spans remain independent axis records and are never merged."
    - "System A rejects a DelegationFrame field, while B and C require it; null, empty, non-UTF-8, out-of-range, boolean offsets, missing keys, extra keys, duplicate JSON keys, and invalid enums fail closed with stable codes."
    - "When multiple invalid fields compare equally, diagnostics use Diagnostic.sort_key ordering; JSON member order never changes the canonical result."
    - "FRAME_COMBINATION_REQUIRES_REVIEW is warning-only: it cannot change any parsed axis, evidence span, adjudication value, or parser validity."
  artifacts:
    - path: experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
      provides: "Closed A/B/C response parser, frozen frame enums, raw-byte span validation, and advisory evaluation."
      exports: [TreatmentContractError, ParsedTreatmentResponse, parse_treatment_response_v1, validate_delegation_frame_v1, validate_source_span_v1, evaluate_frame_advisories_v1]
    - path: experiments/specchoice-v1.3.2/config/treatments/delegation-frame-contract-v1.json
      provides: "Canonical closed response, frame, adjudication, enum, and span contract."
    - path: experiments/specchoice-v1.3.2/config/treatments/frame-advisory-patterns-v1.json
      provides: "Preregistered non-blocking frame-combination warnings."
    - path: experiments/specchoice-v1.3.2/tests/test_treatments_frame.py
      provides: "H1-01 happy-path, boundary, adjacency, empty, ordering, UTF-8, and advisory proofs."
  key_links:
    - from: experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
      to: experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py
      via: "decode_strict_json before every exact-key or enum check"
      pattern: "decode_strict_json"
    - from: experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
      to: experiments/specchoice-v1.3.2/fixtures/treatments/frame-source-v1.txt
      via: "source_sha256 plus exact raw [start_byte,end_byte) verification"
      pattern: "source_sha256|start_byte|end_byte"
    - from: experiments/specchoice-v1.3.2/config/treatments/frame-advisory-patterns-v1.json
      to: experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
      via: "closed advisory-pattern evaluation returning ordered warning diagnostics"
      pattern: "FRAME_COMBINATION_REQUIRES_REVIEW"
  prohibitions:
    - statement: "A frame advisory must not silently alter axis values, evidence, final disposition, or validity."
      status: unverified
      verification: flagged
    - statement: "A missing semantic value must not be coerced to unknown; unknown is accepted only when that literal belongs to the frozen enum."
      status: unverified
      verification: flagged
---

## Phase Goal

**As a** human RISC-V reviewer, **I want to** inspect the exact offline treatment contracts and authorize one immutable execution branch, **so that** no retrieval rank or model output can influence the frozen semantic and branch decisions.

<objective>
Freeze the closed DelegationFrame and response parser contract before any prompt or retrieval work.

Purpose: Establish the H1-01 semantic boundary that every later B/C contract fixture must pass, while preserving A as frame-free and keeping joint-frame advisories non-blocking.
Output: A dependency-free `specchoice_treatments` schema package, two canonical configs, raw source/response fixtures, and one focused unittest module.
</objective>

<execution_context>
@/Users/zhdeng/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhdeng/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-CONTEXT.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-RESEARCH.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-PATTERNS.md
@.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-VALIDATION.md
@experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py
@experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
@experiments/specchoice-v1.3.2/src/specchoice_data/relevance.py

<interfaces>
- Reuse `canonical_json_bytes(value) -> bytes` and `sha256_bytes(data) -> str`; do not create a second canonicalizer or hash helper.
- Reuse `decode_strict_json(raw) -> object` so duplicate keys and non-finite constants fail before field inspection.
- Reuse `Diagnostic(code, severity, fixture_id, field, ...)`, `Diagnostic.sort_key()`, and ordered diagnostics for stable multi-error output.
- Mirror `_validate_span` semantics from `strict_json.py`: integer means int-but-not-bool, byte ranges are half-open/non-empty, and decoded text must equal the authoritative raw slice.
</interfaces>
</context>

## Artifacts this phase produces

- Symbols: `TreatmentContractError`, `ParsedTreatmentResponse`, `REQUIRED_FRAME_AXES`, `FRAME_ENUMS`, `FRAME_COMBINATION_REQUIRES_REVIEW`, `parse_treatment_response_v1`, `validate_delegation_frame_v1`, `validate_source_span_v1`, and `evaluate_frame_advisories_v1`.
- Closed fields: response `schema_version`, `system`, `origin`, `model_generated`, `target_sha256`, optional/required `delegation_frame`, and `adjudication`; axis `value` and `evidence_span`; span `source_sha256`, `start_byte`, `end_byte`, and `text`; adjudication `surfaced`, `parameter_status`, `proposed_name`, `evidence_spans`, and `rationale`.
- Frozen enums: systems `A|B|C`; authority `implementation|ISA|software|platform|unknown`; choice object `direct_value|count|width|legal_set|access_mode|presence|extension_gate|other`; choice-space origin `implementation_selected|ISA_fixed|derived|not_applicable|unknown`; parameter status `accept|classify_out|review|null` subject to the canonical surfaced state.
- Stable errors/diagnostics: `TREATMENT_JSON_INVALID`, `TREATMENT_RESPONSE_KEYS_INVALID`, `TREATMENT_SYSTEM_INVALID`, `DELEGATION_FRAME_FORBIDDEN_FOR_A`, `DELEGATION_FRAME_REQUIRED`, `DELEGATION_FRAME_AXES_INVALID`, `DELEGATION_FRAME_ENUM_INVALID`, `FRAME_EVIDENCE_SPAN_REQUIRED`, `FRAME_EVIDENCE_SPAN_INVALID`, `FRAME_EVIDENCE_TEXT_NOT_UTF8`, `FRAME_EVIDENCE_TEXT_MISMATCH`, `ADJUDICATION_INVALID`, and warning `FRAME_COMBINATION_REQUIRES_REVIEW`.
- Files: `src/specchoice_treatments/{__init__.py,schema.py}`, `config/treatments/{delegation-frame-contract-v1.json,frame-advisory-patterns-v1.json}`, `fixtures/treatments/{frame-source-v1.txt,frame-response-b-valid-v1.json,frame-response-adversarial-v1.json}`, and `tests/test_treatments_frame.py`.
- CLI flags: none; Wave 1 creates no command surface.

<tasks>

<task type="tracer" tdd="true">
  <name>04-01-01: Parse one valid source-bound B response end to end</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py, experiments/specchoice-v1.3.2/config/treatments/delegation-frame-contract-v1.json, experiments/specchoice-v1.3.2/fixtures/treatments/frame-source-v1.txt, experiments/specchoice-v1.3.2/fixtures/treatments/frame-response-b-valid-v1.json, experiments/specchoice-v1.3.2/tests/test_treatments_frame.py</files>
  <read_first>
    - .planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-CONTEXT.md
    - experiments/specchoice-v1.3.2/src/specchoice_measurement/strict_json.py
    - experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
    - experiments/specchoice-v1.3.2/src/specchoice_evidence/canonical.py
    - experiments/specchoice-v1.3.2/src/specchoice_data/relevance.py
  </read_first>
  <behavior>
    - Test 1: canonical B JSON with exactly three axes and exact raw byte slices returns one valid `ParsedTreatmentResponse` whose canonical projection is reproducible.
    - Test 2: `unknown` is valid for authority and choice_space_origin; JSON member insertion order does not alter the projection or diagnostics.
    - Test 3: each axis keeps its own evidence record even when two spans are equal or exactly adjacent.
  </behavior>
  <action>
1. Write the three tests first and prove they fail because `specchoice_treatments.schema` does not exist.
2. Freeze `delegation-frame-contract-v1.json` as canonical JSON. Require the exact response/axis/span/adjudication keys and enums listed above; system A has no frame key, while B/C have exactly the three required axes. Use no optional metadata axis, scope field, tolerance, coercion, or dependency.
3. Implement strict UTF-8 JSON decoding by calling `decode_strict_json`; translate decode/duplicate/non-finite failures to `TreatmentContractError("TREATMENT_JSON_INVALID")`. Validate integer offsets with the existing bool-exclusion rule, require `0 <= start_byte < end_byte <= len(target_raw)`, decode only that raw slice, and compare the decoded text byte-for-byte to the claimed text. Compute `target_sha256` from untouched target bytes.
4. Implement the canonical surfaced adjudication states: `surfaced=false` requires null status/name and empty evidence; `surfaced=true` requires a permitted status, non-empty rationale, and at least one evidence span. Naming is not required for accept. Preserve parsed values without repair.
5. Add an obviously synthetic LF-only source and one human-authored B contract response whose three evidence spans are real byte slices of that file. Return deterministic blocker diagnostics for traversable shape errors and only construct a valid parsed response when no blocker remains.
  </action>
  <acceptance_criteria>
    - H1-01 B happy path passes with exactly three frozen axes and one exact raw-source span per axis.
    - Missing/extra top-level, frame, axis, span, or adjudication keys cannot enter the parsed projection.
    - Byte offsets reject bool, negative, zero-length, reversed, split-code-point, and out-of-range values; null/empty response and empty span text are invalid.
    - Equal/touching spans are validated independently and never merged; canonical JSON order and diagnostic order are stable.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_frame.FrameContractTests.test_valid_b_response_binds_exact_three_axis_spans tests.test_treatments_frame.FrameContractTests.test_unknown_and_key_order_are_deterministic tests.test_treatments_frame.FrameContractTests.test_equal_and_adjacent_spans_remain_axis_scoped -q</automated>
  </verify>
  <done>One production-quality B response travels from raw bytes through strict parsing and exact span verification to a byte-stable canonical projection.</done>
</task>

<task type="auto" tdd="true">
  <name>04-01-02: Close A/B/C boundaries and non-blocking advisory behavior</name>
  <files>experiments/specchoice-v1.3.2/src/specchoice_treatments/__init__.py, experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py, experiments/specchoice-v1.3.2/config/treatments/frame-advisory-patterns-v1.json, experiments/specchoice-v1.3.2/fixtures/treatments/frame-response-adversarial-v1.json, experiments/specchoice-v1.3.2/tests/test_treatments_frame.py</files>
  <read_first>
    - experiments/specchoice-v1.3.2/src/specchoice_treatments/schema.py
    - experiments/specchoice-v1.3.2/config/treatments/delegation-frame-contract-v1.json
    - experiments/specchoice-v1.3.2/fixtures/treatments/frame-response-b-valid-v1.json
    - experiments/specchoice-v1.3.2/src/specchoice_measurement/diagnostics.py
    - /Users/zhdeng/Downloads/LFX_RISCV_SpecChoice_v1.3.2_frozen_execution_baseline.md
  </read_first>
  <behavior>
    - Test 1: A with any frame fails; B/C without a frame or with an extra/missing axis fail; valid C has the identical closed output schema as B.
    - Test 2: duplicate JSON keys, invalid enum values, non-UTF-8 bytes, empty/null inputs, and each span boundary failure produce the declared stable code and no parsed response.
    - Test 3: both preregistered advisory combinations emit one stably ordered warning without changing frame/adjudication values or validity; non-matches emit none.
  </behavior>
  <action>
1. Add adversarial table-driven tests covering every declared error, system boundary, enum edge, and span edge before changing production code.
2. Freeze canonical `frame-advisory-patterns-v1.json` with exact pattern IDs `SOFTWARE_WITH_IMPLEMENTATION_SELECTED_SPACE` and `EXTENSION_GATE_WITH_IMPLEMENTATION_SELECTED_SPACE`, exact `when` maps, and diagnostic `FRAME_COMBINATION_REQUIRES_REVIEW`; reject unknown keys, duplicate pattern IDs, unknown axes/values, alternate diagnostics, or non-canonical bytes.
3. Implement `evaluate_frame_advisories_v1` as a pure post-parse step. It returns warning-severity `Diagnostic` instances ordered by `Diagnostic.sort_key`; warnings are stored separately from blockers and do not participate in the parsed-response validity predicate or rewrite any field.
4. Add a canonical adversarial fixture table containing raw JSON payload text and expected stable code for duplicate key, A-with-frame, B/C missing/extra axis, invalid enum, missing/empty span, bool/negative/equal/reversed/out-of-range offsets, split UTF-8 code point, text mismatch, and malformed adjudication. Keep raw malformed cases as strings/bytes so canonicalization cannot repair them before the test.
5. Export only the named public Wave 1 symbols from `__init__.py`; add no command, filesystem writer, retrieval hook, or provider-facing surface.
  </action>
  <acceptance_criteria>
    - A omits the frame by contract; B and C accept the same exact frame/output schema and reject every other shape.
    - All frozen enum members behave exactly, and absent/null is never translated to unknown.
    - Advisory matches are deterministic warning-only events and do not alter axes, evidence, adjudication, or validity.
    - The complete Wave 1 test module plus canonical regressions pass without new dependencies.
  </acceptance_criteria>
  <verify>
    <automated>cd experiments/specchoice-v1.3.2 &amp;&amp; PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_frame tests.test_canonical -q</automated>
  </verify>
  <done>The closed A/B/C response boundary, all H1-01 edge cases, and advisory-only contract are frozen and executable offline.</done>
</task>

</tasks>

<threat_model>
## Assets

| Asset | Integrity or safety property |
|---|---|
| Raw target bytes and SHA-256 | Span claims must refer to the exact unnormalized source bytes. |
| Three frame axes and adjudication | No coercion, extra axis, or advisory may change semantic output. |
| Canonical diagnostics | Equivalent malformed input produces stable, reviewable failures. |

## Trust Boundaries

| Boundary | Untrusted input crossing it |
|---|---|
| raw response bytes -> strict parser | Arbitrary UTF-8/non-UTF-8, duplicate keys, unknown fields, enums, and offsets. |
| target raw bytes -> evidence validator | Caller-controlled hashes, ranges, and claimed text. |
| advisory config -> parsed response | Pattern data could try to become blocking logic or rewrite semantics. |

## STRIDE Threat Register — ASVS L1, block on High

| Threat ID | Category | Asset / abuse case | Severity | Disposition | Mitigation and verification |
|---|---|---|---|---|---|
| T-04-01-01 | Tampering | Malformed or duplicate-key JSON discards/replaces a frame value. | high | mitigate | `decode_strict_json`, exact-key checks, frozen enums, and adversarial cases in `test_treatments_frame`. |
| T-04-01-02 | Tampering | A forged offset/hash/text claims evidence not present in the raw target. | high | mitigate | Untouched-byte SHA-256 plus half-open range, UTF-8, and exact-text tests, including split-code-point cases. |
| T-04-01-03 | Elevation of Privilege | A warning changes parser validity or final disposition. | high | mitigate | Separate warning collection and before/after equality assertions in advisory tests. |
| T-04-01-04 | Repudiation | Input/member order changes which diagnostics the reviewer sees. | medium | mitigate | `Diagnostic.sort_key` ordering and permutation tests. |
| T-04-01-05 | Information Disclosure | Local test fixture contains synthetic text that could be mistaken for accepted RISC-V evidence. | low | accept | Fixture is visibly synthetic and is not under accepted/preregistration roots; this plan creates no evidence-admission path. |

No High threat is accepted, transferred, or left unresolved.
</threat_model>

<verification>
Quick: `cd experiments/specchoice-v1.3.2 && PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_treatments_frame tests.test_canonical -q`.

Cumulative Wave 1: `cd experiments/specchoice-v1.3.2 && PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest tests.test_data_admission tests.test_data_splits tests.test_data_relevance tests.test_data_h2 tests.test_treatments_frame tests.test_canonical tests.test_filesystem_boundary -q && ruff check --isolated src/specchoice_treatments tests/test_treatments_frame.py`.

Both commands are intended to remain below the 10-second feedback ceiling in `04-VALIDATION.md`.
</verification>

<success_criteria>
- H1-01 is represented by a closed parser contract rather than prompt prose alone.
- One valid B/C-shaped response is proven end to end, while every empty/boundary/adjacency/ordering failure is deterministic.
- A remains frame-free and joint-frame advisories remain non-blocking.
- No dependency, CLI, retrieval behavior, model path, or external action is introduced.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 4 | Exact offline treatment inspection | 04-01 | COVERED | Closed frame/parser is the first executable slice. |
| REQ | H1-01 | Three frozen axes plus verbatim evidence per axis | 04-01 | COVERED | Both tasks cite and test H1-01. |
| RESEARCH | frame/parser | Strict decoder, exact keys/enums, raw spans, stable diagnostics | 04-01 | COVERED | Reuses existing canonical/strict/Diagnostic interfaces. |
| CONTEXT | phase boundary | Frame contract is offline, dependency-light, and non-authorizing | 04-01 | COVERED | No deferred idea or external surface appears. |

<output>
Create `.planning/phases/04-offline-treatments-retrieval-and-branch-freeze/04-01-SUMMARY.md` when done, including exact config/fixture hashes, public symbols, test counts, threat outcomes, and confirmation that no command or external surface was added.
</output>
