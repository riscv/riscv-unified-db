"""
pipeline/ingest/ — Ingestion-stage chunkers.

Exports
-------
    chunker_adoc  — AsciiDoc ISA-manual chunker  (AsciiDocChunker)
    chunker_udb   — UDB YAML/JSON schema chunker (UDBChunker, stub)
"""
